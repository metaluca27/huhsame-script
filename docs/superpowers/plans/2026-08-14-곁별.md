# 곁별(Gyeotbyeol) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 생일만 넣으면 각자 오행 정령으로 바뀌고, 내가 아는 사람들과의 궁합을 성좌 지도로 보여주는 무빌드 정적 웹앱을 만든다.

**Architecture:** `landing/byeol/`에 순수 바닐라 JS로 구성한다. 사주 계산(음력변환·절기·간지)은 vendored된 lunar-javascript 라이브러리에 위임하고, 그 위에서 오행 매핑·정령·궁합·케미·성좌 지도를 직접 만든다. 계산 로직은 순수 함수로 분리해 Node 내장 테스트로 검증하고, 화면은 브라우저에서 확인한다. 데이터는 브라우저 localStorage에만 저장(서버 없음).

**Tech Stack:** HTML/CSS/바닐라 JS (빌드 도구 없음), lunar-javascript(vendored), Node.js 내장 test runner(`node --test`), Firebase Hosting.

## Global Constraints

- 앱 경로: `landing/byeol/` → 배포 시 `https://vaulted-bus-346411.web.app/byeol`
- 서버·API·외부 네트워크 호출 없음. 모든 계산은 클라이언트 로컬.
- 태어난 시간(시주) 입력받지 않음. 연·월·일 3기둥만 사용.
- 저장은 localStorage만. 외부 전송 없음.
- 오행 키는 영문 소문자 고정: `wood`, `fire`, `earth`, `metal`, `water`.
- 정령 이름 고정: 여울(물/water), 노을(불/fire), 새록(나무/wood), 두름(흙/earth), 빛돌(쇠/metal).
- 관계 5종 키/이름 고정: `danbi`(단비), `hanmulgyeol`(한 물결), `saessakjigi`(새싹지기), `janmulgyeol`(잔물결), `keunbawi`(큰바위).
- 원본(사주도령 귀인지도) 고유 표현("귀인지도", "여름 바다형", "약과", "도령" 등)을 쓰지 않는다.
- 브라우저용 계산 파일은 전역(window)에 노출하되, 파일 끝에 `if (typeof module !== 'undefined') module.exports = {...}` 푸터를 달아 Node에서도 require 가능하게 한다.
- 코드 스타일: 기존 앱(`landing/tarot/app.js`)을 따라 `var`/함수 선언 기반, 화면 전환은 `showScreen(id)` + `.screen.active` 토글.

---

## File Structure

```
landing/byeol/
  index.html      — 화면 3개(입력/내정령/지도) 뼈대 + 스크립트 로드
  styles.css      — 성좌 다크 테마 + 아크릴 정령 + 지도 스타일
  lib/lunar.js    — vendored lunar-javascript (수정 금지)
  data.js         — 오행 매핑·정령·관계·케미·계절 상수 (순수 데이터/헬퍼)
  saju.js         — 생일 → 사주 기둥/일간오행/계절/오행분포 (lunar.js 위임)
  compat.js       — 두 오행 → 관계 5종 판정 + 케미 점수
  people.js       — 사람 목록 조립/정렬/저장 (localStorage)
  map.js          — 성좌 궤도 지도 SVG 렌더
  app.js          — 화면 전환 + 입력 핸들러 + 전체 조립
test/
  data.test.js
  saju.test.js
  compat.test.js
  people.test.js
package.json      — 테스트 스크립트만 (루트, 배포에는 미포함)
```

로드 순서(index.html): `lib/lunar.js → data.js → saju.js → compat.js → people.js → map.js → app.js`

---

## Task 1: 프로젝트 뼈대 + 라이브러리 vendoring + 테스트 인프라

**Files:**
- Create: `package.json`
- Create: `landing/byeol/lib/lunar.js` (다운로드)
- Create: `landing/byeol/index.html`
- Create: `landing/byeol/styles.css`
- Create: `test/smoke.test.js`

**Interfaces:**
- Produces: 전역 `Solar`, `Lunar`(lunar.js), 실행 가능한 `npm test`

- [ ] **Step 1: 루트 package.json 생성**

```json
{
  "name": "gyeotbyeol-tests",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: lunar-javascript 라이브러리 다운로드**

Run:
```bash
mkdir -p landing/byeol/lib && curl -L -o landing/byeol/lib/lunar.js https://unpkg.com/lunar-javascript@1.6.13/lunar.js
```
Expected: `landing/byeol/lib/lunar.js` 생성(수백 KB). 실패 시 버전을 `lunar-javascript@1.6.12` 등으로 조정하거나 `https://cdn.jsdelivr.net/npm/lunar-javascript/lunar.js` 사용.

- [ ] **Step 3: 스모크 테스트 작성 (라이브러리가 Node에서 로드·동작하는지)**

`test/smoke.test.js`:
```javascript
const test = require('node:test');
const assert = require('node:assert');
const { Solar } = require('../landing/byeol/lib/lunar.js');

test('lunar.js: 양력 2000-01-01의 일간 천간을 구한다', () => {
  const lunar = Solar.fromYmd(2000, 1, 1).getLunar();
  const gan = lunar.getDayGan();
  assert.ok(typeof gan === 'string' && gan.length >= 1, '일간 천간이 문자열로 나와야 함');
});

test('lunar.js: 음력→양력 변환이 동작한다', () => {
  const solar = Lunar_fromYmd(2000, 1, 1);
  assert.ok(solar.getYear() >= 1999 && solar.getYear() <= 2001);
});

function Lunar_fromYmd(y, m, d) {
  const { Lunar } = require('../landing/byeol/lib/lunar.js');
  return Lunar.fromYmd(y, m, d).getSolar();
}
```

- [ ] **Step 4: 테스트 실행해서 통과 확인**

Run: `npm test`
Expected: 2 tests PASS. 만약 `require` 결과에 `Solar`가 없으면(전역만 노출하는 빌드) `const L = require('...'); const Solar = L.Solar || global.Solar;` 형태로 접근 방식을 조정하고, 이 접근법을 이후 모든 테스트 파일에 동일 적용.

- [ ] **Step 5: index.html 뼈대 작성 (빈 화면 3개 + 스크립트 로드)**

`landing/byeol/index.html`:
```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>곁별</title>
<meta name="description" content="생일만 넣으면 오행 정령으로 바뀌고, 내 곁의 사람들과 궁합을 성좌 지도로 보여줘요.">
<link rel="stylesheet" href="/byeol/styles.css">
</head>
<body>
<main>
  <section class="screen active" id="screen-intro">
    <h1>곁별</h1>
    <p class="subtitle">생일만 넣으면, 내 곁의 사람들이 별로 떠요</p>
  </section>
  <section class="screen" id="screen-me"></section>
  <section class="screen" id="screen-map"></section>
</main>
<script src="/byeol/lib/lunar.js"></script>
<script src="/byeol/data.js"></script>
<script src="/byeol/saju.js"></script>
<script src="/byeol/compat.js"></script>
<script src="/byeol/people.js"></script>
<script src="/byeol/map.js"></script>
<script src="/byeol/app.js"></script>
</body>
</html>
```
(data.js~app.js는 이후 태스크에서 생성. 이 태스크에서는 빈 파일로 만들어 404를 막는다: `touch landing/byeol/{data,saju,compat,people,map,app}.js`)

- [ ] **Step 6: styles.css 기본 테마 작성**

`landing/byeol/styles.css`:
```css
* { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body {
  min-height: 100dvh;
  overflow-x: hidden;
  background: radial-gradient(circle at 50% 30%, #16204a 0%, #0b1330 55%, #05070f 100%);
  color: #eaf0ff;
  font-family: "Apple SD Gothic Neo", "Malgun Gothic", "맑은 고딕", sans-serif;
}
main { min-height: 100dvh; display: flex; flex-direction: column; }
.screen { display: none; min-height: 100dvh; width: 100%; padding: 28px 20px; align-items: center; text-align: center; }
.screen.active { display: flex; flex-direction: column; }
h1 { font-size: 34px; margin: 8px 0; }
.subtitle { color: #9fb0d6; font-size: 15px; line-height: 1.6; }
button { font-family: inherit; cursor: pointer; }
```

- [ ] **Step 7: 커밋**

```bash
git add package.json landing/byeol test/smoke.test.js
git commit -m "곁별: 프로젝트 뼈대·lunar 라이브러리·테스트 인프라"
```

---

## Task 2: 오행·정령·관계 데이터 (data.js)

**Files:**
- Modify: `landing/byeol/data.js`
- Test: `test/data.test.js`

**Interfaces:**
- Produces:
  - `STEM_WUXING`, `BRANCH_WUXING`: 한자 천간/지지 → 오행 키 맵
  - `SEASON_BY_BRANCH`: 지지 → `'spring'|'summer'|'autumn'|'winter'`
  - `SPIRITS`: 오행키 → `{name, el, hanja, symbol, color1, color2, tagline}`
  - `SEASONS`: 계절키 → `{label, adj, mood}`
  - `RELATIONS`: 관계키 → `{label, emoji, base, span, desc, caution}`
  - `SHENG`(상생 맵), `KE`(상극 맵)
  - `typeName(wuxingKey, seasonKey)` → `"고요한 여울"` 형태 문자열

- [ ] **Step 1: 실패 테스트 작성**

`test/data.test.js`:
```javascript
const test = require('node:test');
const assert = require('node:assert');
const D = require('../landing/byeol/data.js');

test('천간→오행 매핑', () => {
  assert.equal(D.STEM_WUXING['甲'], 'wood');
  assert.equal(D.STEM_WUXING['壬'], 'water');
  assert.equal(D.STEM_WUXING['戊'], 'earth');
});

test('지지→오행 매핑', () => {
  assert.equal(D.BRANCH_WUXING['子'], 'water');
  assert.equal(D.BRANCH_WUXING['午'], 'fire');
  assert.equal(D.BRANCH_WUXING['辰'], 'earth');
});

test('지지→계절 매핑', () => {
  assert.equal(D.SEASON_BY_BRANCH['寅'], 'spring');
  assert.equal(D.SEASON_BY_BRANCH['子'], 'winter');
});

test('정령 5종이 모두 정의됨', () => {
  ['wood','fire','earth','metal','water'].forEach(k => {
    assert.ok(D.SPIRITS[k] && D.SPIRITS[k].name, k + ' 정령 필요');
  });
  assert.equal(D.SPIRITS['water'].name, '여울');
  assert.equal(D.SPIRITS['metal'].name, '빛돌');
});

test('관계 5종이 모두 정의됨', () => {
  ['danbi','hanmulgyeol','saessakjigi','janmulgyeol','keunbawi'].forEach(k => {
    assert.ok(D.RELATIONS[k] && typeof D.RELATIONS[k].base === 'number');
  });
});

test('상생·상극 맵', () => {
  assert.equal(D.SHENG['water'], 'wood');   // 수생목
  assert.equal(D.KE['water'], 'fire');      // 수극화
});

test('유형 이름 조합', () => {
  assert.equal(D.typeName('water', 'winter'), '고요한 여울');
  assert.equal(D.typeName('wood', 'spring'), '움트는 새록');
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/data.test.js`
Expected: FAIL (data.js 비어있어 export 없음)

- [ ] **Step 3: data.js 구현**

`landing/byeol/data.js`:
```javascript
var STEM_WUXING = {
  '甲':'wood','乙':'wood','丙':'fire','丁':'fire','戊':'earth',
  '己':'earth','庚':'metal','辛':'metal','壬':'water','癸':'water'
};
var BRANCH_WUXING = {
  '寅':'wood','卯':'wood','巳':'fire','午':'fire',
  '辰':'earth','戌':'earth','丑':'earth','未':'earth',
  '申':'metal','酉':'metal','亥':'water','子':'water'
};
var SEASON_BY_BRANCH = {
  '寅':'spring','卯':'spring','辰':'spring',
  '巳':'summer','午':'summer','未':'summer',
  '申':'autumn','酉':'autumn','戌':'autumn',
  '亥':'winter','子':'winter','丑':'winter'
};
var SHENG = { wood:'fire', fire:'earth', earth:'metal', metal:'water', water:'wood' };
var KE    = { wood:'earth', earth:'water', water:'fire', fire:'metal', metal:'wood' };

var SPIRITS = {
  water: { name:'여울', el:'물', hanja:'水', symbol:'drop',  color1:'#8fe0ff', color2:'#3aa6ea', tagline:'흐름 · 지혜 · 유연' },
  fire:  { name:'노을', el:'불', hanja:'火', symbol:'flame', color1:'#ffb27a', color2:'#ff6f4d', tagline:'열정 · 표현 · 번짐' },
  wood:  { name:'새록', el:'나무', hanja:'木', symbol:'sprout', color1:'#7bec96', color2:'#38b45f', tagline:'자라남 · 챙김' },
  earth: { name:'두름', el:'흙', hanja:'土', symbol:'mount', color1:'#ffdf8f', color2:'#f5b73e', tagline:'안정 · 품음 · 중심' },
  metal: { name:'빛돌', el:'쇠', hanja:'金', symbol:'gem',   color1:'#ffffff', color2:'#c2d0ea', tagline:'결단 · 단단함 · 명료' }
};

var SEASONS = {
  spring: { label:'봄', adj:'움트는',   mood:'막 피어나 설레는 기운' },
  summer: { label:'여름', adj:'무르익은', mood:'한껏 무르익어 왕성한 기운' },
  autumn: { label:'가을', adj:'깊어지는', mood:'차분히 깊어지는 기운' },
  winter: { label:'겨울', adj:'고요한',   mood:'고요히 안으로 모으는 기운' }
};

var RELATIONS = {
  danbi:       { label:'단비',     emoji:'🌟', base:85, span:8, desc:'지칠 때 기운을 채워주는 사람', caution:'기대기만 하면 그 사람이 지쳐요' },
  hanmulgyeol: { label:'한 물결',  emoji:'🤝', base:80, span:7, desc:'설명 없이 손발 맞는 사람', caution:'닮은 만큼 같은 약점도 나눠 가져요' },
  saessakjigi: { label:'새싹지기', emoji:'🌱', base:74, span:7, desc:'내가 마음 써서 키우는 사람', caution:'너무 퍼주면 내 기운이 빠져요' },
  janmulgyeol: { label:'잔물결',   emoji:'🧭', base:70, span:7, desc:'내 옆에서 척척 도와주는 사람', caution:'이끄는 게 지나치면 눌린다 느껴요' },
  keunbawi:    { label:'큰바위',   emoji:'⚡', base:66, span:9, desc:'긴장되지만 나를 다잡아주는 사람', caution:'부딪히면 세게 부딪혀요 — 거리 조절' }
};

function typeName(wuxingKey, seasonKey) {
  return SEASONS[seasonKey].adj + ' ' + SPIRITS[wuxingKey].name;
}

if (typeof module !== 'undefined') {
  module.exports = { STEM_WUXING, BRANCH_WUXING, SEASON_BY_BRANCH, SHENG, KE, SPIRITS, SEASONS, RELATIONS, typeName };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/data.test.js`
Expected: PASS (7 tests)

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol/data.js test/data.test.js
git commit -m "곁별: 오행·정령·관계 데이터 정의"
```

---

## Task 3: 사주 계산 (saju.js)

**Files:**
- Modify: `landing/byeol/saju.js`
- Test: `test/saju.test.js`

**Interfaces:**
- Consumes: `Solar`, `Lunar`(lunar.js), `STEM_WUXING`, `BRANCH_WUXING`, `SEASON_BY_BRANCH`(data.js)
- Produces:
  - `computeSaju({year, month, day, calendar})` → `{ dayWuxing, season, distribution, pillars }`
    - `calendar`: `'solar'` 또는 `'lunar'`
    - `dayWuxing`: 일간 오행 키 (정령 결정)
    - `season`: 계절 키 (월지 기반)
    - `distribution`: `{wood,fire,earth,metal,water}` 오행별 개수(연·월·일 천간3+지지3=6글자)
    - `pillars`: `{year:'甲子', month, day}` 디버그용
  - `countWuxing(stems, branches)` → distribution (순수 함수, 테스트용)

- [ ] **Step 1: 실패 테스트 작성**

`test/saju.test.js`:
```javascript
const test = require('node:test');
const assert = require('node:assert');
const S = require('../landing/byeol/saju.js');

test('countWuxing: 6글자 오행 개수를 센다', () => {
  const dist = S.countWuxing(['甲','丙','戊'], ['子','午','辰']);
  // 甲=wood, 丙=fire, 戊=earth / 子=water, 午=fire, 辰=earth
  assert.equal(dist.wood, 1);
  assert.equal(dist.fire, 2);
  assert.equal(dist.earth, 2);
  assert.equal(dist.water, 1);
  assert.equal(dist.metal, 0);
});

test('computeSaju: 양력 생일에서 일간 오행·계절·분포를 구한다', () => {
  const r = S.computeSaju({ year:1990, month:6, day:15, calendar:'solar' });
  assert.ok(['wood','fire','earth','metal','water'].includes(r.dayWuxing));
  assert.ok(['spring','summer','autumn','winter'].includes(r.season));
  const total = r.distribution.wood + r.distribution.fire + r.distribution.earth + r.distribution.metal + r.distribution.water;
  assert.equal(total, 6, '연월일 6글자 합은 6');
});

test('computeSaju: 음력 입력도 처리한다', () => {
  const r = S.computeSaju({ year:1990, month:5, day:22, calendar:'lunar' });
  assert.ok(r.dayWuxing);
  assert.equal(Object.values(r.distribution).reduce((a,b)=>a+b,0), 6);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/saju.test.js`
Expected: FAIL

- [ ] **Step 3: saju.js 구현**

`landing/byeol/saju.js`:
```javascript
(function (root) {
  var D = (typeof module !== 'undefined') ? require('./data.js') : root;
  var lunarLib = (typeof module !== 'undefined') ? require('./lib/lunar.js') : root;
  var Solar = lunarLib.Solar || root.Solar;
  var Lunar = lunarLib.Lunar || root.Lunar;

  function getLunarFrom(input) {
    if (input.calendar === 'lunar') {
      return Lunar.fromYmd(input.year, input.month, input.day);
    }
    return Solar.fromYmd(input.year, input.month, input.day).getLunar();
  }

  function countWuxing(stems, branches) {
    var dist = { wood:0, fire:0, earth:0, metal:0, water:0 };
    stems.forEach(function (s) { if (D.STEM_WUXING[s]) dist[D.STEM_WUXING[s]]++; });
    branches.forEach(function (b) { if (D.BRANCH_WUXING[b]) dist[D.BRANCH_WUXING[b]]++; });
    return dist;
  }

  function computeSaju(input) {
    var lunar = getLunarFrom(input);
    var ec = lunar.getEightChar();
    var yG = ec.getYearGan(), yZ = ec.getYearZhi();
    var mG = ec.getMonthGan(), mZ = ec.getMonthZhi();
    var dG = ec.getDayGan(), dZ = ec.getDayZhi();
    return {
      dayWuxing: D.STEM_WUXING[dG],
      season: D.SEASON_BY_BRANCH[mZ],
      distribution: countWuxing([yG, mG, dG], [yZ, mZ, dZ]),
      pillars: { year: yG + yZ, month: mG + mZ, day: dG + dZ }
    };
  }

  var api = { computeSaju: computeSaju, countWuxing: countWuxing };
  if (typeof module !== 'undefined') module.exports = api;
  else { root.computeSaju = computeSaju; root.countWuxing = countWuxing; }
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/saju.test.js`
Expected: PASS (3 tests). `getEightChar`/`getYearGan` 등의 메서드명이 다르면 라이브러리 실제 API에 맞춰 조정하고(예: `ec.getYear()`가 `"甲子"` 문자열이면 `.charAt(0)`/`.charAt(1)`로 분리), 스모크 테스트에 검증을 추가한다.

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol/saju.js test/saju.test.js
git commit -m "곁별: 생일→사주(일간오행·계절·오행분포) 계산"
```

---

## Task 4: 궁합 로직 (compat.js)

**Files:**
- Modify: `landing/byeol/compat.js`
- Test: `test/compat.test.js`

**Interfaces:**
- Consumes: `SHENG`, `KE`, `RELATIONS`(data.js)
- Produces:
  - `relationKey(me, other)` → 관계키 (두 오행 키 입력)
  - `chemiScore(relKey, seed)` → 숫자 (base ~ base+span, seed로 결정적)
  - `computeCompat(me, other, seed)` → `{ relationKey, label, emoji, score, desc, caution }`
  - `seedFromBirth({year,month,day})` → 정수 seed

- [ ] **Step 1: 실패 테스트 작성**

`test/compat.test.js`:
```javascript
const test = require('node:test');
const assert = require('node:assert');
const C = require('../landing/byeol/compat.js');

test('relationKey: 나=물 기준 5관계', () => {
  assert.equal(C.relationKey('water', 'metal'), 'danbi');       // 금생수: 상대가 나를 살림
  assert.equal(C.relationKey('water', 'water'), 'hanmulgyeol'); // 같은 오행
  assert.equal(C.relationKey('water', 'wood'), 'saessakjigi');  // 수생목: 내가 키움
  assert.equal(C.relationKey('water', 'fire'), 'janmulgyeol');  // 수극화: 내가 이끎
  assert.equal(C.relationKey('water', 'earth'), 'keunbawi');    // 토극수: 나를 누름
});

test('relationKey: 나=나무 기준도 정확', () => {
  assert.equal(C.relationKey('wood', 'water'), 'danbi');        // 수생목
  assert.equal(C.relationKey('wood', 'fire'), 'saessakjigi');   // 목생화
  assert.equal(C.relationKey('wood', 'earth'), 'janmulgyeol');  // 목극토
  assert.equal(C.relationKey('wood', 'metal'), 'keunbawi');     // 금극목
});

test('chemiScore: 밴드 범위 안, 결정적', () => {
  const s1 = C.chemiScore('danbi', 12345);
  const s2 = C.chemiScore('danbi', 12345);
  assert.equal(s1, s2, '같은 seed면 같은 점수');
  assert.ok(s1 >= 85 && s1 <= 93, '단비 밴드 85~93 안'); // base85 + 0~8
});

test('computeCompat: 통합 결과', () => {
  const r = C.computeCompat('water', 'metal', C.seedFromBirth({year:1988,month:3,day:2}));
  assert.equal(r.relationKey, 'danbi');
  assert.equal(r.label, '단비');
  assert.ok(r.score >= 85);
  assert.ok(r.desc.length > 0);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/compat.test.js`
Expected: FAIL

- [ ] **Step 3: compat.js 구현**

`landing/byeol/compat.js`:
```javascript
(function (root) {
  var D = (typeof module !== 'undefined') ? require('./data.js') : root;

  function relationKey(me, other) {
    if (me === other) return 'hanmulgyeol';
    if (D.SHENG[other] === me) return 'danbi';        // 상대가 나를 생
    if (D.SHENG[me] === other) return 'saessakjigi';  // 내가 상대를 생
    if (D.KE[me] === other) return 'janmulgyeol';     // 내가 상대를 극
    if (D.KE[other] === me) return 'keunbawi';        // 상대가 나를 극
    return 'hanmulgyeol'; // 도달 불가(방어)
  }

  function chemiScore(relKey, seed) {
    var rel = D.RELATIONS[relKey];
    var span = rel.span + 1;
    var h = Math.abs(seed) % span;
    return rel.base + h;
  }

  function seedFromBirth(b) {
    return (b.year * 10000 + b.month * 100 + b.day);
  }

  function computeCompat(me, other, seed) {
    var key = relationKey(me, other);
    var rel = D.RELATIONS[key];
    return {
      relationKey: key,
      label: rel.label,
      emoji: rel.emoji,
      score: chemiScore(key, seed),
      desc: rel.desc,
      caution: rel.caution
    };
  }

  var api = { relationKey: relationKey, chemiScore: chemiScore, seedFromBirth: seedFromBirth, computeCompat: computeCompat };
  if (typeof module !== 'undefined') module.exports = api;
  else { root.relationKey = relationKey; root.chemiScore = chemiScore; root.seedFromBirth = seedFromBirth; root.computeCompat = computeCompat; }
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/compat.test.js`
Expected: PASS (4 tests)

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol/compat.js test/compat.test.js
git commit -m "곁별: 오행 상생상극 궁합 관계·케미 점수 로직"
```

---

## Task 5: 사람 목록 상태 (people.js)

**Files:**
- Modify: `landing/byeol/people.js`
- Test: `test/people.test.js`

**Interfaces:**
- Consumes: `computeSaju`(saju.js), `computeCompat`, `seedFromBirth`(compat.js), `SPIRITS`, `typeName`(data.js)
- Produces:
  - `makePerson(input, myWuxing)` → `{ id, name, birth:{year,month,day,calendar}, wuxing, season, typeName, spiritName, color1, color2, relationKey, label, emoji, score, desc, caution }`
  - `sortByChemi(list)` → 케미 내림차순 새 배열
  - `countByRelation(list)` → `{danbi:n, hanmulgyeol:n, ...}`
  - `saveState(me, list)` / `loadState()` → localStorage `byeol_me`, `byeol_people` (브라우저 전용, 순수부와 분리)

- [ ] **Step 1: 실패 테스트 작성** (순수부만 테스트; localStorage는 브라우저 확인)

`test/people.test.js`:
```javascript
const test = require('node:test');
const assert = require('node:assert');
const P = require('../landing/byeol/people.js');

const mkInput = (name, y, m, d) => ({ name, year:y, month:m, day:d, calendar:'solar' });

test('makePerson: 상대의 정령·관계·케미를 조립한다 (나=물)', () => {
  const p = P.makePerson(mkInput('민지', 1992, 8, 3), 'water');
  assert.ok(p.id);
  assert.equal(p.name, '민지');
  assert.ok(['danbi','hanmulgyeol','saessakjigi','janmulgyeol','keunbawi'].includes(p.relationKey));
  assert.ok(p.spiritName && p.typeName && typeof p.score === 'number');
});

test('sortByChemi: 케미 내림차순', () => {
  const list = [{score:70},{score:90},{score:80}];
  const sorted = P.sortByChemi(list);
  assert.deepEqual(sorted.map(x=>x.score), [90,80,70]);
});

test('countByRelation: 관계별 집계', () => {
  const list = [{relationKey:'danbi'},{relationKey:'danbi'},{relationKey:'keunbawi'}];
  const c = P.countByRelation(list);
  assert.equal(c.danbi, 2);
  assert.equal(c.keunbawi, 1);
  assert.equal(c.hanmulgyeol, 0);
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `node --test test/people.test.js`
Expected: FAIL

- [ ] **Step 3: people.js 구현**

`landing/byeol/people.js`:
```javascript
(function (root) {
  var isNode = (typeof module !== 'undefined');
  var D = isNode ? require('./data.js') : root;
  var Saju = isNode ? require('./saju.js') : root;
  var Compat = isNode ? require('./compat.js') : root;

  var _seq = 0;
  function nextId() { _seq++; return 'p' + Date.now().toString(36) + _seq; }

  function makePerson(input, myWuxing) {
    var saju = Saju.computeSaju(input);
    var seed = Compat.seedFromBirth(input);
    var compat = Compat.computeCompat(myWuxing, saju.dayWuxing, seed);
    var sp = D.SPIRITS[saju.dayWuxing];
    return {
      id: nextId(),
      name: input.name,
      birth: { year: input.year, month: input.month, day: input.day, calendar: input.calendar },
      wuxing: saju.dayWuxing,
      season: saju.season,
      typeName: D.typeName(saju.dayWuxing, saju.season),
      spiritName: sp.name,
      color1: sp.color1, color2: sp.color2,
      relationKey: compat.relationKey,
      label: compat.label, emoji: compat.emoji, score: compat.score,
      desc: compat.desc, caution: compat.caution
    };
  }

  function sortByChemi(list) {
    return list.slice().sort(function (a, b) { return b.score - a.score; });
  }

  function countByRelation(list) {
    var c = { danbi:0, hanmulgyeol:0, saessakjigi:0, janmulgyeol:0, keunbawi:0 };
    list.forEach(function (p) { if (c[p.relationKey] != null) c[p.relationKey]++; });
    return c;
  }

  function saveState(me, list) {
    if (typeof localStorage === 'undefined') return;
    localStorage.setItem('byeol_me', JSON.stringify(me));
    localStorage.setItem('byeol_people', JSON.stringify(list));
  }
  function loadState() {
    if (typeof localStorage === 'undefined') return { me:null, list:[] };
    var me = null, list = [];
    try { me = JSON.parse(localStorage.getItem('byeol_me')); } catch (e) {}
    try { list = JSON.parse(localStorage.getItem('byeol_people')) || []; } catch (e) {}
    return { me: me, list: list };
  }

  var api = { makePerson, sortByChemi, countByRelation, saveState, loadState };
  if (isNode) module.exports = api;
  else { root.makePerson = makePerson; root.sortByChemi = sortByChemi; root.countByRelation = countByRelation; root.saveState = saveState; root.loadState = loadState; }
})(typeof window !== 'undefined' ? window : globalThis);
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `node --test test/people.test.js`
Expected: PASS (3 tests)

- [ ] **Step 5: 전체 테스트 재확인 + 커밋**

Run: `npm test`
Expected: 모든 테스트 PASS
```bash
git add landing/byeol/people.js test/people.test.js
git commit -m "곁별: 사람 목록 조립·정렬·집계·저장"
```

---

## Task 6: 입력 화면 + 내 정령 결과 (index.html, app.js)

**Files:**
- Modify: `landing/byeol/index.html` (screen-intro, screen-me 내용)
- Modify: `landing/byeol/app.js`
- Modify: `landing/byeol/styles.css` (폼·정령 카드 스타일)

**Interfaces:**
- Consumes: `computeSaju`(saju.js), `SPIRITS`, `SEASONS`, `typeName`(data.js), `saveState`/`loadState`(people.js)
- Produces: 전역 `showScreen(id)`, `state = { me: null, people: [] }`, `renderMe()`

- [ ] **Step 1: index.html의 screen-intro/screen-me 작성**

`landing/byeol/index.html`의 두 섹션 교체:
```html
  <section class="screen active" id="screen-intro">
    <h1>곁별 ⭐</h1>
    <p class="subtitle">생일만 넣으면 내 오행 정령이 나오고,<br>내 곁의 사람들이 별로 떠요</p>
    <div class="form">
      <input type="text" id="me-name" class="fld" placeholder="내 이름 또는 별명" maxlength="12">
      <input type="number" id="me-year" class="fld" placeholder="태어난 해 (예: 1990)" min="1900" max="2099">
      <div class="row">
        <input type="number" id="me-month" class="fld" placeholder="월" min="1" max="12">
        <input type="number" id="me-day" class="fld" placeholder="일" min="1" max="31">
      </div>
      <div class="seg" id="me-cal">
        <button type="button" class="seg-btn active" data-cal="solar">양력</button>
        <button type="button" class="seg-btn" data-cal="lunar">음력</button>
      </div>
      <p class="hint">태어난 시간은 안 물어봐요. 생년월일만 있으면 돼요.</p>
      <button id="btn-me">내 정령 보기</button>
    </div>
  </section>

  <section class="screen" id="screen-me">
    <div id="me-card"></div>
    <button id="btn-to-add">곁의 사람 추가하러 가기</button>
  </section>
```

- [ ] **Step 2: 폼/정령 카드 스타일 추가**

`landing/byeol/styles.css`에 추가:
```css
.form { width: 100%; max-width: 360px; display: flex; flex-direction: column; gap: 12px; margin-top: 22px; }
.fld { width: 100%; padding: 14px 16px; border-radius: 14px; border: 1px solid #2a3559; background: #0e1630; color: #eaf0ff; font-size: 16px; }
.fld::placeholder { color: #6f7ea8; }
.row { display: flex; gap: 12px; }
.row .fld { flex: 1; }
.seg { display: flex; gap: 8px; }
.seg-btn { flex: 1; padding: 12px; border-radius: 12px; border: 1px solid #2a3559; background: #0e1630; color: #9fb0d6; font-size: 15px; }
.seg-btn.active { background: #1c2650; color: #fff; border-color: #5b7cff; }
.hint { color: #7f8fbb; font-size: 12.5px; margin: 2px 0; }
button#btn-me, button#btn-to-add, button#btn-add, button#btn-to-map {
  padding: 15px; border-radius: 14px; border: none; background: linear-gradient(135deg,#5b7cff,#8f6bff);
  color: #fff; font-size: 16px; font-weight: 700; margin-top: 6px;
}
.spirit-card { max-width: 360px; margin: 0 auto; }
.spirit-badge { width: 110px; height: 110px; margin: 0 auto 14px; border-radius: 30px; position: relative;
  display: flex; align-items: center; justify-content: center; box-shadow: inset 0 2px 8px #fff9, 0 12px 26px #0006; }
.spirit-badge svg { width: 56px; height: 56px; }
.spirit-name { font-size: 26px; font-weight: 800; margin: 4px 0; }
.spirit-sub { color: #9fb0d6; font-size: 14px; }
.dist { display: flex; gap: 6px; justify-content: center; margin: 16px 0; flex-wrap: wrap; }
.dist-item { font-size: 12px; padding: 6px 10px; border-radius: 999px; background: #141c3c; border: 1px solid #28325a; }
.dist-note { color: #c8d3f0; font-size: 13.5px; line-height: 1.6; }
```

- [ ] **Step 3: app.js에 입력 처리 + 내 정령 렌더 작성**

`landing/byeol/app.js`:
```javascript
// 곁별 — 화면 전환 + 입력 + 조립
var state = { me: null, people: [] };
var pendingCal = 'solar';

function showScreen(id) {
  document.querySelectorAll('.screen').forEach(function (s) { s.classList.remove('active'); });
  var t = document.getElementById('screen-' + id);
  if (t) t.classList.add('active');
  window.scrollTo(0, 0);
}

var WUXING_ORDER = ['wood','fire','earth','metal','water'];
var WUXING_KO = { wood:'나무', fire:'불', earth:'흙', metal:'쇠', water:'물' };

function spiritSymbolSVG(symbol) {
  var paths = {
    drop:  '<path fill="#fff" d="M12 2.5C12 2.5 5 11 5 15.5A7 7 0 0 0 19 15.5C19 11 12 2.5 12 2.5z"/>',
    flame: '<path fill="#fff" d="M13 2c.6 3-1.3 4.6-2.8 6.2C8.8 9.7 7 11.4 7 14a6 6 0 0 0 12 0c0-2.3-1.2-3.7-2.3-5-.4 1-1.2 1.6-2 1.6 1-2.3.3-5.6-1.7-8.6z"/>',
    sprout:'<path fill="#fff" d="M12 2c-3.3 0-6 2.5-6 5.6 0 .5.1 1 .2 1.5C4.4 9.8 3 11.5 3 13.6 3 16.3 5.3 18 8 18h3v4h2v-4h1c2.8 0 5-1.8 5-4.4 0-2-1.3-3.7-3.2-4.4.1-.5.2-1 .2-1.6C16 4.5 15.3 2 12 2z"/>',
    mount: '<path fill="#fff" d="M2 20L9 7l3.5 6.5L15 9l7 11H2z"/>',
    gem:   '<path fill="#fff" d="M7 3h10l4 6-9 12L3 9l4-6z"/>'
  };
  return '<svg viewBox="0 0 24 24">' + (paths[symbol] || '') + '</svg>';
}

function distributionNote(dist) {
  var max = null, min = null;
  WUXING_ORDER.forEach(function (k) {
    if (max === null || dist[k] > dist[max]) max = k;
    if (min === null || dist[k] < dist[min]) min = k;
  });
  return '내 사주엔 ' + WUXING_KO[max] + '(이/가) 많고 ' + WUXING_KO[min] + '(이/가) 적어요.';
}

function renderMe() {
  var me = state.me;
  var sp = SPIRITS[me.wuxing];
  var badge = '<div class="spirit-badge" style="background:linear-gradient(155deg,' + sp.color1 + ',' + sp.color2 + ')">' + spiritSymbolSVG(sp.symbol) + '</div>';
  var dist = me.distribution;
  var chips = WUXING_ORDER.map(function (k) {
    return '<span class="dist-item">' + WUXING_KO[k] + ' ' + dist[k] + '</span>';
  }).join('');
  document.getElementById('me-card').innerHTML =
    '<div class="spirit-card">' + badge +
    '<div class="spirit-name">' + me.typeName + '</div>' +
    '<div class="spirit-sub">' + sp.el + '(' + sp.hanja + ') 정령 · ' + sp.tagline + '</div>' +
    '<div class="dist">' + chips + '</div>' +
    '<p class="dist-note">' + distributionNote(dist) + '</p></div>';
}

function readMeInput() {
  return {
    name: (document.getElementById('me-name').value || '나').trim(),
    year: parseInt(document.getElementById('me-year').value, 10),
    month: parseInt(document.getElementById('me-month').value, 10),
    day: parseInt(document.getElementById('me-day').value, 10),
    calendar: pendingCal
  };
}

function initEvents() {
  document.querySelectorAll('#me-cal .seg-btn').forEach(function (b) {
    b.addEventListener('click', function () {
      document.querySelectorAll('#me-cal .seg-btn').forEach(function (x) { x.classList.remove('active'); });
      b.classList.add('active');
      pendingCal = b.getAttribute('data-cal');
    });
  });
  document.getElementById('btn-me').addEventListener('click', function () {
    var input = readMeInput();
    if (!input.year || !input.month || !input.day) { alert('생년월일을 다 넣어주세요'); return; }
    var saju = computeSaju(input);
    var sp = SPIRITS[saju.dayWuxing];
    state.me = {
      name: input.name, birth: input, wuxing: saju.dayWuxing, season: saju.season,
      typeName: typeName(saju.dayWuxing, saju.season), distribution: saju.distribution,
      color1: sp.color1, color2: sp.color2
    };
    renderMe();
    showScreen('me');
    saveState(state.me, state.people);
  });
  document.getElementById('btn-to-add').addEventListener('click', function () { showScreen('add'); });
}

function boot() {
  var saved = loadState();
  if (saved.me) { state.me = saved.me; state.people = saved.list || []; }
  initEvents();
}
document.addEventListener('DOMContentLoaded', boot);
```

- [ ] **Step 4: 브라우저에서 확인**

`.claude/launch.json`에 곁별 정적 서버 설정을 추가하고(포트 5178) preview_start로 띄운 뒤, 이름·생년월일 입력 → "내 정령 보기" → 정령 카드(유형명·오행분포)가 뜨는지 확인. 콘솔 에러 없어야 함.

launch.json 항목:
```json
{ "name": "곁별", "runtimeExecutable": "npx", "runtimeArgs": ["-y","serve","landing","-l","5178"], "port": 5178, "url": "http://localhost:5178/byeol" }
```

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol .claude/launch.json
git commit -m "곁별: 생일 입력 화면·내 정령 결과 렌더"
```

---

## Task 7: 곁의 사람 추가 화면 + 목록 (index.html, app.js)

**Files:**
- Modify: `landing/byeol/index.html` (screen-add 추가)
- Modify: `landing/byeol/app.js`
- Modify: `landing/byeol/styles.css`

**Interfaces:**
- Consumes: `makePerson`(people.js), `state`, `saveState`
- Produces: `renderPeopleList()`, `addPersonFromInput()`

- [ ] **Step 1: screen-add 섹션 추가**

`index.html`의 screen-me 뒤에 삽입:
```html
  <section class="screen" id="screen-add">
    <h2>곁의 사람 추가</h2>
    <p class="subtitle">아는 사람 생일을 넣으면 나와의 궁합이 지도에 떠요</p>
    <div class="form">
      <input type="text" id="add-name" class="fld" placeholder="이름 또는 별명" maxlength="12">
      <input type="number" id="add-year" class="fld" placeholder="태어난 해">
      <div class="row">
        <input type="number" id="add-month" class="fld" placeholder="월" min="1" max="12">
        <input type="number" id="add-day" class="fld" placeholder="일" min="1" max="31">
      </div>
      <div class="seg" id="add-cal">
        <button type="button" class="seg-btn active" data-cal="solar">양력</button>
        <button type="button" class="seg-btn" data-cal="lunar">음력</button>
      </div>
      <button id="btn-add">지도에 별로 올리기</button>
    </div>
    <div id="people-list"></div>
    <button id="btn-to-map">성좌 지도 보기</button>
  </section>
```

- [ ] **Step 2: 목록 스타일 추가**

`styles.css`에 추가:
```css
#people-list { width: 100%; max-width: 360px; margin: 18px auto; display: flex; flex-direction: column; gap: 8px; }
.pl-item { display: flex; align-items: center; gap: 10px; padding: 10px 12px; background: #101836; border: 1px solid #28325a; border-radius: 12px; }
.pl-dot { width: 22px; height: 22px; border-radius: 7px; flex: none; }
.pl-name { font-weight: 600; }
.pl-rel { color: #9fb0d6; font-size: 12.5px; margin-left: auto; }
.pl-del { background: none; border: none; color: #6f7ea8; font-size: 18px; }
```

- [ ] **Step 3: app.js에 추가/목록 로직 작성**

`app.js`에 함수 추가 + initEvents 확장:
```javascript
var addCal = 'solar';

function renderPeopleList() {
  var box = document.getElementById('people-list');
  if (!state.people.length) { box.innerHTML = '<p class="hint">아직 아무도 없어요. 위에서 추가해봐요.</p>'; return; }
  box.innerHTML = state.people.map(function (p) {
    return '<div class="pl-item">' +
      '<span class="pl-dot" style="background:linear-gradient(155deg,' + p.color1 + ',' + p.color2 + ')"></span>' +
      '<span class="pl-name">' + p.name + '</span>' +
      '<span class="pl-rel">' + p.emoji + ' ' + p.label + ' · ' + p.score + '</span>' +
      '<button class="pl-del" data-id="' + p.id + '">×</button></div>';
  }).join('');
  box.querySelectorAll('.pl-del').forEach(function (b) {
    b.addEventListener('click', function () {
      state.people = state.people.filter(function (x) { return x.id !== b.getAttribute('data-id'); });
      saveState(state.me, state.people);
      renderPeopleList();
    });
  });
}

function addPersonFromInput() {
  var input = {
    name: (document.getElementById('add-name').value || '이름 없음').trim(),
    year: parseInt(document.getElementById('add-year').value, 10),
    month: parseInt(document.getElementById('add-month').value, 10),
    day: parseInt(document.getElementById('add-day').value, 10),
    calendar: addCal
  };
  if (!input.year || !input.month || !input.day) { alert('생년월일을 다 넣어주세요'); return; }
  var person = makePerson(input, state.me.wuxing);
  state.people.push(person);
  saveState(state.me, state.people);
  ['add-name','add-year','add-month','add-day'].forEach(function (id) { document.getElementById(id).value = ''; });
  renderPeopleList();
}
```
initEvents() 끝에 추가:
```javascript
  document.querySelectorAll('#add-cal .seg-btn').forEach(function (b) {
    b.addEventListener('click', function () {
      document.querySelectorAll('#add-cal .seg-btn').forEach(function (x) { x.classList.remove('active'); });
      b.classList.add('active'); addCal = b.getAttribute('data-cal');
    });
  });
  document.getElementById('btn-add').addEventListener('click', addPersonFromInput);
  document.getElementById('btn-to-add').addEventListener('click', function () { renderPeopleList(); showScreen('add'); });
  document.getElementById('btn-to-map').addEventListener('click', function () { renderMap(); showScreen('map'); });
```
(기존 btn-to-add 핸들러는 이 버전으로 대체)

- [ ] **Step 4: 브라우저 확인**

preview 새로고침 → 내 정령 후 사람 추가 → 목록에 관계/케미 뜨고, ×로 삭제되고, 새로고침 후에도 유지(localStorage)되는지 확인.

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol
git commit -m "곁별: 곁의 사람 추가·목록·삭제·저장"
```

---

## Task 8: 성좌 궤도 지도 (map.js)

**Files:**
- Modify: `landing/byeol/map.js`
- Modify: `landing/byeol/index.html` (screen-map 내용)
- Modify: `landing/byeol/styles.css`

**Interfaces:**
- Consumes: `state`(app.js), `sortByChemi`(people.js), `SPIRITS`
- Produces: 전역 `renderMap()` — `#byeol-map`에 SVG 성좌 지도 그림

- [ ] **Step 1: screen-map 뼈대**

`index.html`의 screen-map 교체:
```html
  <section class="screen" id="screen-map">
    <h2 id="map-title">내 곁의 별</h2>
    <div id="byeol-map"></div>
    <div id="map-counts"></div>
    <button id="btn-back-add">사람 더 추가</button>
  </section>
```

- [ ] **Step 2: 지도 스타일**

`styles.css`에 추가:
```css
#byeol-map { width: 100%; max-width: 420px; margin: 8px auto; }
#byeol-map svg { width: 100%; height: auto; border-radius: 20px; background: radial-gradient(circle at 50% 42%, #1b2a5c 0%, #0b1330 45%, #05070f 100%); }
#map-counts { display: flex; gap: 6px; justify-content: center; flex-wrap: wrap; max-width: 420px; margin: 12px auto; }
.cnt { flex: 1; min-width: 62px; text-align: center; background: #141c3c; border: 1px solid #28325a; border-radius: 12px; padding: 8px 4px; }
.cnt .n { font-size: 18px; font-weight: 800; }
.cnt .l { font-size: 11px; color: #9fb0d6; }
```

- [ ] **Step 3: map.js 구현 (성좌 궤도 배치)**

`landing/byeol/map.js`:
```javascript
(function (root) {
  // 케미 점수 → 중심으로부터의 반지름(높을수록 안쪽). 배치는 각도 균등 분산.
  function radiusFor(score, maxR, minR) {
    var t = Math.max(0, Math.min(1, (score - 66) / (93 - 66))); // 66~93 정규화
    return maxR - t * (maxR - minR); // 높은 점수 = 작은 반지름(안쪽)
  }

  function renderMap() {
    var me = root.state.me;
    var people = root.sortByChemi(root.state.people);
    var W = 420, H = 420, cx = W/2, cy = H/2;
    var svg = ['<svg viewBox="0 0 ' + W + ' ' + H + '">'];

    // 궤도 3겹
    [70,130,185].forEach(function (r) {
      svg.push('<circle cx="'+cx+'" cy="'+cy+'" r="'+r+'" fill="none" stroke="#33406b" stroke-dasharray="2 6" opacity="0.4"/>');
    });
    // 잔별
    var stars = [[50,40],[370,60],[380,360],[60,350],[210,28],[400,200],[30,210]];
    stars.forEach(function (s) { svg.push('<circle cx="'+s[0]+'" cy="'+s[1]+'" r="1.2" fill="#cdd8f5" opacity="0.7"/>'); });

    // 각 사람: 각도 균등 분산 + 케미 반지름
    var n = people.length;
    people.forEach(function (p, i) {
      var ang = (-Math.PI/2) + (i * 2*Math.PI / Math.max(n,1));
      var r = radiusFor(p.score, 185, 62);
      var x = cx + r*Math.cos(ang), y = cy + r*Math.sin(ang);
      var t = Math.max(0, Math.min(1, (p.score-66)/(93-66)));
      var dotR = 7 + t*6;           // 케미 높을수록 큰 별
      var lw = 1 + t*1.4, op = 0.35 + t*0.55; // 연결선 진하기
      svg.push('<line x1="'+cx+'" y1="'+cy+'" x2="'+x.toFixed(1)+'" y2="'+y.toFixed(1)+'" stroke="#8fb4ff" stroke-width="'+lw.toFixed(1)+'" opacity="'+op.toFixed(2)+'"/>');
      svg.push('<circle cx="'+x.toFixed(1)+'" cy="'+y.toFixed(1)+'" r="'+dotR.toFixed(1)+'" fill="'+p.color2+'"/>');
      svg.push('<text x="'+x.toFixed(1)+'" y="'+(y-dotR-4).toFixed(1)+'" text-anchor="middle" font-size="10" fill="#dfe8ff">'+escapeXml(p.name)+'</text>');
    });

    // 중앙: 나
    svg.push('<circle cx="'+cx+'" cy="'+cy+'" r="24" fill="#ffcf5b"/>');
    svg.push('<text x="'+cx+'" y="'+(cy+4)+'" text-anchor="middle" font-size="12" font-weight="700" fill="#5a3d00">나</text>');
    svg.push('</svg>');
    document.getElementById('byeol-map').innerHTML = svg.join('');

    renderCounts(root.countByRelation(root.state.people));
  }

  function renderCounts(c) {
    var R = root.RELATIONS;
    var order = ['danbi','hanmulgyeol','saessakjigi','janmulgyeol','keunbawi'];
    document.getElementById('map-counts').innerHTML = order.map(function (k) {
      return '<div class="cnt"><div class="n">'+c[k]+'</div><div class="l">'+R[k].emoji+' '+R[k].label+'</div></div>';
    }).join('');
  }

  function escapeXml(s) { return String(s).replace(/[<>&]/g, function (ch) { return ({'<':'&lt;','>':'&gt;','&':'&amp;'})[ch]; }); }

  root.renderMap = renderMap;
})(typeof window !== 'undefined' ? window : globalThis);
```
(map.js는 브라우저 전역 `state`, `sortByChemi`, `countByRelation`, `RELATIONS`에 의존 — Node 테스트 대상 아님. `RELATIONS`는 data.js가 window에 노출.)

- [ ] **Step 4: btn-back-add 핸들러 추가**

app.js initEvents()에 추가:
```javascript
  document.getElementById('btn-back-add').addEventListener('click', function () { renderPeopleList(); showScreen('add'); });
```

- [ ] **Step 5: 브라우저 확인**

사람 3~5명 추가 후 "성좌 지도 보기" → 나 중심, 케미 높은 사람이 안쪽·큰 별·진한 선으로 배치되고, 아래 관계 5종 카운트가 맞는지 확인.

- [ ] **Step 6: 커밋**

```bash
git add landing/byeol
git commit -m "곁별: 성좌 궤도 지도 + 관계 카운트"
```

---

## Task 9: 케미 순위 목록 + 별 상세 (app.js, index.html)

**Files:**
- Modify: `landing/byeol/index.html` (screen-map에 순위·상세 영역)
- Modify: `landing/byeol/app.js`
- Modify: `landing/byeol/styles.css`

**Interfaces:**
- Consumes: `sortByChemi`, `state`, `RELATIONS`, `SPIRITS`
- Produces: `renderRanking()`, `showPersonDetail(id)`

- [ ] **Step 1: 순위·상세 영역 추가**

screen-map 안 `#map-counts` 뒤에:
```html
    <h3 class="rank-title">케미 순위</h3>
    <div id="rank-list"></div>
    <div id="person-detail" class="detail-hidden"></div>
```

- [ ] **Step 2: 스타일 추가**

```css
.rank-title { margin: 18px 0 8px; font-size: 16px; }
#rank-list { width: 100%; max-width: 420px; margin: 0 auto; display: flex; flex-direction: column; gap: 8px; }
.rk { display: flex; align-items: center; gap: 10px; padding: 11px 13px; background: #101836; border: 1px solid #28325a; border-radius: 12px; cursor: pointer; }
.rk-medal { font-size: 15px; width: 22px; }
.rk-dot { width: 20px; height: 20px; border-radius: 6px; }
.rk-name { font-weight: 600; }
.rk-meta { margin-left: auto; color: #9fb0d6; font-size: 12.5px; text-align: right; }
.detail-hidden { display: none; }
.detail-open { display: block; position: fixed; left: 0; right: 0; bottom: 0; background: #0e1630; border-top: 1px solid #33406b;
  border-radius: 20px 20px 0 0; padding: 22px; max-width: 460px; margin: 0 auto; box-shadow: 0 -10px 30px #0008; }
.detail-open h4 { margin: 0 0 6px; font-size: 20px; }
.detail-open .d-rel { font-size: 14px; margin: 8px 0; }
.detail-open .d-caution { color: #ffcf5b; font-size: 13.5px; margin-top: 8px; }
.detail-open .d-close { margin-top: 16px; width: 100%; padding: 12px; border-radius: 12px; border: none; background: #1c2650; color: #fff; }
```

- [ ] **Step 3: app.js에 순위·상세 렌더 작성**

```javascript
var MEDALS = ['🥇','🥈','🥉'];

function renderRanking() {
  var ranked = sortByChemi(state.people);
  var box = document.getElementById('rank-list');
  box.innerHTML = ranked.map(function (p, i) {
    return '<div class="rk" data-id="' + p.id + '">' +
      '<span class="rk-medal">' + (MEDALS[i] || (i+1)) + '</span>' +
      '<span class="rk-dot" style="background:linear-gradient(155deg,' + p.color1 + ',' + p.color2 + ')"></span>' +
      '<span class="rk-name">' + p.name + '</span>' +
      '<span class="rk-meta">' + p.emoji + ' ' + p.label + '<br>케미 ' + p.score + '</span></div>';
  }).join('');
  box.querySelectorAll('.rk').forEach(function (row) {
    row.addEventListener('click', function () { showPersonDetail(row.getAttribute('data-id')); });
  });
}

function showPersonDetail(id) {
  var p = state.people.find(function (x) { return x.id === id; });
  if (!p) return;
  var box = document.getElementById('person-detail');
  box.className = 'detail-open';
  box.innerHTML = '<h4>' + p.name + ' · ' + p.typeName + '</h4>' +
    '<div class="spirit-sub">' + p.emoji + ' ' + p.label + ' · 케미 ' + p.score + '</div>' +
    '<p class="d-rel">' + p.desc + '</p>' +
    '<p class="d-caution">⚠ ' + p.caution + '</p>' +
    '<button class="d-close">닫기</button>';
  box.querySelector('.d-close').addEventListener('click', function () { box.className = 'detail-hidden'; });
}
```
`btn-to-map`과 `btn-back-add`의 지도 진입 시 `renderRanking()`도 호출하도록 수정:
```javascript
  document.getElementById('btn-to-map').addEventListener('click', function () { renderMap(); renderRanking(); showScreen('map'); });
```

- [ ] **Step 4: 브라우저 확인**

지도 화면에서 케미 순위 목록(메달·관계·점수)이 뜨고, 행을 누르면 하단 상세(설명·조심할 점)가 열리고 닫히는지 확인.

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol
git commit -m "곁별: 케미 순위 목록·별 상세 시트"
```

---

## Task 10: 마무리(메타/OG/문구) + 배포

**Files:**
- Modify: `landing/byeol/index.html` (OG 메타)
- Create: `landing/byeol/images/og.png` (임시/플레이스홀더 가능)
- Modify: `landing/byeol/styles.css` (반응형 점검)

**Interfaces:** 없음(마무리)

- [ ] **Step 1: OG/메타 태그 보강**

`index.html` `<head>`에 추가:
```html
<meta property="og:type" content="website">
<meta property="og:url" content="https://vaulted-bus-346411.web.app/byeol">
<meta property="og:title" content="곁별 ⭐ 내 곁의 별">
<meta property="og:description" content="생일만 넣으면 오행 정령이 되고, 내 곁의 사람들과 궁합을 성좌 지도로 봐요">
<meta property="og:image" content="https://vaulted-bus-346411.web.app/byeol/images/og.png">
<meta name="twitter:card" content="summary_large_image">
```

- [ ] **Step 2: 전체 테스트 재확인**

Run: `npm test`
Expected: 모든 테스트 PASS

- [ ] **Step 3: 모바일 뷰 점검**

preview를 mobile(375px)로 리사이즈해 입력·정령·지도·순위가 가로 스크롤 없이 보이는지 확인. 깨지면 styles.css 조정.

- [ ] **Step 4: 배포 (사용자 승인 후)**

Run:
```bash
firebase deploy --only hosting --project vaulted-bus-346411
```
Expected: 배포 성공. `https://vaulted-bus-346411.web.app/byeol` 접속해 전체 흐름(입력→정령→추가→지도→순위) 동작 확인.
※ 배포는 외부 공개 행위이므로 실행 전 사용자에게 확인받는다.

- [ ] **Step 5: 커밋**

```bash
git add landing/byeol
git commit -m "곁별: OG 메타·반응형 마무리 및 /byeol 배포"
```

---

## 남은 튜닝(구현 후 별도)

- 케미 점수를 생일 해시 대신 음양/계절 조화 등 의미있는 요소로 고도화
- 정령 실제 일러스트(아크릴 굿즈) 교체 — 현재는 SVG 심볼
- 계절별 성격 카피 세분화(현재는 계절 mood 공통 문구)
- v2: "당신 근처의 별"(위치 기반) — 설계 문서 9장 참고

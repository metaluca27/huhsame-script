# my scan2677 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 사진 속 글자를 추출·손글씨 변환·번역하는 모바일 웹앱 "my scan2677"을 기존 워커 + Firebase 호스팅 구조에 추가한다.

**Architecture:** 프론트는 `landing/scan/index.html` 단일 파일(다른 미니앱과 동일 패턴). 백엔드는 기존 공용 Cloudflare Worker(`small-recipe-9345`)에 `POST /scan` 엔드포인트 하나 추가 — Gemini 비전 우선, Claude 비전 폴백. 이미지 → base64 → AI 한 번 호출 → `{text, translation}` JSON.

**Tech Stack:** 순수 HTML/CSS/JS (프레임워크 없음), Cloudflare Worker, Gemini 3.5 Flash (비전), Firebase Hosting.

## Global Constraints

- **git 저장소 아님** — 이 프로젝트 루트는 git이 아니다. 모든 "커밋" 단계는 생략한다.
- **테스트 프레임워크 없음** — 단일 HTML 앱 관례에 따라 각 태스크는 수동 검증 단계(정확한 명령/기대 출력)로 마무리한다.
- **기존 워커 코드 불변** — `docs/worker/worker.js`의 기존 엔드포인트(/triage, /recipe, /tarot, /hospitals)와 헬퍼는 한 글자도 수정하지 않는다. 추가만 한다.
- 앱 이름 표기는 정확히 `my scan2677`. 배포 경로는 `/scan`.
- 워커 주소: `https://small-recipe-9345.atlia0318.workers.dev` (프론트 상수로 사용).
- 워커 배포는 사용자가 Cloudflare 대시보드에서 수동으로 한다 (기존 `docs/worker/배포안내.md` 방식). 계획 실행 중 해당 시점에 사용자 체크포인트를 둔다.
- 설계서: `docs/superpowers/specs/2026-07-07-myscan2677-design.md`

---

### Task 1: 워커에 `POST /scan` 엔드포인트 추가

**Files:**
- Modify: `docs/worker/worker.js` (라우팅: 82~85행 `/tarot` 블록 바로 아래에 추가, 함수: 파일 끝 `json()` 함수 앞에 추가, 헤더 주석: 8행 아래에 한 줄 추가)

**Interfaces:**
- Consumes: 기존 상수 `GEMINI_MODEL`, `ANTHROPIC_MODEL`, 기존 헬퍼 `json(obj, status, cors)`
- Produces: `POST /scan` — 요청 `{image: base64문자열, mode: "document"|"handwriting"|"translate", targetLang?: "ko"|"en"|"ja"|"zh", mime?: "image/jpeg"}` → 응답 `{text: string, translation: string}` 또는 `{error: string}`

- [ ] **Step 1: 헤더 주석에 엔드포인트 한 줄 추가**

`worker.js` 8행 (`POST /tarot ...` 줄) 바로 아래에 추가:

```js
     POST /scan        : my scan2677 — 사진 글자 추출/손글씨/번역 (Gemini 비전 또는 Claude)
```

- [ ] **Step 2: 라우팅 추가**

`fetch()` 안, `/tarot` 처리 블록(82~85행) 바로 아래에 추가:

```js
      if (url.pathname === "/scan") {
        if (request.method !== "POST") return json({ error: "method not allowed" }, 405, cors);
        return await handleScan(request, env, cors);
      }
```

- [ ] **Step 3: 스캔 프롬프트·핸들러·비전 호출 함수 추가**

파일 끝의 `function json(...)` 정의 **바로 앞**에 아래 블록 전체를 추가:

```js
// ----- my scan2677 (사진 글자 추출/손글씨/번역) -----
const SCAN_LANG_NAMES = { ko: "한국어", en: "영어", ja: "일본어", zh: "중국어" };

const SCAN_PROMPTS = {
  document: `너는 정확한 OCR 도우미다. 이미지에 보이는 모든 글자를 원문 그대로 추출하라.
- 줄바꿈과 문단 구조를 최대한 유지하라.
- 오탈자를 고치거나 문장을 다듬지 마라. 보이는 그대로 옮겨라.
- 표는 각 행을 한 줄로 쓰고, 칸 사이는 " | "로 구분하라.
- 글자가 전혀 없으면 정확히 "NO_TEXT"라고만 답하라.
- 설명, 인사, 마크다운 코드블록 없이 추출된 텍스트만 출력하라.`,
  handwriting: `너는 손글씨 판독 전문가다. 이미지 속 손으로 쓴 글씨를 텍스트로 변환하라.
- 줄바꿈을 유지하라.
- 판독이 불확실한 글자는 그 자리에 [?]로 표시하라. 임의로 추측해 채우지 마라.
- 글자가 전혀 없으면 정확히 "NO_TEXT"라고만 답하라.
- 설명 없이 변환된 텍스트만 출력하라.`,
};

function scanTranslatePrompt(langName) {
  return `너는 OCR + 번역 도우미다. 두 단계로 작업하라.
1) 이미지에 보이는 글자를 원문 그대로 추출한다 (줄바꿈 유지).
2) 추출한 내용을 ${langName}(으)로 자연스럽게 번역한다.
출력 형식을 정확히 지켜라:
(원문 텍스트)
=====
(${langName} 번역문)
- "=====" 는 반드시 그 줄에 홀로 쓴다.
- 글자가 전혀 없으면 정확히 "NO_TEXT"라고만 답하라.
- 다른 설명은 붙이지 마라.`;
}

async function handleScan(request, env, cors) {
  if (!env.GEMINI_API_KEY && !env.ANTHROPIC_API_KEY) {
    return json({ error: "AI 키 미설정 (GEMINI_API_KEY 또는 ANTHROPIC_API_KEY)" }, 500, cors);
  }
  const body = await request.json().catch(() => ({}));

  let image = typeof body.image === "string" ? body.image : "";
  // dataURL("data:image/jpeg;base64,....")로 오면 접두사 제거
  if (image.startsWith("data:")) {
    const comma = image.indexOf(",");
    image = comma > -1 ? image.slice(comma + 1) : "";
  }
  if (!image) return json({ error: "image(base64) 필요" }, 400, cors);
  if (image.length > 6000000) {
    return json({ error: "이미지가 너무 커요. 다시 촬영해 주세요." }, 413, cors);
  }

  const mode = ["document", "handwriting", "translate"].includes(body.mode) ? body.mode : "document";
  const targetLang = SCAN_LANG_NAMES[body.targetLang] ? body.targetLang : "ko";
  const langName = SCAN_LANG_NAMES[targetLang];
  const system = mode === "translate" ? scanTranslatePrompt(langName) : SCAN_PROMPTS[mode];
  const mime = typeof body.mime === "string" && body.mime.indexOf("image/") === 0 ? body.mime : "image/jpeg";

  let raw;
  try {
    raw = env.GEMINI_API_KEY
      ? await scanGemini(env, system, image, mime)
      : await scanClaude(env, system, image, mime);
  } catch (e) {
    return json(
      { error: "지금은 처리할 수 없어요. 잠시 후 다시 시도해주세요.", detail: String((e && e.message) || e) },
      502,
      cors
    );
  }

  raw = (raw || "").trim();
  if (!raw || raw === "NO_TEXT") return json({ text: "", translation: "" }, 200, cors);

  if (mode === "translate") {
    const m = raw.match(/^=====\s*$/m);
    if (m) {
      const idx = raw.indexOf(m[0]);
      const text = raw.slice(0, idx).trim();
      const translation = raw.slice(idx + m[0].length).trim();
      return json({ text, translation }, 200, cors);
    }
    // 구분선이 없으면 전체를 원문으로 취급
    return json({ text: raw, translation: "" }, 200, cors);
  }
  return json({ text: raw, translation: "" }, 200, cors);
}

async function scanGemini(env, system, imageB64, mime) {
  const apiUrl = `https://generativelanguage.googleapis.com/v1beta/models/${GEMINI_MODEL}:generateContent?key=${env.GEMINI_API_KEY}`;
  const res = await fetch(apiUrl, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({
      system_instruction: { parts: [{ text: system }] },
      contents: [
        {
          role: "user",
          parts: [
            { inline_data: { mime_type: mime, data: imageB64 } },
            { text: "이미지를 처리해줘." },
          ],
        },
      ],
      generationConfig: { maxOutputTokens: 8192, temperature: 0.1 },
    }),
  });
  if (!res.ok) throw new Error("gemini " + res.status);
  const data = await res.json();
  const cand = data.candidates && data.candidates[0];
  if (!cand || cand.finishReason === "SAFETY" || cand.finishReason === "BLOCKLIST") {
    throw new Error("이 사진은 처리할 수 없어요");
  }
  return ((cand.content && cand.content.parts) || []).map((p) => p.text || "").join("").trim();
}

async function scanClaude(env, system, imageB64, mime) {
  const res = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      "x-api-key": env.ANTHROPIC_API_KEY,
      "anthropic-version": "2023-06-01",
    },
    body: JSON.stringify({
      model: ANTHROPIC_MODEL,
      max_tokens: 8192,
      system,
      messages: [
        {
          role: "user",
          content: [
            { type: "image", source: { type: "base64", media_type: mime, data: imageB64 } },
            { type: "text", text: "이미지를 처리해줘." },
          ],
        },
      ],
    }),
  });
  if (!res.ok) throw new Error("anthropic " + res.status);
  const data = await res.json();
  if (data.stop_reason === "refusal") throw new Error("이 사진은 처리할 수 없어요");
  return (data.content || [])
    .filter((b) => b.type === "text")
    .map((b) => b.text)
    .join("\n")
    .trim();
}
```

- [ ] **Step 4: 문법 검사**

Run: `node --check docs/worker/worker.js`
Expected: 아무 출력 없이 종료 (exit 0). 문법 오류가 있으면 해당 줄을 고치고 재실행.

- [ ] **Step 5: 기존 코드 불변 확인**

Run: `node -e "const s=require('fs').readFileSync('docs/worker/worker.js','utf8'); for (const n of ['handleTriage','handleRecipe','handleTarot','handleHospitals','callGemini','callClaude','parseEgenItems']) if(!s.includes('function '+n)) { console.error('MISSING '+n); process.exit(1);} console.log('OK')"`
Expected: `OK`

---

### Task 2: 워커 배포 (사용자 수동) + 원격 스모크 테스트

**Files:**
- Modify: `docs/worker/배포안내.md` (문서 상단 안내 문구와 3단계 테스트 절만 갱신)

**Interfaces:**
- Consumes: Task 1이 완성한 `docs/worker/worker.js` 전체
- Produces: 운영 중인 `https://small-recipe-9345.atlia0318.workers.dev/scan` 엔드포인트 (이후 태스크의 프론트가 호출)

- [ ] **Step 1: 배포안내 문서 갱신**

`docs/worker/배포안내.md`의 6~7행(기존 "이 작업으로 `/tarot`이라는 새 기능만..." 문단)을 아래로 교체:

```markdown
이 작업으로 `/scan`(my scan2677 글자 스캔)이라는 새 기능만 "추가"됩니다. 기존에 잘 쓰고 있는
메디앱(/triage, /hospitals), 전통시장 앱(/recipe), 심연의 타로(/tarot)의 코드는 한 글자도
바뀌지 않았으니 안심하고 진행하셔도 됩니다.
```

그리고 "### /tarot 새 기능 테스트" 절(60~63행)을 아래로 교체:

```markdown
### /scan 새 기능 테스트

`/scan` 자체의 실제 호출 테스트(사진 글자 인식이 잘 되는지)는 이 작업을 요청한 컨트롤러 쪽에서
별도로 진행합니다. 사용자께서는 위의 "기존 앱 정상 동작 확인"까지만 해 주시면 됩니다.
```

- [ ] **Step 2: 사용자 체크포인트 — 워커 배포 요청**

사용자에게 안내: "`docs/worker/배포안내.md` 순서대로 `docs/worker/worker.js` 전체를 Cloudflare 대시보드(`small-recipe-9345` 워커)에 붙여넣고 Deploy 해주세요. 완료되면 알려주세요." **배포 완료 답변을 받기 전에는 Step 3으로 진행하지 않는다.**

- [ ] **Step 3: 스모크 테스트 — 글자 이미지 생성 후 실제 호출**

PowerShell로 실행 (테스트 이미지를 코드로 생성하므로 별도 사진 불필요):

```powershell
Add-Type -AssemblyName System.Drawing
$bmp = New-Object System.Drawing.Bitmap(600, 200)
$g = [System.Drawing.Graphics]::FromImage($bmp)
$g.Clear([System.Drawing.Color]::White)
$font = New-Object System.Drawing.Font("Malgun Gothic", 32)
$g.DrawString("안녕하세요 테스트 123", $font, [System.Drawing.Brushes]::Black, 20, 70)
$g.Dispose()
$tmp = Join-Path $env:TEMP "myscan_test.png"
$bmp.Save($tmp, [System.Drawing.Imaging.ImageFormat]::Png)
$bmp.Dispose()
$b64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes($tmp))
$body = @{ image = $b64; mode = "document"; mime = "image/png" } | ConvertTo-Json
$r = Invoke-RestMethod -Uri "https://small-recipe-9345.atlia0318.workers.dev/scan" -Method Post -ContentType "application/json" -Body $body
$r | ConvertTo-Json -Depth 3
```

Expected: JSON 응답의 `text`에 `테스트`와 `123`이 포함. (`error` 필드가 오면 워커 로그/배포 상태 확인 후 재시도.)

- [ ] **Step 4: 번역 모드 스모크 테스트**

Step 3 변수($b64)가 살아있는 같은 세션에서 실행:

```powershell
$body2 = @{ image = $b64; mode = "translate"; targetLang = "en"; mime = "image/png" } | ConvertTo-Json
$r2 = Invoke-RestMethod -Uri "https://small-recipe-9345.atlia0318.workers.dev/scan" -Method Post -ContentType "application/json" -Body $body2
$r2 | ConvertTo-Json -Depth 3
```

Expected: `text`에 한국어 원문, `translation`에 영어 번역(예: "Hello"류 표현 + "123") 포함.

---

### Task 3: 프론트 `landing/scan/index.html` 작성

**Files:**
- Create: `landing/scan/index.html`

**Interfaces:**
- Consumes: Task 2의 `POST /scan` API (`{image, mode, targetLang, mime}` → `{text, translation}` 또는 `{error}`)
- Produces: `/scan` 경로로 서비스되는 완결된 앱 화면 (다른 태스크가 의존하는 건 경로뿐)

- [ ] **Step 1: 파일 전체 작성**

`landing/scan/index.html`을 아래 내용 그대로 생성:

```html
<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<title>my scan2677 — 사진 속 글자를 텍스트로</title>
<meta name="description" content="사진을 올리면 글자를 뽑아드려요. 문서·손글씨·번역까지 한 번에. my scan2677" />
<link rel="preconnect" href="https://cdn.jsdelivr.net" crossorigin />
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css" />
<style>
  :root{
    --bg:#0e0f12; --bg-soft:#16181d; --fg:#f4f4f5; --fg-dim:#a1a1aa;
    --line:#26272e; --accent:#b794ff; --accent-2:#7c5cff; --radius:16px;
  }
  *{box-sizing:border-box}
  html,body{margin:0;padding:0;background:var(--bg);color:var(--fg);
    font-family:"Pretendard Variable", Pretendard, -apple-system, system-ui, Roboto, sans-serif;
    line-height:1.55;-webkit-font-smoothing:antialiased}
  a{color:inherit;text-decoration:none}
  button{font:inherit;cursor:pointer}
  .wrap{max-width:560px;margin:0 auto;padding:0 16px 60px}

  header{position:sticky;top:0;z-index:10;background:rgba(14,15,18,.85);backdrop-filter:blur(10px);
    border-bottom:1px solid var(--line)}
  .head-inner{max-width:560px;margin:0 auto;display:flex;align-items:center;gap:10px;height:52px;padding:0 16px}
  .back{color:var(--fg-dim);font-size:14px}
  h1{font-size:17px;margin:0;font-weight:800;letter-spacing:-.01em}

  .tabs{display:flex;gap:6px;margin:16px 0 12px}
  .tab{flex:1;padding:10px 0;border-radius:12px;border:1px solid var(--line);background:var(--bg-soft);
    color:var(--fg-dim);font-weight:700;font-size:14px}
  .tab.on{background:var(--accent-2);border-color:var(--accent-2);color:#fff}

  .lang-row{display:none;align-items:center;gap:8px;margin-bottom:12px;font-size:14px;color:var(--fg-dim)}
  .lang-row.show{display:flex}
  .lang-row select{background:var(--bg-soft);color:var(--fg);border:1px solid var(--line);
    border-radius:10px;padding:8px 10px;font:inherit}

  .drop{border:2px dashed var(--line);border-radius:var(--radius);background:var(--bg-soft);
    padding:36px 16px;text-align:center;color:var(--fg-dim);font-size:15px}
  .drop b{color:var(--fg)}
  .drop .icon{font-size:34px;display:block;margin-bottom:8px}

  .preview{display:none;margin-top:12px}
  .preview.show{display:block}
  .preview img{width:100%;border-radius:var(--radius);border:1px solid var(--line);max-height:320px;object-fit:contain;background:#000}
  .preview .re{margin-top:8px;background:transparent;border:1px solid var(--line);color:var(--fg-dim);
    border-radius:10px;padding:8px 12px;font-size:13px}

  .go{display:block;width:100%;margin-top:14px;padding:15px 0;border:none;border-radius:14px;
    background:var(--accent-2);color:#fff;font-weight:800;font-size:16px}
  .go:disabled{opacity:.45}

  .status{display:none;margin-top:14px;text-align:center;color:var(--fg-dim);font-size:14px}
  .status.show{display:block}
  .spin{display:inline-block;width:18px;height:18px;border:2px solid var(--line);border-top-color:var(--accent);
    border-radius:50%;animation:sp 1s linear infinite;vertical-align:-4px;margin-right:8px}
  @keyframes sp{to{transform:rotate(360deg)}}

  .errbox{display:none;margin-top:14px;background:#2a1518;border:1px solid #5c2b31;color:#ffb4bb;
    border-radius:12px;padding:14px;font-size:14px}
  .errbox.show{display:block}
  .errbox button{margin-top:10px;background:#5c2b31;border:none;color:#fff;border-radius:10px;padding:8px 14px;font-size:13px}

  .result{display:none;margin-top:18px}
  .result.show{display:block}
  .result label{font-size:13px;color:var(--fg-dim);display:block;margin:12px 0 6px;font-weight:700}
  textarea{width:100%;min-height:120px;background:var(--bg-soft);color:var(--fg);border:1px solid var(--line);
    border-radius:12px;padding:12px;font:inherit;font-size:15px;resize:vertical}
  .act-row{display:flex;gap:8px;margin-top:12px}
  .act{flex:1;padding:11px 0;border-radius:12px;border:1px solid var(--line);background:var(--bg-soft);
    color:var(--fg);font-weight:700;font-size:14px}
  .act.primary{background:var(--accent-2);border-color:var(--accent-2);color:#fff}

  .hist{margin-top:34px}
  .hist h2{font-size:15px;color:var(--fg-dim);margin:0 0 10px;display:flex;justify-content:space-between;align-items:center}
  .hist h2 button{background:none;border:none;color:var(--fg-dim);font-size:12px;text-decoration:underline}
  .hist-item{background:var(--bg-soft);border:1px solid var(--line);border-radius:12px;padding:12px 14px;
    margin-bottom:8px;font-size:14px}
  .hist-item .meta{color:var(--fg-dim);font-size:12px;margin-bottom:4px;display:flex;justify-content:space-between}
  .hist-item .meta button{background:none;border:none;color:var(--fg-dim);font-size:12px}
  .hist-item .txt{white-space:pre-wrap;word-break:break-all;max-height:72px;overflow:hidden;cursor:pointer}
  .hist-item.open .txt{max-height:none}
  .hist-empty{color:var(--fg-dim);font-size:13px;text-align:center;padding:14px 0}

  .notice{margin-top:30px;color:var(--fg-dim);font-size:12px;text-align:center}
</style>
</head>
<body>

<header>
  <div class="head-inner">
    <a class="back" href="/">← 다락방</a>
    <h1>📷 my scan2677</h1>
  </div>
</header>

<main class="wrap">
  <div class="tabs">
    <button class="tab on" data-mode="document">문서</button>
    <button class="tab" data-mode="handwriting">손글씨</button>
    <button class="tab" data-mode="translate">번역</button>
  </div>

  <div class="lang-row" id="langRow">
    <span>번역할 언어</span>
    <select id="langSel">
      <option value="ko" selected>한국어로</option>
      <option value="en">영어로</option>
      <option value="ja">일본어로</option>
      <option value="zh">중국어로</option>
    </select>
  </div>

  <div class="drop" id="drop">
    <span class="icon">📸</span>
    <b>사진을 선택하거나 촬영하세요</b><br/>
    문서 · 간판 · 손글씨 메모 무엇이든 좋아요
  </div>
  <input type="file" id="file" accept="image/*" hidden />

  <div class="preview" id="preview">
    <img id="pimg" alt="선택한 사진 미리보기" />
    <button class="re" id="rebtn">다른 사진 선택</button>
  </div>

  <button class="go" id="go" disabled>스캔하기</button>

  <div class="status" id="status"><span class="spin"></span>글자를 읽고 있어요…</div>

  <div class="errbox" id="errbox">
    <div id="errmsg"></div>
    <button id="retry">다시 시도</button>
  </div>

  <div class="result" id="result">
    <label for="outText">추출된 글자 (수정 가능)</label>
    <textarea id="outText"></textarea>
    <div id="transWrap" style="display:none">
      <label for="outTrans">번역</label>
      <textarea id="outTrans"></textarea>
    </div>
    <div class="act-row">
      <button class="act primary" id="copyBtn">복사</button>
      <button class="act" id="shareBtn">공유</button>
      <button class="act" id="dlBtn">.txt 저장</button>
    </div>
  </div>

  <div class="hist">
    <h2>최근 스캔 기록 <button id="clearHist">전체 삭제</button></h2>
    <div id="histList"></div>
  </div>

  <p class="notice">사진은 글자 인식 처리에만 사용되고 어디에도 저장되지 않아요.<br/>기록은 이 휴대폰 브라우저에만 저장됩니다.</p>
</main>

<script>
var SCAN_API = "https://small-recipe-9345.atlia0318.workers.dev/scan";
var HIST_KEY = "myscan2677_history";
var MODE_LABEL = { document: "문서", handwriting: "손글씨", translate: "번역" };

var mode = "document";
var imageB64 = null;     // base64 (접두사 없음)
var lastResult = null;   // { text, translation }

var $ = function (id) { return document.getElementById(id); };

// --- 탭 ---
document.querySelectorAll(".tab").forEach(function (t) {
  t.addEventListener("click", function () {
    document.querySelectorAll(".tab").forEach(function (x) { x.classList.remove("on"); });
    t.classList.add("on");
    mode = t.dataset.mode;
    $("langRow").classList.toggle("show", mode === "translate");
  });
});

// --- 사진 선택 + 축소 ---
$("drop").addEventListener("click", function () { $("file").click(); });
$("rebtn").addEventListener("click", function () { $("file").click(); });

$("file").addEventListener("change", function () {
  var f = this.files && this.files[0];
  if (!f) return;
  var img = new Image();
  var url = URL.createObjectURL(f);
  img.onload = function () {
    var MAX = 1600;
    var w = img.width, h = img.height;
    if (Math.max(w, h) > MAX) {
      var r = MAX / Math.max(w, h);
      w = Math.round(w * r); h = Math.round(h * r);
    }
    var cv = document.createElement("canvas");
    cv.width = w; cv.height = h;
    cv.getContext("2d").drawImage(img, 0, 0, w, h);
    var dataUrl = cv.toDataURL("image/jpeg", 0.85);
    imageB64 = dataUrl.split(",")[1];
    $("pimg").src = dataUrl;
    $("preview").classList.add("show");
    $("drop").style.display = "none";
    $("go").disabled = false;
    hide($("result")); hide($("errbox"));
    URL.revokeObjectURL(url);
  };
  img.onerror = function () {
    URL.revokeObjectURL(url);
    showError("이 사진은 열 수 없어요. 다른 사진으로 시도해주세요.");
  };
  img.src = url;
  this.value = "";
});

// --- 스캔 호출 ---
$("go").addEventListener("click", doScan);
$("retry").addEventListener("click", doScan);

function doScan() {
  if (!imageB64) return;
  hide($("errbox")); hide($("result"));
  $("status").classList.add("show");
  $("go").disabled = true;

  fetch(SCAN_API, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ image: imageB64, mode: mode, targetLang: $("langSel").value, mime: "image/jpeg" }),
  })
    .then(function (res) { return res.json().then(function (d) { return { ok: res.ok, d: d }; }); })
    .then(function (r) {
      $("status").classList.remove("show");
      $("go").disabled = false;
      if (!r.ok || r.d.error) { showError(r.d.error || "처리에 실패했어요. 잠시 후 다시 시도해주세요."); return; }
      if (!r.d.text && !r.d.translation) { showError("사진에서 글자를 찾지 못했어요. 글자가 잘 보이게 다시 찍어보세요."); return; }
      lastResult = r.d;
      $("outText").value = r.d.text || "";
      $("outTrans").value = r.d.translation || "";
      $("transWrap").style.display = mode === "translate" && r.d.translation ? "block" : "none";
      $("result").classList.add("show");
      saveHistory(r.d);
    })
    .catch(function () {
      $("status").classList.remove("show");
      $("go").disabled = false;
      showError("인터넷 연결을 확인하고 다시 시도해주세요.");
    });
}

function showError(msg) {
  $("errmsg").textContent = msg;
  $("errbox").classList.add("show");
}
function hide(el) { el.classList.remove("show"); }

// --- 결과: 복사 / 공유 / 다운로드 ---
function currentText() {
  var t = $("outText").value.trim();
  var tr = $("outTrans").value.trim();
  return tr && $("transWrap").style.display !== "none" ? t + "\n\n[번역]\n" + tr : t;
}

$("copyBtn").addEventListener("click", function () {
  navigator.clipboard.writeText(currentText()).then(function () {
    $("copyBtn").textContent = "복사됨!";
    setTimeout(function () { $("copyBtn").textContent = "복사"; }, 1500);
  });
});

$("shareBtn").addEventListener("click", function () {
  if (navigator.share) {
    navigator.share({ text: currentText() }).catch(function () {});
  } else {
    navigator.clipboard.writeText(currentText());
    $("shareBtn").textContent = "복사됨!";
    setTimeout(function () { $("shareBtn").textContent = "공유"; }, 1500);
  }
});

$("dlBtn").addEventListener("click", function () {
  var d = new Date();
  var pad = function (n) { return String(n).padStart(2, "0"); };
  var name = "scan_" + d.getFullYear() + pad(d.getMonth() + 1) + pad(d.getDate()) + "_" + pad(d.getHours()) + pad(d.getMinutes()) + ".txt";
  var blob = new Blob([currentText()], { type: "text/plain;charset=utf-8" });
  var a = document.createElement("a");
  a.href = URL.createObjectURL(blob);
  a.download = name;
  a.click();
  URL.revokeObjectURL(a.href);
});

// --- 기록 (localStorage, 최근 20건, 텍스트만) ---
function loadHistory() {
  try { return JSON.parse(localStorage.getItem(HIST_KEY)) || []; } catch (e) { return []; }
}
function saveHistory(res) {
  var list = loadHistory();
  list.unshift({ t: Date.now(), mode: mode, text: res.text || "", translation: res.translation || "" });
  if (list.length > 20) list = list.slice(0, 20);
  try { localStorage.setItem(HIST_KEY, JSON.stringify(list)); } catch (e) {}
  renderHistory();
}
function renderHistory() {
  var list = loadHistory();
  var box = $("histList");
  box.innerHTML = "";
  if (!list.length) { box.innerHTML = '<div class="hist-empty">아직 스캔 기록이 없어요</div>'; return; }
  list.forEach(function (item, i) {
    var d = new Date(item.t);
    var pad = function (n) { return String(n).padStart(2, "0"); };
    var when = (d.getMonth() + 1) + "/" + d.getDate() + " " + pad(d.getHours()) + ":" + pad(d.getMinutes());
    var el = document.createElement("div");
    el.className = "hist-item";
    var meta = document.createElement("div");
    meta.className = "meta";
    meta.innerHTML = "<span>" + when + " · " + (MODE_LABEL[item.mode] || item.mode) + "</span>";
    var del = document.createElement("button");
    del.textContent = "삭제";
    del.addEventListener("click", function (ev) {
      ev.stopPropagation();
      var l = loadHistory(); l.splice(i, 1);
      try { localStorage.setItem(HIST_KEY, JSON.stringify(l)); } catch (e) {}
      renderHistory();
    });
    meta.appendChild(del);
    var txt = document.createElement("div");
    txt.className = "txt";
    txt.textContent = item.text + (item.translation ? "\n\n[번역]\n" + item.translation : "");
    txt.addEventListener("click", function () { el.classList.toggle("open"); });
    el.appendChild(meta); el.appendChild(txt);
    box.appendChild(el);
  });
}
$("clearHist").addEventListener("click", function () {
  if (!loadHistory().length) return;
  if (confirm("스캔 기록을 모두 삭제할까요?")) {
    localStorage.removeItem(HIST_KEY);
    renderHistory();
  }
});

renderHistory();
</script>

</body>
</html>
```

- [ ] **Step 2: 로컬 브라우저 검증**

Run: `Start-Process "C:\Users\atlia\Desktop\huhsame-script\landing\scan\index.html"`

확인 항목 (사진이 담긴 아무 이미지 파일로 갤러리 선택 경로 테스트):
1. 탭 3개가 보이고, "번역" 탭에서만 언어 선택이 나타난다.
2. 사진 선택 → 미리보기 표시 → "스캔하기" 활성화.
3. 스캔하기 → 로딩 표시 → 결과 텍스트 표시 (워커가 이미 배포되어 있으므로 실제 인식 동작).
4. 복사 버튼 → "복사됨!" 표시. .txt 저장 → 파일 다운로드.
5. 새로고침 후에도 기록 목록에 방금 스캔이 남아 있다. 삭제 버튼 동작.
6. 번역 탭 + 외국어(또는 한국어→영어) 사진 → 원문과 번역 두 칸 표시.

Expected: 전 항목 통과. (file:// 환경에서 클립보드가 막히면 복사 확인은 배포 후 https에서 재확인해도 된다.)

---

### Task 4: 랜딩 페이지 놀이터에 my scan2677 카드 추가

**Files:**
- Modify: `landing/index.html` (293~299행 — `#playground` 섹션의 `.cards` 안)

**Interfaces:**
- Consumes: Task 3의 `/scan` 경로
- Produces: 없음 (링크 노출만)

- [ ] **Step 1: 카드 추가**

`landing/index.html`의 `#playground` 섹션 안, 심연의 타로 카드 `</a>` 바로 아래에 추가:

```html
        <a class="card c3" href="/scan" style="cursor:pointer">
          <div class="dot">📷</div>
          <h3>my scan2677</h3>
          <p>사진 속 글자를 텍스트로. 문서·손글씨는 물론 번역까지, 찰칵 한 번이면 끝나요.</p>
        </a>
```

- [ ] **Step 2: 확인**

Run: `Start-Process "C:\Users\atlia\Desktop\huhsame-script\landing\index.html"`
Expected: "다락방 놀이터" 섹션에 타로 카드 옆(모바일에선 아래)에 my scan2677 카드가 보이고, 클릭 시 /scan 으로 이동(로컬에선 404여도 무방 — 경로만 확인).

---

### Task 5: Firebase 배포 + 실기기 확인

**Files:**
- 없음 (배포만)

**Interfaces:**
- Consumes: Task 3, 4의 결과물 (`landing/` 전체)
- Produces: `https://vaulted-bus-346411.web.app/scan` 운영 페이지

- [ ] **Step 1: 배포**

프로젝트 루트에서 Run: `firebase deploy --only hosting`
Expected: `Deploy complete!` 와 Hosting URL 출력. (`firebase` 명령이 없으면 `npx firebase-tools deploy --only hosting`.)

- [ ] **Step 2: 배포 확인 (데스크톱)**

Run: `Invoke-WebRequest -Uri "https://vaulted-bus-346411.web.app/scan" -UseBasicParsing | Select-Object -ExpandProperty StatusCode`
Expected: `200`

- [ ] **Step 3: 사용자 체크포인트 — 폰 실사용 테스트**

사용자에게 안내: 폰 브라우저에서 `https://vaulted-bus-346411.web.app/scan` 접속 후 설계서의 세 가지 시나리오 확인 —
1. 인쇄물(책/전단) 촬영 → 문서 탭 → 텍스트 추출
2. 손글씨 메모 촬영 → 손글씨 탭 → 변환 (불확실 글자 [?] 표시)
3. 외국어 간판/문구 → 번역 탭 → 원문 + 번역
그리고 복사·공유·txt 저장·기록 저장/삭제 각 버튼 확인. 문제가 있으면 증상을 알려달라고 요청.

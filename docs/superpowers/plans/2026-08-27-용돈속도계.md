# 용돈 속도계 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 이번 달 예산 대비 지출 속도를 보여주는 앱인토스 미니앱을 만든다. 매일 열어보게 만들어 인앱 광고 노출을 반복시키는 것이 목적이다.

**Architecture:** 계산 로직을 `budget-math.js` 한 파일에 순수 함수로 몰아넣고, HTML은 그 결과를 그리기만 한다. 계산이 이 앱의 전부이고 설계 단계에서도 산수를 틀렸으므로 로직만 따로 테스트한다. 미니앱 패키지는 기존 13개와 같은 패턴(루트에 원본 HTML 한 벌 + `sync.mjs`가 미니앱용 `index.html` 생성)을 따른다.

**Tech Stack:** 바닐라 JS(클래식 스크립트), `@apps-in-toss/web-framework` 3.1.1, vite 6, Node 내장 테스트 러너(`node --test`) — 새 의존성 없음.

**설계 문서:** `huhsame-script/docs/superpowers/specs/2026-08-27-용돈속도계-design.md`

## Global Constraints

- 작업 저장소는 `C:\Users\atlia\Documents\metaluca-fresh` 이다. `Desktop\metaluca8560`·`Desktop\_meta_tmp`는 낡은 클론이므로 쓰지 않는다.
- **AI를 호출하지 않는다.** 외부 API 호출이 전혀 없어야 한다. 광고 수익이 목표인데 호출 비용이 노출 단가와 비슷해 사용자가 늘수록 손해가 된다.
- **저장은 `localStorage`만 쓴다.** 서버로 아무것도 보내지 않는다.
- `budget-math.js`는 **클래식 스크립트**로 쓴다(`export` 문 금지). 브라우저에서는 전역 `BudgetMath`로, Node 테스트에서는 `require`로 쓴다. 저장소 루트에 `package.json`이 없어 `.js`는 CommonJS로 해석된다.
- 미니앱 밖으로 나가는 링크(`href="http..."`)를 넣지 않는다. 앱인토스 검수 반려 사유다.
- 금액은 **백원 단위 내림**으로 표시한다.
- 화면 문구는 해요체를 쓴다.
- 기간은 **매월 1일 시작 고정**이다.
- appName은 `yongdon-speed`로 한다(콘솔 등록 후에는 수정 불가).

---

### Task 1: 계산 로직과 테스트

이 앱의 전부다. 순수 함수만 담고 DOM·localStorage를 건드리지 않는다.

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\budget-math.js`
- Test: `C:\Users\atlia\Documents\metaluca-fresh\test\budget-math.test.js`

**Interfaces:**
- Consumes: 없음
- Produces: 전역/모듈 객체 `BudgetMath` 에 아래 함수들
  - `daysInMonth(year, month)` → `number` (month는 1~12)
  - `floorTo100(amount)` → `number`
  - `computeStatus({ budget, remaining, year, month, day })` → 아래 객체
    ```
    {
      totalDays, elapsedDays, leftDays,
      baseline,        // 지금쯤 남아 있어야 할 돈 (백원 내림)
      spent,           // 예산 - 남은 돈
      diff,            // baseline - remaining. 양수면 앞서가는 중
      dailyAverage,    // spent / elapsedDays. elapsedDays 0이면 0
      runOutDay,       // 바닥나는 날짜(1~31). 계산 불가면 null
      state,           // 'ahead' | 'easy' | 'onTrack' | 'over'
      perDayLeft       // 남은 돈 ÷ (남은 날 + 1). 오늘 포함, 하루에 쓸 수 있는 돈
    }
    ```

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test\budget-math.test.js` 파일을 만든다.

```js
const test = require('node:test');
const assert = require('node:assert');
const BudgetMath = require('../budget-math.js');

test('daysInMonth: 달마다 일수가 맞다', () => {
  assert.strictEqual(BudgetMath.daysInMonth(2026, 8), 31);
  assert.strictEqual(BudgetMath.daysInMonth(2026, 9), 30);
  assert.strictEqual(BudgetMath.daysInMonth(2026, 2), 28);
  assert.strictEqual(BudgetMath.daysInMonth(2028, 2), 29); // 윤년
});

test('floorTo100: 백원 단위로 내린다', () => {
  assert.strictEqual(BudgetMath.floorTo100(258064), 258000);
  assert.strictEqual(BudgetMath.floorTo100(99), 0);
  assert.strictEqual(BudgetMath.floorTo100(-1234), -1300); // 음수도 내림
});

// 설계 문서의 예시와 같은 값이어야 한다
test('computeStatus: 8월 15일, 예산 50만, 남은 돈 23만', () => {
  const r = BudgetMath.computeStatus({
    budget: 500000, remaining: 230000, year: 2026, month: 8, day: 15,
  });
  assert.strictEqual(r.totalDays, 31);
  assert.strictEqual(r.elapsedDays, 15);
  assert.strictEqual(r.leftDays, 16);
  assert.strictEqual(r.baseline, 258000);       // 500000 * 16/31 = 258064 → 258000
  assert.strictEqual(r.spent, 270000);
  assert.strictEqual(r.diff, 28000);            // 258000 - 230000
  assert.strictEqual(r.dailyAverage, 18000);    // 270000 / 15
  assert.strictEqual(r.runOutDay, 27);          // 15 + floor(230000/18000)=12
  assert.strictEqual(r.state, 'ahead');
});

test('computeStatus: 아직 한 푼도 안 썼으면 바닥 예상이 없다', () => {
  const r = BudgetMath.computeStatus({
    budget: 500000, remaining: 500000, year: 2026, month: 8, day: 1,
  });
  assert.strictEqual(r.spent, 0);
  assert.strictEqual(r.dailyAverage, 0);
  assert.strictEqual(r.runOutDay, null);        // 0으로 나누지 않는다
  assert.strictEqual(r.perDayLeft, 16129);      // 500000 / 31
  assert.strictEqual(r.state, 'easy');
});

test('computeStatus: 예산을 넘기면 state가 over 이고 남은 돈이 음수다', () => {
  const r = BudgetMath.computeStatus({
    budget: 500000, remaining: -32000, year: 2026, month: 8, day: 28,
  });
  assert.strictEqual(r.state, 'over');
  assert.strictEqual(r.spent, 532000);
});

test('computeStatus: 차이가 예산의 2% 안쪽이면 onTrack', () => {
  // 8월 15일 기준선 258000. 남은 돈 250000이면 차이 8000 (예산의 1.6%)
  const r = BudgetMath.computeStatus({
    budget: 500000, remaining: 250000, year: 2026, month: 8, day: 15,
  });
  assert.strictEqual(r.diff, 8000);
  assert.strictEqual(r.state, 'onTrack');
});

test('computeStatus: 마지막 날이면 남은 날이 0이고 나눗셈이 깨지지 않는다', () => {
  const r = BudgetMath.computeStatus({
    budget: 500000, remaining: 20000, year: 2026, month: 8, day: 31,
  });
  assert.strictEqual(r.leftDays, 0);
  assert.strictEqual(r.baseline, 0);
  assert.strictEqual(r.perDayLeft, 20000);      // 남은 날이 0이면 남은 돈 그대로
  assert.ok(Number.isFinite(r.perDayLeft));
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/budget-math.test.js
```
Expected: FAIL — `Cannot find module '../budget-math.js'`

- [ ] **Step 3: 최소 구현을 쓴다**

`budget-math.js` 파일을 만든다.

```js
// 용돈 속도계 — 계산 로직.
// 클래식 스크립트로 쓴다(export 금지). 브라우저에서는 전역 BudgetMath로,
// Node 테스트에서는 require로 쓴다. DOM과 localStorage를 건드리지 않는다.
var BudgetMath = (function () {
  var ON_TRACK_RATIO = 0.02; // 차이가 예산의 2% 안쪽이면 "딱 맞게"로 본다

  function daysInMonth(year, month) {
    return new Date(year, month, 0).getDate();
  }

  function floorTo100(amount) {
    return Math.floor(amount / 100) * 100;
  }

  function computeStatus(input) {
    var budget = input.budget;
    var remaining = input.remaining;
    var totalDays = daysInMonth(input.year, input.month);
    var elapsedDays = input.day;
    var leftDays = totalDays - elapsedDays;

    var baseline = floorTo100(budget * (leftDays / totalDays));
    var spent = budget - remaining;
    var diff = baseline - remaining;
    var dailyAverage = elapsedDays > 0 ? Math.round(spent / elapsedDays) : 0;

    // 하루 평균이 0 이하면 바닥날 날이 없다. 0으로 나누지 않는다.
    var runOutDay = null;
    if (dailyAverage > 0 && remaining > 0) {
      var daysLeftAtPace = Math.floor(remaining / dailyAverage);
      var candidate = elapsedDays + daysLeftAtPace;
      runOutDay = candidate <= totalDays ? candidate : null;
    }

    // 오늘을 포함해 나눈다. 마지막 날이어도 leftDays+1 이 1이라 0으로 나누지 않는다.
    var perDayLeft = Math.floor(remaining / (leftDays + 1));

    var state;
    if (remaining <= 0) {
      state = 'over';
    } else if (Math.abs(diff) <= budget * ON_TRACK_RATIO) {
      state = 'onTrack';
    } else if (diff > 0) {
      state = 'ahead'; // 기준보다 적게 남음 = 많이 썼다
    } else {
      state = 'easy';
    }

    return {
      totalDays: totalDays,
      elapsedDays: elapsedDays,
      leftDays: leftDays,
      baseline: baseline,
      spent: spent,
      diff: diff,
      dailyAverage: dailyAverage,
      runOutDay: runOutDay,
      state: state,
      perDayLeft: perDayLeft,
    };
  }

  return {
    daysInMonth: daysInMonth,
    floorTo100: floorTo100,
    computeStatus: computeStatus,
  };
})();

if (typeof module !== 'undefined' && module.exports) {
  module.exports = BudgetMath;
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/budget-math.test.js
```
Expected: PASS — `# pass 7`, `# fail 0`

통과하지 않으면 기대값을 고치지 말고 구현을 고친다. 기대값은 설계 문서에서 검산한 값이다.

- [ ] **Step 5: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add budget-math.js test/budget-math.test.js
git commit -m "용돈 속도계: 예산 페이스 계산 로직과 테스트 추가

기준 잔액·차이·바닥 예상일을 계산하는 순수 함수예요. 0으로 나누는 경우
(첫날, 지출 0원, 마지막 날)를 전부 막았어요. 설계 문서의 검산값을 테스트로 옮겼어요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 화면 만들기 (웹 버전)

미니앱으로 감싸기 전에 브라우저에서 먼저 완성한다. 이 파일이 원본 한 벌이 된다.

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\yongdon-speed.html`

**Interfaces:**
- Consumes: Task 1의 `BudgetMath.computeStatus(...)`, `BudgetMath.floorTo100(...)`
- Produces: `localStorage` 키 `yongdon-speed` 에 아래 모양으로 저장
  ```
  { budget, remaining, month, lastBudget, lastResult }
  // month 는 "2026-08" 형식. lastResult 는 지난달 마감 금액(양수면 남김, 음수면 초과)
  ```

- [ ] **Step 1: HTML 뼈대와 첫 화면을 만든다**

`yongdon-speed.html` 을 만든다. 화면은 두 개이고 `hidden` 속성으로 전환한다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<title>용돈 속도계 — AI 트렌드 다락방</title>
<style>
  :root { --bg:#0f1115; --card:#171a21; --line:#252a34; --text:#e8eaed; --muted:#9aa0aa;
          --ahead:#ff6b6b; --easy:#51cf66; --ontrack:#4dabf7; }
  * { box-sizing:border-box; }
  body { margin:0; background:var(--bg); color:var(--text);
         font-family:-apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo","Malgun Gothic",sans-serif;
         padding:env(safe-area-inset-top) 20px env(safe-area-inset-bottom); }
  .wrap { max-width:480px; margin:0 auto; padding:32px 0 40px; }
  h1 { font-size:20px; margin:0 0 24px; }
  .card { background:var(--card); border:1px solid var(--line); border-radius:16px; padding:24px; }
  .label { color:var(--muted); font-size:14px; text-align:center; }
  .big { font-size:38px; font-weight:800; text-align:center; margin:6px 0 20px; letter-spacing:-1px; }
  .row { display:flex; justify-content:space-between; align-items:baseline;
         padding:12px 0; border-top:1px solid var(--line); }
  .diff { text-align:center; font-size:18px; font-weight:700; margin:16px 0 4px; }
  .sub { text-align:center; color:var(--muted); font-size:14px; }
  input { width:100%; padding:16px; font-size:20px; text-align:center; border-radius:12px;
          border:1px solid var(--line); background:#0b0d11; color:var(--text); }
  button { width:100%; padding:16px; font-size:16px; font-weight:700; border:0;
           border-radius:12px; background:#3182f6; color:#fff; margin-top:12px; cursor:pointer; }
  button.ghost { background:transparent; border:1px solid var(--line); color:var(--text); }
  .btns { display:flex; gap:10px; margin-top:20px; }
  .btns button { margin-top:0; }
  .note { color:var(--muted); font-size:12px; text-align:center; margin-top:20px; }
  .err { color:#ff8787; font-size:13px; text-align:center; margin-top:10px; min-height:18px; }
</style>
</head>
<body>
<div class="wrap">

  <section id="setup" hidden>
    <h1>이번 달 쓸 수 있는 돈이 얼마예요?</h1>
    <div class="card">
      <input id="budgetInput" type="number" inputmode="numeric" placeholder="500000" />
      <div class="err" id="setupErr"></div>
      <button id="startBtn">시작하기</button>
    </div>
    <p class="note">이 기록은 서버로 보내지 않고 내 폰에만 저장돼요 🔒</p>
  </section>

  <section id="main" hidden>
    <div class="card">
      <div class="label">지금 남아 있어야 할 돈</div>
      <div class="big" id="baseline">-</div>
      <div class="row"><span class="label">실제 남은 돈</span><strong id="remaining">-</strong></div>
      <div class="diff" id="diffText">-</div>
      <div class="sub" id="subText"></div>
    </div>
    <div class="btns">
      <button class="ghost" id="editBtn">남은 돈 고치기</button>
      <button class="ghost" id="spendBtn">쓴 돈 기록</button>
    </div>
    <p class="note">이 기록은 서버로 보내지 않고 내 폰에만 저장돼요 🔒</p>
  </section>

</div>
<script src="budget-math.js"></script>
<script>
  var KEY = 'yongdon-speed';
  var won = function (n) { return Math.round(n).toLocaleString('ko-KR') + '원'; };
  var load = function () { try { return JSON.parse(localStorage.getItem(KEY)) || null; } catch (e) { return null; } };
  var save = function (s) { try { localStorage.setItem(KEY, JSON.stringify(s)); } catch (e) {} };
  var monthKey = function (d) { return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0'); };

  function show(id) {
    document.getElementById('setup').hidden = id !== 'setup';
    document.getElementById('main').hidden = id !== 'main';
  }

  function render(state) {
    var now = new Date();
    var r = BudgetMath.computeStatus({
      budget: state.budget, remaining: state.remaining,
      year: now.getFullYear(), month: now.getMonth() + 1, day: now.getDate(),
    });
    document.getElementById('baseline').textContent = won(r.baseline);
    document.getElementById('remaining').textContent = won(state.remaining);

    var el = document.getElementById('diffText');
    var colors = { ahead:'var(--ahead)', easy:'var(--easy)', onTrack:'var(--ontrack)', over:'var(--ahead)' };
    var texts = {
      ahead: won(r.diff) + ' 앞서가는 중 🏃',
      easy: won(-r.diff) + ' 여유 있어요 🌿',
      onTrack: '딱 맞게 가고 있어요 👌',
      over: '이번 달 예산을 ' + won(-state.remaining) + ' 넘었어요',
    };
    el.textContent = texts[r.state];
    el.style.color = colors[r.state];

    var sub = document.getElementById('subText');
    if (r.state === 'over') {
      sub.textContent = '남은 ' + r.leftDays + '일이에요. 여기서 멈추면 ' + won(-state.remaining) + ' 초과로 마감돼요';
    } else if (r.runOutDay) {
      sub.textContent = '이 페이스면 ' + r.runOutDay + '일에 바닥나요';
    } else {
      sub.textContent = '이번 달 하루 ' + won(r.perDayLeft) + '씩 쓸 수 있어요';
    }
  }

  function start() {
    var state = load();
    if (!state) { show('setup'); return; }
    show('main');
    render(state);
  }

  document.getElementById('startBtn').onclick = function () {
    var v = Number(document.getElementById('budgetInput').value);
    var err = document.getElementById('setupErr');
    if (!Number.isFinite(v) || v <= 0) { err.textContent = '0보다 큰 숫자를 넣어주세요'; return; }
    err.textContent = '';
    var s = { budget: v, remaining: v, month: monthKey(new Date()), lastBudget: v, lastResult: null };
    save(s); show('main'); render(s);
  };

  document.getElementById('editBtn').onclick = function () {
    var s = load();
    var v = Number(prompt('지금 남은 돈이 얼마예요?', s.remaining));
    if (!Number.isFinite(v)) return;
    s.remaining = v; save(s); render(s);
  };

  document.getElementById('spendBtn').onclick = function () {
    var s = load();
    var v = Number(prompt('얼마 쓰셨어요?', ''));
    if (!Number.isFinite(v) || v <= 0) return;
    s.remaining = s.remaining - v; save(s); render(s);
  };

  start();
</script>
</body>
</html>
```

- [ ] **Step 2: 브라우저에서 확인한다**

`.claude/launch.json` 에 항목을 추가한다(포트 5185는 아직 안 쓰인다).

```json
{
  "name": "yongdon-web",
  "runtimeExecutable": "npx",
  "runtimeArgs": ["-y", "serve", "C:/Users/atlia/Documents/metaluca-fresh", "-l", "5185"],
  "port": 5185
}
```

preview_start 로 띄우고 `/yongdon-speed.html` 로 이동해 확인한다.

확인 항목:
1. 첫 화면에서 `500000` 입력 → 시작하기 → 메인 화면이 뜬다
2. 콘솔 에러가 0건이다
3. "남은 돈 고치기"로 `230000` 입력 → 숫자가 바뀐다
4. "쓴 돈 기록"으로 `9000` 입력 → 남은 돈이 221,000원이 된다
5. 새로고침해도 값이 남아 있다
6. 빈 값·문자·음수를 넣어도 화면이 깨지지 않는다

- [ ] **Step 3: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add yongdon-speed.html
git commit -m "용돈 속도계: 웹 화면 추가

예산 입력 화면과 메인 화면 두 개예요. 계산은 budget-math.js에 맡기고
화면은 그리기만 해요. 기록은 localStorage에만 저장하고 서버로 보내지 않아요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 월 넘김 처리

월초에 다시 오게 만드는 장치다. 이 앱의 재방문 설계에서 가장 중요한 부분이라 따로 뗀다.

**Files:**
- Modify: `C:\Users\atlia\Documents\metaluca-fresh\yongdon-speed.html`
- Test: `C:\Users\atlia\Documents\metaluca-fresh\test\budget-math.test.js` (추가)
- Modify: `C:\Users\atlia\Documents\metaluca-fresh\budget-math.js` (함수 추가)

**Interfaces:**
- Consumes: Task 1의 `BudgetMath`
- Produces: `BudgetMath.isNewMonth(savedMonth, nowMonth)` → `boolean`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test\budget-math.test.js` 맨 아래에 붙인다.

```js
test('isNewMonth: 저장된 달과 지금 달이 다르면 참', () => {
  assert.strictEqual(BudgetMath.isNewMonth('2026-08', '2026-09'), true);
  assert.strictEqual(BudgetMath.isNewMonth('2026-08', '2026-08'), false);
  assert.strictEqual(BudgetMath.isNewMonth('2026-12', '2027-01'), true);
  assert.strictEqual(BudgetMath.isNewMonth(null, '2026-08'), true); // 저장값 없으면 새 달로 본다
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/budget-math.test.js
```
Expected: FAIL — `BudgetMath.isNewMonth is not a function`

- [ ] **Step 3: 함수를 추가한다**

`budget-math.js` 의 `computeStatus` 아래, `return` 문 위에 넣는다.

```js
  function isNewMonth(savedMonth, nowMonth) {
    if (!savedMonth) return true;
    return savedMonth !== nowMonth;
  }
```

그리고 반환 객체에 추가한다.

```js
  return {
    daysInMonth: daysInMonth,
    floorTo100: floorTo100,
    computeStatus: computeStatus,
    isNewMonth: isNewMonth,
  };
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/budget-math.test.js
```
Expected: PASS — `# pass 8`, `# fail 0`

- [ ] **Step 5: 화면에 새 달 안내를 붙인다**

`yongdon-speed.html` 의 `<section id="main">` 바로 뒤에 화면을 하나 추가한다.

```html
  <section id="newMonth" hidden>
    <h1 id="newMonthTitle">새 달이 시작됐어요</h1>
    <div class="card">
      <div class="sub" id="lastResultText"></div>
      <div class="big" id="proposeBudget">-</div>
      <div class="label">이번 달도 이 금액으로 갈까요?</div>
      <button id="keepBtn">네, 그대로 갈게요</button>
      <button class="ghost" id="changeBtn">바꿀래요</button>
    </div>
  </section>
```

`show` 함수를 세 화면을 다루도록 고친다.

```js
  function show(id) {
    ['setup', 'main', 'newMonth'].forEach(function (s) {
      document.getElementById(s).hidden = s !== id;
    });
  }
```

`start` 함수를 고쳐 새 달을 감지한다.

```js
  function start() {
    var state = load();
    if (!state) { show('setup'); return; }

    var nowMonth = monthKey(new Date());
    if (BudgetMath.isNewMonth(state.month, nowMonth)) {
      var m = Number(nowMonth.split('-')[1]);
      document.getElementById('newMonthTitle').textContent = m + '월이 시작됐어요';
      var last = state.remaining;
      document.getElementById('lastResultText').textContent =
        last >= 0 ? '지난달은 ' + won(last) + ' 남기고 끝냈어요 👏'
                  : '지난달은 ' + won(-last) + ' 넘겼어요';
      document.getElementById('proposeBudget').textContent = won(state.budget);
      state.lastBudget = state.budget;
      state.lastResult = last;
      save(state);
      show('newMonth');
      return;
    }
    show('main');
    render(state);
  }
```

버튼 두 개를 연결한다.

```js
  document.getElementById('keepBtn').onclick = function () {
    var s = load();
    s.budget = s.lastBudget; s.remaining = s.lastBudget;
    s.month = monthKey(new Date());
    save(s); show('main'); render(s);
  };

  document.getElementById('changeBtn').onclick = function () {
    var s = load();
    s.month = monthKey(new Date());
    save(s);
    document.getElementById('budgetInput').value = '';
    show('setup');
  };
```

`startBtn` 도 새 달에서 다시 들어올 수 있으므로 `month` 를 항상 갱신하게 고친다.

```js
  document.getElementById('startBtn').onclick = function () {
    var v = Number(document.getElementById('budgetInput').value);
    var err = document.getElementById('setupErr');
    if (!Number.isFinite(v) || v <= 0) { err.textContent = '0보다 큰 숫자를 넣어주세요'; return; }
    err.textContent = '';
    var prev = load();
    var s = {
      budget: v, remaining: v, month: monthKey(new Date()),
      lastBudget: v, lastResult: prev ? prev.lastResult : null,
    };
    save(s); show('main'); render(s);
  };
```

- [ ] **Step 6: 브라우저에서 월 넘김을 확인한다**

개발자 콘솔에서 저장값의 달을 지난달로 바꾼 뒤 새로고침한다.

```js
var s = JSON.parse(localStorage.getItem('yongdon-speed'));
s.month = '2026-07';
localStorage.setItem('yongdon-speed', JSON.stringify(s));
location.reload();
```

확인 항목:
1. "8월이 시작됐어요" 화면이 뜬다
2. 지난달 결과가 한 줄 보인다
3. "네, 그대로 갈게요" → 예산이 초기화되고 메인 화면으로 간다
4. 다시 새로고침하면 새 달 화면이 다시 뜨지 않는다

- [ ] **Step 7: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add budget-math.js test/budget-math.test.js yongdon-speed.html
git commit -m "용돈 속도계: 월 넘김 처리와 지난달 결과 안내 추가

1일이 되면 새 달 화면을 띄우고 지난달을 얼마 남기고 끝냈는지 한 줄 보여줘요.
월초에 다시 열어보게 만드는 장치예요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 미니앱 패키지로 감싸기

기존 13개와 같은 구조로 만든다. 참고할 것은 `miniapp-age` 다(외부 호출이 없어 가장 비슷하다).

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-yongdon\package.json`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-yongdon\apps-in-toss.config.ts`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-yongdon\sync.mjs`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-yongdon\src\ait-bridge.js`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-yongdon\.gitignore`

**Interfaces:**
- Consumes: Task 2·3에서 만든 `yongdon-speed.html`, `budget-math.js`
- Produces: `miniapp-yongdon/index.html`(생성물, git 무시), `yongdon-speed.ait`

- [ ] **Step 1: 패키지 파일 5개를 만든다**

`package.json`:

```json
{
  "name": "yongdon-speed-miniapp",
  "private": true,
  "version": "1.0.0",
  "description": "용돈 속도계 — 앱인토스 미니앱 패키지",
  "type": "module",
  "scripts": {
    "web:dev": "node sync.mjs && vite --port 5185",
    "web:build": "node sync.mjs && vite build",
    "dev": "npm run web:dev",
    "build": "npm run web:build && ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.1.1"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "@apps-in-toss/devtools": "^3.1.1"
  }
}
```

`apps-in-toss.config.ts`:

```ts
import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  // 앱인토스 콘솔에 등록한 appName과 반드시 일치해야 해요.
  appName: 'yongdon-speed',

  webView: {},
  webBundleDir: 'dist',

  brand: {
    primaryColor: '#3182f6'
  },

  permissions: []
});
```

`sync.mjs` — `budget-math.js` 를 `public/` 으로 복사한다(타로와 같은 방식).

```js
// 루트 yongdon-speed.html을 미니앱용 index.html로 변환하는 스크립트.
// 원본 한 벌만 유지하고, 미니앱 전용 차이는 여기서 주입해요.
// budget-math.js는 클래식 스크립트라 번들링하지 않고 public/으로 그대로 복사해요.
import { readFileSync, writeFileSync, mkdirSync, copyFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const here = dirname(fileURLToPath(import.meta.url));
const src = readFileSync(join(here, '..', 'yongdon-speed.html'), 'utf8');

mkdirSync(join(here, 'public'), { recursive: true });
copyFileSync(join(here, '..', 'budget-math.js'), join(here, 'public', 'budget-math.js'));

let out = src
  // public/ 루트 기준으로 경로 조정
  .replace('<script src="budget-math.js"></script>', '<script src="/budget-math.js"></script>')
  // SDK 브릿지 주입 (vite가 모듈로 번들링)
  .replace('</head>', '  <script type="module" src="/src/ait-bridge.js"></script>\n</head>');

if (out === src) throw new Error('sync.mjs: 변환이 하나도 적용되지 않았어요. 원본 마크업이 바뀌었는지 확인하세요.');
if (/href="https?:\/\//.test(out)) throw new Error('sync.mjs: 미니앱 빌드에 외부 링크가 남아 있어요. 검수 반려 위험 — 링크를 제거하세요.');

writeFileSync(join(here, 'index.html'), out);
console.log('miniapp-yongdon/index.html generated from ../yongdon-speed.html');
```

`src/ait-bridge.js`:

```js
// 앱인토스 SDK 브릿지 — 공유 기능을 미니앱 SDK에 연결해요.
import { share, getTossShareLink } from '@apps-in-toss/web-framework';

window.AppsInToss = {
  share,
  // 공유 링크: 토스 앱에서 이 미니앱을 바로 여는 딥링크
  getTossShareLink: () => getTossShareLink('intoss://yongdon-speed'),
};
```

`.gitignore`:

```
node_modules/
dist/
index.html
public/
*.ait
.granite/
```

- [ ] **Step 2: 의존성을 설치하고 빌드한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh/miniapp-yongdon && npm install && npm run build
```
Expected: `앱인토스 빌드가 완료되었습니다. (yongdon-speed.ait)` 와 `deploymentId: ...` 가 출력된다.

- [ ] **Step 3: 번들 내용을 확인한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh/miniapp-yongdon
grep -c "BudgetMath" dist/budget-math.js
grep -oE '(src|href)="[^"]*"' dist/index.html | grep -vE 'data:|#'
```
Expected:
- 첫 명령이 1 이상을 출력한다(계산 파일이 번들에 들어갔다)
- 두 번째 명령의 결과에 `http`로 시작하는 주소가 하나도 없다

- [ ] **Step 4: 미니앱 개발 서버로 확인한다**

`.claude/launch.json` 에 추가한다.

```json
{
  "name": "miniapp-yongdon",
  "runtimeExecutable": "npm",
  "runtimeArgs": ["--prefix", "C:/Users/atlia/Documents/metaluca-fresh/miniapp-yongdon", "run", "web:dev"],
  "port": 5185
}
```

preview_start 로 띄우고 확인한다.

확인 항목:
1. 콘솔 에러가 0건이다
2. `window.BudgetMath` 가 객체다
3. `window.AppsInToss.share` 가 함수다
4. 예산 입력 → 메인 화면 → 남은 돈 고치기가 모두 동작한다

- [ ] **Step 5: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add miniapp-yongdon/package.json miniapp-yongdon/apps-in-toss.config.ts \
        miniapp-yongdon/sync.mjs miniapp-yongdon/src/ait-bridge.js miniapp-yongdon/.gitignore
git commit -m "용돈 속도계: 앱인토스 미니앱 패키지 추가

기존 미니앱과 같은 구조예요. SDK 3.1.1을 쓰고, sync.mjs가 루트 원본에서
index.html을 만들면서 계산 파일을 public/으로 복사해요. 외부 호출이 없어
CORS 설정은 필요 없어요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 콘솔 등록과 출시

여기부터는 루카가 직접 해야 한다. API 키와 콘솔 계정이 필요하다.

**Files:** 없음 (콘솔 작업)

**Interfaces:**
- Consumes: Task 4가 만든 `yongdon-speed.ait`

- [ ] **Step 1: 콘솔에서 앱을 만든다**

앱인토스 개발자센터 → 앱 추가.

- appName: `yongdon-speed` (**한 번 정하면 수정 불가**. `apps-in-toss.config.ts` 와 반드시 같아야 한다)
- 한글 이름: 용돈 속도계
- **카테고리: 재테크** — 이 앱의 목적이 새 탭 선점이다
- 상세설명은 "무엇을 보고 누르고 경험하는지" 구체적으로 쓴다(AI가 심사한다)

- [ ] **Step 2: 배포한다**

```powershell
npm --prefix C:/Users/atlia/Documents/metaluca-fresh/miniapp-yongdon run deploy
```

배포 API 키가 필요하다. 콘솔의 API 키 메뉴에서 발급한다.

- [ ] **Step 3: QR로 실기기에서 확인한다**

콘솔 [앱 출시] → 해당 버전의 [테스트] → QR 스캔.

확인 항목:
1. 앱이 바로 뜬다(흰 화면 지연이 없다)
2. 예산을 넣으면 메인 화면이 나온다
3. 앱을 껐다 켜도 값이 남아 있다
4. 남은 돈 고치기·쓴 돈 기록이 동작한다

- [ ] **Step 4: 검토를 요청한다**

출시 노트:

```
1. 신규 앱입니다. 이번 달 예산을 입력하면 지금 지출 속도가 적정한지
   보여주는 앱입니다.
2. 예산 대비 "지금쯤 남아 있어야 할 돈"과 "실제 남은 돈"을 비교해
   앞서가는지 여유 있는지 알려주고, 이 속도면 며칠에 바닥나는지 계산합니다.
3. 모든 기록은 사용자 기기에만 저장되며 서버로 전송하지 않습니다.
```

---

## 나중에 (이번 범위 아님)

- 인앱 광고 배너 — 광고 ID를 발급받은 뒤 별도 작업으로 붙인다. 자리는 메인 화면 맨 아래 하나다
- `prompt()` 대신 앱 안 입력창 — 지금은 브라우저 기본 창을 쓴다. 실기기에서 어색하면 그때 바꾼다
- 구독료 다이어트 앱 — 설계 문서는 이미 있다(`2026-08-27-구독료다이어트-design.md`)

# 구독료 다이어트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 쓰고 있는 구독을 체크하면 연간 총액을 보여주고, 안 쓰는 것을 골라내 절약액을 계산해주는 앱인토스 미니앱을 만든다.

**Architecture:** 서비스 목록(자주 바뀌는 데이터)과 계산(안 바뀌는 로직)을 각각 클래식 스크립트 한 벌로 분리하고, HTML은 그 결과를 그리기만 한다. 화면은 고르기 → 충격 → 다이어트 세 단계다. 미니앱 패키지는 기존 15개와 같은 패턴(루트에 원본 HTML 한 벌 + `sync.mjs`가 미니앱용 `index.html` 생성)을 따른다.

**Tech Stack:** 바닐라 JS(클래식 스크립트), `@apps-in-toss/web-framework` 3.1.1, vite 6, Node 내장 테스트 러너 — 새 의존성 없음.

**설계 문서:** `huhsame-script/docs/superpowers/specs/2026-08-27-구독료다이어트-design.md`

## Global Constraints

- 작업 저장소는 `C:\Users\atlia\Documents\metaluca-fresh` 이다. `Desktop\metaluca8560`·`Desktop\_meta_tmp`는 낡은 클론이므로 쓰지 않는다.
- **AI를 호출하지 않는다.** 외부 API 호출이 전혀 없어야 한다.
- **저장은 `localStorage`만 쓴다.** 서버로 아무것도 보내지 않는다.
- **네이티브 대화상자를 쓰지 않는다.** `prompt` / `alert` / `confirm` 금지. 웹뷰가 JS 대화상자 콜백을 구현하지 않으면 조용히 `null`을 반환해 앱이 먹통이 된다(용돈 속도계에서 실제로 겪어 전량 교체했다). 입력은 화면 안에서 받는다.
- 클래식 스크립트로 쓴다(`export` 문 금지). 브라우저에서는 전역으로, Node 테스트에서는 `require`로 쓴다. 저장소 루트에 `package.json`이 없어 `.js`는 CommonJS로 해석된다.
- 미니앱 밖으로 나가는 링크(`href="http..."`)와 절대 `http(s)` `src`를 넣지 않는다. 앱인토스 검수 반려 사유다.
- 화면 문구는 해요체를 쓴다.
- 미리 넣는 가격은 **값이 확실한 것만** 넣고 나머지는 비워 둔다. 틀린 숫자는 이 앱의 유일한 무기인 총액의 신뢰를 무너뜨린다. 가격 옆에 기준 시점을 밝히고, 사용자가 그 자리에서 고칠 수 있게 한다.
- 테스트 실행 명령은 정확히 `node --test test/subscription.test.js` 이다. `node --test test/`는 이 환경에서 폴더를 모듈로 오인해 실패한다.
- appName은 **`gudok-check`** 다. 계획을 쓸 때는 `subscription-diet`으로 잡았으나 앱인토스에 이미 등록된 이름이라 쓸 수 없었다. 파일명·폴더명(`subscription-*`, `miniapp-subdiet`)은 옛 이름 그대로이고 appName만 다르다. 2026-08-29 출시 완료.

---

### Task 1: 서비스 목록과 계산 로직

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\subscription-data.js`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\subscription-math.js`
- Test: `C:\Users\atlia\Documents\metaluca-fresh\test\subscription.test.js`

**Interfaces:**
- Consumes: 없음
- Produces:
  - 전역 `SUBSCRIPTION_SERVICES` — 배열. 각 항목은 `{ id, name, category, price, hint }`.
    `price`는 숫자이거나 `null`(값이 확실하지 않아 비워 둔 것)이다.
    `hint`는 문자열이거나 `null`. 요금제가 여러 개이거나 다른 구독에 포함되는 경우를 알린다.
  - 전역 `SubMath` 에 아래 함수들
    - `monthlyTotal(ids, prices)` → `number`. `prices`는 `{ id: 금액 }` 객체
    - `summarize(monthly)` → `{ monthly, yearly, daily, coffees }`
    - `savings(ids, prices)` → `{ monthly, yearly }`
    - `topSpenders(ids, prices, n)` → `[{ id, price }]` 금액 내림차순 최대 n개
    - `missingPriceIds(ids, prices)` → `[id]` 금액이 아직 없는 항목들

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`test\subscription.test.js` 를 만든다.

```js
const test = require('node:test');
const assert = require('node:assert');
const SERVICES = require('../subscription-data.js');
const SubMath = require('../subscription-math.js');

test('목록: 항목마다 id·이름·분류가 있고 id가 겹치지 않는다', () => {
  assert.ok(SERVICES.length >= 25, '서비스가 25개 이상이어야 해요');
  const ids = new Set();
  for (const s of SERVICES) {
    assert.ok(s.id && typeof s.id === 'string', 'id가 있어야 해요');
    assert.ok(s.name && typeof s.name === 'string', '이름이 있어야 해요');
    assert.ok(s.category && typeof s.category === 'string', '분류가 있어야 해요');
    assert.ok(s.price === null || typeof s.price === 'number', 'price는 숫자이거나 null이어야 해요');
    assert.ok(s.hint === null || typeof s.hint === 'string', 'hint는 문자열이거나 null이어야 해요');
    assert.ok(!ids.has(s.id), 'id가 겹치면 안 돼요: ' + s.id);
    ids.add(s.id);
  }
});

test('목록: 다른 구독에 포함되는 항목은 안내를 달아 둔다', () => {
  // 쿠팡플레이는 쿠팡 와우에 포함이라 둘 다 세면 총액이 부풀어 오른다.
  const play = SERVICES.filter(s => s.id === 'coupangplay')[0];
  assert.ok(play, '쿠팡플레이가 목록에 있어야 해요');
  assert.strictEqual(play.price, 0, '와우에 포함이므로 기본 금액은 0이어야 해요');
  assert.ok(play.hint && play.hint.indexOf('쿠팡 와우') >= 0, '포함 사실을 안내해야 해요');
});

test('목록: 값이 확실하지 않은 항목은 null로 비워 둔다', () => {
  const blank = SERVICES.filter(s => s.price === null);
  assert.ok(blank.length > 0, '비워 둔 항목이 있어야 해요');
});

test('monthlyTotal: 고른 것들의 금액을 더한다', () => {
  const prices = { a: 10000, b: 5500, c: 7890 };
  assert.strictEqual(SubMath.monthlyTotal(['a', 'b'], prices), 15500);
  assert.strictEqual(SubMath.monthlyTotal([], prices), 0);
});

test('monthlyTotal: 금액이 없는 항목은 0으로 친다', () => {
  const prices = { a: 10000 };
  assert.strictEqual(SubMath.monthlyTotal(['a', 'b'], prices), 10000);
});

test('summarize: 설계 문서의 예시와 값이 같다', () => {
  const r = SubMath.summarize(155800);
  assert.strictEqual(r.monthly, 155800);
  assert.strictEqual(r.yearly, 1869600);   // 155800 * 12
  assert.strictEqual(r.daily, 5122);       // 1869600 / 365 반올림
  assert.strictEqual(r.coffees, 374);      // 1869600 / 5000 반올림
});

test('summarize: 0원이면 전부 0이고 나눗셈이 깨지지 않는다', () => {
  const r = SubMath.summarize(0);
  assert.deepStrictEqual(r, { monthly: 0, yearly: 0, daily: 0, coffees: 0 });
});

test('savings: 설계 문서의 예시와 값이 같다', () => {
  const prices = { watcha: 7900, adobe: 24000, dropbox: 16200 };
  const r = SubMath.savings(['watcha', 'adobe', 'dropbox'], prices);
  assert.strictEqual(r.monthly, 48100);
  assert.strictEqual(r.yearly, 577200);
});

test('savings: 하나도 안 골랐으면 0이다', () => {
  assert.deepStrictEqual(SubMath.savings([], { a: 1000 }), { monthly: 0, yearly: 0 });
});

test('topSpenders: 비싼 순으로 n개만 준다', () => {
  const prices = { a: 5000, b: 29000, c: 24000, d: 7890 };
  const top = SubMath.topSpenders(['a', 'b', 'c', 'd'], prices, 3);
  assert.deepStrictEqual(top, [
    { id: 'b', price: 29000 },
    { id: 'c', price: 24000 },
    { id: 'd', price: 7890 },
  ]);
});

test('topSpenders: 고른 게 n보다 적으면 있는 만큼만 준다', () => {
  const top = SubMath.topSpenders(['a'], { a: 1000 }, 3);
  assert.strictEqual(top.length, 1);
});

test('topSpenders: 금액이 없는 항목은 빼고 준다', () => {
  const top = SubMath.topSpenders(['a', 'b'], { a: 1000 }, 3);
  assert.deepStrictEqual(top, [{ id: 'a', price: 1000 }]);
});

test('missingPriceIds: 금액이 아직 없는 것만 알려준다', () => {
  const prices = { a: 1000, b: 0 };
  assert.deepStrictEqual(SubMath.missingPriceIds(['a', 'b', 'c'], prices), ['c']);
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/subscription.test.js
```
Expected: FAIL — `Cannot find module '../subscription-data.js'`

- [ ] **Step 3: 서비스 목록을 만든다**

`subscription-data.js` 를 만든다. **`price`는 값이 확실한 것만 채우고 나머지는 `null`로 둔다.**
요금제가 여러 개인 서비스는 가장 흔한 것 하나만 쓴다.

```js
// 구독료 다이어트 — 서비스 목록.
// 클래식 스크립트로 쓴다(export 금지). 브라우저에서는 전역 SUBSCRIPTION_SERVICES로,
// Node 테스트에서는 require로 쓴다.
//
// price 규칙: 값이 확실한 것만 숫자로 넣고, 아니면 null로 비워 둔다.
//   틀린 금액은 이 앱의 유일한 무기인 "연간 총액"의 신뢰를 무너뜨린다.
//   null이면 사용자가 체크할 때 앱이 금액을 물어본다.
// hint: 요금제가 여러 개거나 다른 구독에 포함되는 경우를 알려준다. 없으면 null.
//   예: 쿠팡플레이는 쿠팡 와우에 포함이라 둘 다 세면 총액이 부풀어 오른다.
// 기준 시점: 2026년 8월. 가격이 바뀌면 이 파일만 고치면 된다.
var SUBSCRIPTION_SERVICES = [
  // hint: 금액 패널에 함께 보여줄 한 줄 안내. 없으면 null.
  { id: 'netflix',    name: '넷플릭스',        category: '영상',      price: 13500, hint: '스탠다드 기준이에요. 광고형 7,000원 · 프리미엄 17,000원' },
  { id: 'disney',     name: '디즈니+',         category: '영상',      price: 9900,  hint: '스탠다드 기준이에요. 프리미엄 13,900원' },
  { id: 'tving',      name: '티빙',            category: '영상',      price: 13500, hint: '스탠다드 기준이에요. 베이직 9,500원 · 프리미엄 17,000원' },
  { id: 'wavve',      name: '웨이브',          category: '영상',      price: 10900, hint: '스탠다드 기준이에요. 베이직 7,900원 · 프리미엄 13,900원' },
  { id: 'coupangplay',name: '쿠팡플레이',      category: '영상',      price: 0,     hint: '쿠팡 와우를 쓰시면 이미 포함돼 있어요. 광고 없는 프리미엄 패스는 3,900원 추가예요' },
  { id: 'appletv',    name: '애플TV+',         category: '영상',      price: 6500,  hint: null },
  { id: 'watcha',     name: '왓챠',            category: '영상',      price: 7900,  hint: '베이직 기준이에요. 상위 요금제 12,900원' },
  { id: 'laftel',     name: '라프텔',          category: '영상',      price: 9900,  hint: '기본 기준이에요. 상위 요금제 14,900원' },
  { id: 'ytpremium',  name: '유튜브 프리미엄',  category: '영상',      price: 14900, hint: 'Premium 기준이에요. Lite는 8,500원' },
  { id: 'appleone',   name: '애플 원',         category: '영상',      price: 14900, hint: '개인 기준이에요. 가족 20,900원. 애플TV+·애플뮤직·아이클라우드가 포함돼요' },

  { id: 'ytmusic',    name: '유튜브 뮤직',      category: '음악',      price: null,  hint: '유튜브 프리미엄을 쓰시면 이미 포함돼 있어요' },
  { id: 'spotify',    name: '스포티파이',       category: '음악',      price: null,  hint: null },
  { id: 'applemusic', name: '애플 뮤직',       category: '음악',      price: null,  hint: '애플 원을 쓰시면 이미 포함돼 있어요' },
  { id: 'melon',      name: '멜론',            category: '음악',      price: null,  hint: null },
  { id: 'flo',        name: '플로',            category: '음악',      price: null,  hint: null },
  { id: 'genie',      name: '지니뮤직',        category: '음악',      price: null,  hint: null },
  { id: 'bugs',       name: '벅스',            category: '음악',      price: null,  hint: null },

  { id: 'millie',     name: '밀리의 서재',      category: '전자책',    price: null,  hint: null },
  { id: 'ridi',       name: '리디셀렉트',       category: '전자책',    price: null,  hint: null },
  { id: 'crema',      name: '크레마클럽',       category: '전자책',    price: null,  hint: null },

  { id: 'naverplus',  name: '네이버플러스',     category: '쇼핑·배송', price: null,  hint: '네이버 MYBOX 용량이 함께 나와요' },
  { id: 'coupang',    name: '쿠팡 와우',        category: '쇼핑·배송', price: 7890,  hint: '쿠팡플레이가 포함돼 있어요' },
  { id: 'baemin',     name: '배민클럽',        category: '쇼핑·배송', price: null,  hint: null },
  { id: 'kurly',      name: '컬리멤버스',       category: '쇼핑·배송', price: null,  hint: null },

  { id: 'icloud',     name: '아이클라우드',     category: '클라우드',  price: null,  hint: '애플 원을 쓰시면 이미 포함돼 있어요' },
  { id: 'googleone',  name: '구글 원',         category: '클라우드',  price: null,  hint: null },
  { id: 'naverbox',   name: '네이버 MYBOX',    category: '클라우드',  price: null,  hint: '네이버플러스를 쓰시면 기본 용량이 포함돼 있어요' },
  { id: 'ms365',      name: 'MS365',           category: '클라우드',  price: null,  hint: '오피스와 원드라이브가 함께 나와요' },

  { id: 'chatgpt',    name: '챗GPT',           category: 'AI·업무',   price: null,  hint: null },
  { id: 'claude',     name: '클로드',          category: 'AI·업무',   price: null,  hint: null },
  { id: 'gemini',     name: '제미나이',        category: 'AI·업무',   price: null,  hint: null },
  { id: 'perplexity', name: '퍼플렉시티',      category: 'AI·업무',   price: null,  hint: null },
  { id: 'canva',      name: '캔바',            category: 'AI·업무',   price: null,  hint: null },
  { id: 'notion',     name: '노션',            category: 'AI·업무',   price: null,  hint: null },
  { id: 'adobe',      name: '어도비',          category: 'AI·업무',   price: null,  hint: null },

  { id: 'psplus',     name: '플스 플러스',      category: '게임',      price: null,  hint: null },
  { id: 'xboxpass',   name: '엑스박스 게임패스', category: '게임',      price: null,  hint: null },
  { id: 'nintendo',   name: '닌텐도 온라인',    category: '게임',      price: null,  hint: null },

  { id: 'strava',     name: '스트라바',        category: '운동·건강', price: null,  hint: null },
  { id: 'calm',       name: '캄',              category: '운동·건강', price: null,  hint: null },
];

if (typeof module !== 'undefined' && module.exports) {
  module.exports = SUBSCRIPTION_SERVICES;
}
```

- [ ] **Step 4: 계산 로직을 만든다**

`subscription-math.js` 를 만든다.

```js
// 구독료 다이어트 — 계산 로직.
// 클래식 스크립트로 쓴다(export 금지). DOM과 localStorage를 건드리지 않는다.
var SubMath = (function () {
  var COFFEE_PRICE = 5000; // 커피 환산 기준
  var DAYS_PER_YEAR = 365;

  function priceOf(prices, id) {
    var v = prices[id];
    return typeof v === 'number' && isFinite(v) ? v : 0;
  }

  function monthlyTotal(ids, prices) {
    var sum = 0;
    for (var i = 0; i < ids.length; i++) sum += priceOf(prices, ids[i]);
    return sum;
  }

  function summarize(monthly) {
    var yearly = monthly * 12;
    return {
      monthly: monthly,
      yearly: yearly,
      daily: Math.round(yearly / DAYS_PER_YEAR),
      coffees: Math.round(yearly / COFFEE_PRICE),
    };
  }

  function savings(ids, prices) {
    var m = monthlyTotal(ids, prices);
    return { monthly: m, yearly: m * 12 };
  }

  function topSpenders(ids, prices, n) {
    var rows = [];
    for (var i = 0; i < ids.length; i++) {
      var p = prices[ids[i]];
      // 금액을 아직 안 넣은 항목은 순위에 넣지 않는다.
      if (typeof p === 'number' && isFinite(p)) rows.push({ id: ids[i], price: p });
    }
    rows.sort(function (a, b) { return b.price - a.price; });
    return rows.slice(0, n);
  }

  function missingPriceIds(ids, prices) {
    var out = [];
    for (var i = 0; i < ids.length; i++) {
      var p = prices[ids[i]];
      if (!(typeof p === 'number' && isFinite(p))) out.push(ids[i]);
    }
    return out;
  }

  return {
    monthlyTotal: monthlyTotal,
    summarize: summarize,
    savings: savings,
    topSpenders: topSpenders,
    missingPriceIds: missingPriceIds,
  };
})();

if (typeof module !== 'undefined' && module.exports) {
  module.exports = SubMath;
}
```

- [ ] **Step 5: 테스트를 돌려 통과를 확인한다**

Run:
```bash
cd /c/Users/atlia/Documents/metaluca-fresh && node --test test/subscription.test.js
```
Expected: PASS — `# pass 13`, `# fail 0`

통과하지 않으면 기대값을 고치지 말고 구현을 고친다. 기대값은 설계 문서에서 검산한 값이다.

- [ ] **Step 6: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add subscription-data.js subscription-math.js test/subscription.test.js
git commit -m "구독료 다이어트: 서비스 목록과 계산 로직 추가

체크한 구독의 월 합계·연 합계·하루치·커피 환산과 절약액을 계산해요.
가격은 값이 확실한 것만 넣고 나머지는 null로 비워 뒀어요. 틀린 금액을
보여주느니 사용자에게 물어보는 편이 나아서예요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 고르는 화면

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\subscription-diet.html`

**Interfaces:**
- Consumes: 전역 `SUBSCRIPTION_SERVICES`, `SubMath.missingPriceIds(ids, prices)`
- Produces: `localStorage` 키 `subscription-diet` 에 `{ selected, prices, custom, unused }`
  - `selected` — 고른 서비스 id 배열
  - `prices` — `{ id: 금액 }`. 목록의 기본값 위에 사용자가 고친 값을 덮어쓴 결과
  - `custom` — 직접 추가한 항목 `[{ id, name, price }]`. id는 `custom-1` 처럼 짓는다
  - `unused` — "안 쓴다"고 고른 id 배열 (Task 3에서 채운다)

- [ ] **Step 1: HTML과 고르는 화면을 만든다**

`subscription-diet.html` 을 만든다. 화면 셋을 `hidden` 속성으로 전환한다.
**네이티브 대화상자를 쓰지 않는다** — 금액 입력은 카드 안에 인라인으로 연다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover" />
<title>구독료 다이어트 — AI 트렌드 다락방</title>
<style>
  :root { --bg:#0f1115; --card:#171a21; --line:#252a34; --text:#e8eaed; --muted:#9aa0aa;
          --accent:#3182f6; --warn:#ff6b6b; --good:#51cf66; }
  * { box-sizing:border-box; }
  body { margin:0; background:var(--bg); color:var(--text);
         font-family:-apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo","Malgun Gothic",sans-serif;
         padding:env(safe-area-inset-top) 20px env(safe-area-inset-bottom); }
  .wrap { max-width:480px; margin:0 auto; padding:28px 0 40px; }
  h1 { font-size:20px; margin:0 0 6px; }
  .lead { color:var(--muted); font-size:14px; margin:0 0 20px; }
  .cat { color:var(--muted); font-size:13px; margin:18px 0 8px; }
  .chips { display:flex; flex-wrap:wrap; gap:8px; }
  .chip { padding:10px 14px; border-radius:999px; border:1px solid var(--line);
          background:var(--card); color:var(--text); font-size:14px; cursor:pointer; }
  .chip.on { border-color:var(--accent); background:rgba(49,130,246,0.15); }
  .chip .won { color:var(--muted); font-size:12px; margin-left:6px; }
  .chip.on .won { color:var(--text); }
  .bar { position:sticky; bottom:0; background:var(--bg); padding:14px 0 0; margin-top:24px; }
  button { width:100%; padding:16px; font-size:16px; font-weight:700; border:0;
           border-radius:12px; background:var(--accent); color:#fff; cursor:pointer; }
  button:disabled { background:#2a2f3a; color:var(--muted); cursor:default; }
  button.ghost { background:transparent; border:1px solid var(--line); color:var(--text); }
  .panel { background:var(--card); border:1px solid var(--line); border-radius:16px;
           padding:20px; margin-top:14px; }
  .panel h2 { font-size:16px; margin:0 0 4px; }
  .panel p { color:var(--muted); font-size:13px; margin:0 0 12px; }
  input { width:100%; padding:14px; font-size:18px; text-align:center; border-radius:12px;
          border:1px solid var(--line); background:#0b0d11; color:var(--text); }
  .err { color:#ff8787; font-size:13px; text-align:center; margin-top:8px; min-height:18px; }
  .row2 { display:flex; gap:10px; margin-top:12px; }
  .note { color:var(--muted); font-size:12px; text-align:center; margin-top:18px; }
</style>
</head>
<body>
<div class="wrap">

  <section id="pick">
    <h1>쓰고 있는 구독을 골라주세요</h1>
    <p class="lead">가격은 2026년 8월 기준이에요. 눌러서 고칠 수 있어요.</p>
    <div id="chipArea"></div>

    <div class="panel" id="pricePanel" hidden>
      <h2 id="priceTitle">금액</h2>
      <p>한 달에 얼마 내세요?</p>
      <p id="priceHint" style="color:var(--muted);font-size:12px"></p>
      <input id="priceInput" type="number" inputmode="numeric" placeholder="9900" />
      <div class="err" id="priceErr"></div>
      <div class="row2">
        <button class="ghost" id="priceCancel">취소</button>
        <button id="priceOk">확인</button>
      </div>
    </div>

    <div class="panel" id="customPanel" hidden>
      <h2>직접 추가</h2>
      <p>목록에 없는 구독이 있나요?</p>
      <input id="customName" type="text" placeholder="서비스 이름" />
      <div style="height:10px"></div>
      <input id="customPrice" type="number" inputmode="numeric" placeholder="한 달 금액" />
      <div class="err" id="customErr"></div>
      <div class="row2">
        <button class="ghost" id="customCancel">취소</button>
        <button id="customOk">추가</button>
      </div>
    </div>

    <div class="bar">
      <button class="ghost" id="addCustomBtn">+ 직접 추가</button>
      <div style="height:10px"></div>
      <button id="toResult" disabled>얼마 쓰는지 보기</button>
    </div>
    <p class="note">이 기록은 서버로 보내지 않고 내 폰에만 저장돼요 🔒</p>
  </section>

</div>
<script src="subscription-data.js"></script>
<script src="subscription-math.js"></script>
<script>
  var KEY = 'subscription-diet';
  var won = function (n) { return Math.round(n).toLocaleString('ko-KR') + '원'; };

  function load() {
    try { return JSON.parse(localStorage.getItem(KEY)) || null; } catch (e) { return null; }
  }
  function save(s) { try { localStorage.setItem(KEY, JSON.stringify(s)); } catch (e) {} }

  // 기본 가격 위에 사용자가 고친 값을 덮어쓴 것이 state.prices 다.
  function defaultPrices() {
    var p = {};
    SUBSCRIPTION_SERVICES.forEach(function (s) { if (s.price !== null) p[s.id] = s.price; });
    return p;
  }

  var state = load() || { selected: [], prices: defaultPrices(), custom: [], unused: [] };
  var pendingId = null; // 금액 입력 패널이 열린 대상

  function allServices() { return SUBSCRIPTION_SERVICES.concat(state.custom); }

  function renderChips() {
    var area = document.getElementById('chipArea');
    area.textContent = '';
    var cats = [];
    allServices().forEach(function (s) { if (cats.indexOf(s.category) < 0) cats.push(s.category); });

    cats.forEach(function (cat) {
      var label = document.createElement('div');
      label.className = 'cat'; label.textContent = cat;
      area.appendChild(label);
      var box = document.createElement('div');
      box.className = 'chips';
      allServices().filter(function (s) { return s.category === cat; }).forEach(function (s) {
        var b = document.createElement('button');
        b.className = 'chip' + (state.selected.indexOf(s.id) >= 0 ? ' on' : '');
        b.type = 'button';
        b.appendChild(document.createTextNode(s.name));
        var price = state.prices[s.id];
        if (typeof price === 'number') {
          var w = document.createElement('span');
          w.className = 'won'; w.textContent = won(price);
          b.appendChild(w);
        }
        b.onclick = function () { toggle(s.id); };
        box.appendChild(b);
      });
      area.appendChild(box);
    });

    document.getElementById('toResult').disabled = state.selected.length === 0;
  }

  function toggle(id) {
    var i = state.selected.indexOf(id);
    if (i >= 0) {
      state.selected.splice(i, 1);
      save(state); renderChips();
      return;
    }
    state.selected.push(id);
    save(state);
    // 금액을 모르는 항목이면 바로 물어본다.
    if (SubMath.missingPriceIds([id], state.prices).length > 0) openPrice(id);
    else renderChips();
  }

  function nameOf(id) {
    var found = allServices().filter(function (s) { return s.id === id; })[0];
    return found ? found.name : id;
  }

  function hintOf(id) {
    var found = allServices().filter(function (s) { return s.id === id; })[0];
    return found && found.hint ? found.hint : '';
  }

  function openPrice(id) {
    pendingId = id;
    document.getElementById('priceTitle').textContent = nameOf(id);
    document.getElementById('priceHint').textContent = hintOf(id);
    document.getElementById('priceInput').value =
      typeof state.prices[id] === 'number' ? state.prices[id] : '';
    document.getElementById('priceErr').textContent = '';
    document.getElementById('pricePanel').hidden = false;
    document.getElementById('priceInput').focus();
  }
  function closePrice() {
    pendingId = null;
    document.getElementById('pricePanel').hidden = true;
  }

  document.getElementById('priceOk').onclick = function () {
    var raw = document.getElementById('priceInput').value;
    var err = document.getElementById('priceErr');
    if (raw.trim() === '') { err.textContent = '금액을 넣어주세요'; return; }
    var v = Number(raw);
    if (!isFinite(v) || v < 0) { err.textContent = '0 이상의 숫자를 넣어주세요'; return; }
    state.prices[pendingId] = v;
    save(state); closePrice(); renderChips();
  };

  document.getElementById('priceCancel').onclick = function () {
    // 금액을 안 넣었으면 선택도 되돌린다. 금액 없는 항목이 남으면 총액이 틀어진다.
    if (pendingId && SubMath.missingPriceIds([pendingId], state.prices).length > 0) {
      var i = state.selected.indexOf(pendingId);
      if (i >= 0) state.selected.splice(i, 1);
      save(state);
    }
    closePrice(); renderChips();
  };

  document.getElementById('addCustomBtn').onclick = function () {
    document.getElementById('customName').value = '';
    document.getElementById('customPrice').value = '';
    document.getElementById('customErr').textContent = '';
    document.getElementById('customPanel').hidden = false;
  };
  document.getElementById('customCancel').onclick = function () {
    document.getElementById('customPanel').hidden = true;
  };
  document.getElementById('customOk').onclick = function () {
    var name = document.getElementById('customName').value.trim();
    var raw = document.getElementById('customPrice').value;
    var err = document.getElementById('customErr');
    if (name === '') { err.textContent = '이름을 넣어주세요'; return; }
    if (raw.trim() === '') { err.textContent = '금액을 넣어주세요'; return; }
    var v = Number(raw);
    if (!isFinite(v) || v < 0) { err.textContent = '0 이상의 숫자를 넣어주세요'; return; }
    var id = 'custom-' + (state.custom.length + 1) + '-' + name.length;
    state.custom.push({ id: id, name: name, category: '직접 추가', price: v });
    state.prices[id] = v;
    state.selected.push(id);
    save(state);
    document.getElementById('customPanel').hidden = true;
    renderChips();
  };

  renderChips();
</script>
</body>
</html>
```

- [ ] **Step 2: 브라우저에서 확인한다**

`C:\Users\atlia\Desktop\huhsame-script\.claude\launch.json` 의 `configurations` 배열에 항목을 추가한다
(그 파일이 미리보기 도구가 읽는 파일이다. metaluca-fresh 안에 만들지 않는다).

```json
{
  "name": "subdiet-web",
  "runtimeExecutable": "npx",
  "runtimeArgs": ["-y", "serve", "C:/Users/atlia/Documents/metaluca-fresh", "-l", "5187"],
  "port": 5187
}
```

preview_start 로 띄우고 `http://localhost:5187/subscription-diet.html` 로 이동해 실제 클릭으로 확인한다.

확인 항목:
1. 분류별로 칩이 그려지고, 가격이 있는 항목은 칩에 금액이 함께 보인다
2. 가격이 있는 항목(넷플릭스)을 누르면 바로 선택된다
3. 가격이 없는 항목(웨이브)을 누르면 금액 패널이 열린다
4. 금액을 넣고 확인하면 칩에 금액이 표시되고 선택된다
5. 금액 패널에서 취소하면 그 항목의 선택이 풀린다 (금액 없는 채로 선택되면 안 된다)
6. 직접 추가로 이름과 금액을 넣으면 칩이 생기고 선택된다
7. 하나도 안 골랐을 때 "얼마 쓰는지 보기" 버튼이 비활성이다
8. 새로고침해도 고른 것과 고친 금액이 남아 있다
9. 빈 값·문자·음수를 넣으면 메시지가 뜨고 상태가 망가지지 않는다
10. `grep -c "prompt(\|alert(\|confirm(" subscription-diet.html` 이 0이다
11. 콘솔 에러가 0건이다

- [ ] **Step 3: 커밋한다**

launch.json은 다른 저장소 소속이므로 커밋하지 않는다.

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add subscription-diet.html
git commit -m "구독료 다이어트: 구독 고르는 화면 추가

분류별 칩을 눌러 고르고, 가격을 모르는 항목은 그 자리에서 금액을 물어봐요.
목록에 없는 구독은 직접 추가할 수 있어요. 네이티브 대화상자를 쓰지 않고
화면 안에서 입력을 받아요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 충격 화면과 다이어트 화면

**Files:**
- Modify: `C:\Users\atlia\Documents\metaluca-fresh\subscription-diet.html`

**Interfaces:**
- Consumes: `SubMath.monthlyTotal`, `SubMath.summarize`, `SubMath.savings`, `SubMath.topSpenders`
- Produces: 없음 (마지막 화면)

- [ ] **Step 1: 두 화면의 마크업을 추가한다**

`<section id="pick">` 이 닫힌 뒤에 넣는다.

```html
  <section id="result" hidden>
    <p class="lead" id="countLine"></p>
    <div class="panel" style="text-align:center">
      <div style="color:var(--muted);font-size:14px">한 달</div>
      <div style="font-size:26px;font-weight:700" id="monthly">-</div>
      <div style="color:var(--muted);font-size:14px;margin-top:14px">1년이면</div>
      <div style="font-size:40px;font-weight:800;color:var(--warn);letter-spacing:-1px" id="yearly">-</div>
      <div style="color:var(--muted);font-size:14px;margin-top:14px" id="perDay"></div>
      <div style="color:var(--muted);font-size:14px" id="coffee"></div>
    </div>
    <div class="panel">
      <h2>제일 비싼 것</h2>
      <div id="topList"></div>
    </div>
    <div class="bar">
      <button id="toDiet">안 쓰는 것 골라내기</button>
      <div style="height:10px"></div>
      <button class="ghost" id="shareBtn">공유하기</button>
      <div style="height:10px"></div>
      <button class="ghost" id="backToPick">다시 고르기</button>
    </div>
  </section>

  <section id="diet" hidden>
    <h1>3개월 넘게 안 쓴 게 있나요?</h1>
    <p class="lead">고른 것 중에서 눌러주세요.</p>
    <div id="unusedArea"></div>
    <div class="panel" id="savingBox" hidden style="text-align:center">
      <div style="color:var(--muted);font-size:14px">이것만 정리하면</div>
      <div style="font-size:32px;font-weight:800;color:var(--good)" id="savingYear">-</div>
      <div style="color:var(--muted);font-size:14px">1년에 아껴요</div>
    </div>
    <div class="bar">
      <button class="ghost" id="backToResult">뒤로</button>
    </div>
    <p class="note">해지는 각 서비스에서 해야 해요</p>
  </section>
```

- [ ] **Step 2: 화면 전환과 계산을 붙인다**

`renderChips();` 호출 바로 위에 넣는다.

```js
  function show(id) {
    ['pick', 'result', 'diet'].forEach(function (s) {
      document.getElementById(s).hidden = s !== id;
    });
    window.scrollTo(0, 0);
  }

  function renderResult() {
    var sum = SubMath.summarize(SubMath.monthlyTotal(state.selected, state.prices));
    document.getElementById('countLine').textContent =
      '쓰고 계신 구독 ' + state.selected.length + '개';
    document.getElementById('monthly').textContent = won(sum.monthly);
    document.getElementById('yearly').textContent = won(sum.yearly) + ' 💸';
    document.getElementById('perDay').textContent = '하루로 치면 ' + won(sum.daily);
    document.getElementById('coffee').textContent = '커피로 치면 ' + sum.coffees + '잔';

    var list = document.getElementById('topList');
    list.textContent = '';
    SubMath.topSpenders(state.selected, state.prices, 3).forEach(function (row, i) {
      var d = document.createElement('div');
      d.style.cssText = 'display:flex;justify-content:space-between;padding:10px 0;'
        + (i > 0 ? 'border-top:1px solid var(--line)' : '');
      var left = document.createElement('span');
      left.textContent = (i + 1) + '. ' + nameOf(row.id);
      var right = document.createElement('strong');
      right.textContent = won(row.price);
      d.appendChild(left); d.appendChild(right);
      list.appendChild(d);
    });
  }

  function renderDiet() {
    var area = document.getElementById('unusedArea');
    area.textContent = '';
    var box = document.createElement('div');
    box.className = 'chips';
    state.selected.forEach(function (id) {
      var b = document.createElement('button');
      b.type = 'button';
      b.className = 'chip' + (state.unused.indexOf(id) >= 0 ? ' on' : '');
      b.textContent = nameOf(id);
      b.onclick = function () {
        var i = state.unused.indexOf(id);
        if (i >= 0) state.unused.splice(i, 1); else state.unused.push(id);
        save(state); renderDiet();
      };
      box.appendChild(b);
    });
    area.appendChild(box);

    var saving = SubMath.savings(state.unused, state.prices);
    var boxEl = document.getElementById('savingBox');
    if (state.unused.length === 0) {
      boxEl.hidden = true;
    } else {
      boxEl.hidden = false;
      document.getElementById('savingYear').textContent = won(saving.yearly);
    }
  }

  document.getElementById('toResult').onclick = function () { renderResult(); show('result'); };
  document.getElementById('backToPick').onclick = function () { renderChips(); show('pick'); };
  document.getElementById('toDiet').onclick = function () { renderDiet(); show('diet'); };
  document.getElementById('backToResult').onclick = function () { renderResult(); show('result'); };

  document.getElementById('shareBtn').onclick = function () {
    var sum = SubMath.summarize(SubMath.monthlyTotal(state.selected, state.prices));
    var text = '저는 구독료로 1년에 ' + won(sum.yearly) + ' 쓰고 있었어요 😱';
    try {
      if (window.AppsInToss && window.AppsInToss.share) {
        window.AppsInToss.share({ message: text });
        return;
      }
    } catch (e) {}
    if (navigator.share) { navigator.share({ text: text }).catch(function () {}); }
  };
```

- [ ] **Step 3: 브라우저에서 확인한다**

같은 서버(`http://localhost:5187/subscription-diet.html`)에서 실제 클릭으로 확인한다.

확인 항목:
1. 넷플릭스(13,500)·왓챠(7,900)·애플TV+(6,500)를 고르면 한 달 27,900원, 1년 334,800원이 나온다
   (금액이 서로 달라야 순위 확인이 흔들리지 않는다. 넷플릭스와 티빙은 둘 다 13,500원이라 쓰지 않는다)
2. 하루치와 커피 잔수가 함께 보인다 (334800÷365=917원, 334800÷5000=67잔)
3. "제일 비싼 것"이 넷플릭스 → 왓챠 → 애플TV+ 순으로 나온다
4. "안 쓰는 것 골라내기"에서 왓챠를 고르면 1년에 94,800원 아낀다고 나온다
5. 아무것도 안 골랐으면 절약액 상자가 안 보인다
6. 뒤로·다시 고르기로 화면을 오갈 수 있고 상태가 유지된다
7. 공유하기를 눌러도 에러가 나지 않는다 (브라우저에서는 SDK가 없어 조용히 넘어간다)
8. 콘솔 에러가 0건이다

- [ ] **Step 4: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add subscription-diet.html
git commit -m "구독료 다이어트: 총액 화면과 절약액 화면 추가

연간 총액을 크게 보여주고 하루치·커피 잔수로 환산해요. 안 쓰는 구독을
고르면 1년에 얼마 아끼는지 계산해줘요. 해지는 각 서비스에서 해야 하므로
바깥 링크는 넣지 않았어요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 미니앱 패키지로 감싸기

기존 15개와 같은 구조로 만든다. 참고할 것은 `miniapp-yongdon` 이다(외부 호출이 없고 클래식 스크립트를 `public/`으로 복사하는 구조가 같다).

**Files:**
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-subdiet\package.json`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-subdiet\apps-in-toss.config.ts`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-subdiet\sync.mjs`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-subdiet\src\ait-bridge.js`
- Create: `C:\Users\atlia\Documents\metaluca-fresh\miniapp-subdiet\.gitignore`

**Interfaces:**
- Consumes: `subscription-diet.html`, `subscription-data.js`, `subscription-math.js`
- Produces: `subscription-diet.ait`

- [ ] **Step 1: 패키지 파일 5개를 만든다**

`package.json`:

```json
{
  "name": "subscription-diet-miniapp",
  "private": true,
  "version": "1.0.0",
  "description": "구독료 다이어트 — 앱인토스 미니앱 패키지",
  "type": "module",
  "scripts": {
    "web:dev": "node sync.mjs && vite --port 5188",
    "web:build": "node sync.mjs && vite build",
    "dev": "npm run web:dev",
    "build": "npm run web:build && ait build",
    "deploy": "ait deploy"
  },
  "dependencies": {
    "@apps-in-toss/web-framework": "^3.1.1"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "@apps-in-toss/devtools": "^3.1.1"
  }
}
```

`apps-in-toss.config.ts`:

```ts
import { defineConfig } from '@apps-in-toss/web-framework/config';

export default defineConfig({
  // 앱인토스 콘솔에 등록한 appName과 반드시 일치해야 해요.
  appName: 'subscription-diet',

  webView: {},
  webBundleDir: 'dist',

  brand: {
    primaryColor: '#3182f6'
  },

  permissions: []
});
```

`sync.mjs` — 교체가 각각 적용됐는지 따로 확인한다(부분 실패가 조용히 지나가면 안 된다).

```js
// 루트 subscription-diet.html을 미니앱용 index.html로 변환하는 스크립트.
// 원본 한 벌만 유지하고, 미니앱 전용 차이는 여기서 주입해요.
// 데이터·계산 파일은 클래식 스크립트라 번들링하지 않고 public/으로 그대로 복사해요.
import { readFileSync, writeFileSync, mkdirSync, copyFileSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const here = dirname(fileURLToPath(import.meta.url));
const src = readFileSync(join(here, '..', 'subscription-diet.html'), 'utf8');

mkdirSync(join(here, 'public'), { recursive: true });
for (const f of ['subscription-data.js', 'subscription-math.js']) {
  copyFileSync(join(here, '..', f), join(here, 'public', f));
}

let out = src;

// 교체마다 실제로 적용됐는지 따로 확인한다. 하나만 성공하고 지나가면
// 빌드는 되는데 앱이 조용히 깨진다.
const replacements = [
  ['<script src="subscription-data.js"></script>', '<script src="/subscription-data.js"></script>'],
  ['<script src="subscription-math.js"></script>', '<script src="/subscription-math.js"></script>'],
  ['</head>', '  <script type="module" src="/src/ait-bridge.js"></script>\n</head>'],
];
for (const [from, to] of replacements) {
  if (!out.includes(from)) {
    throw new Error(`sync.mjs: 원본에서 "${from}" 를 찾지 못했어요. 마크업이 바뀌었는지 확인하세요.`);
  }
  out = out.replace(from, to);
}

// 검수 반려를 막는 마지막 방어선. href와 src를 모두 본다.
if (/(?:href|src)="https?:\/\//.test(out)) {
  throw new Error('sync.mjs: 미니앱 빌드에 외부 링크(href 또는 src)가 남아 있어요.');
}

writeFileSync(join(here, 'index.html'), out);
console.log('miniapp-subdiet/index.html generated from ../subscription-diet.html');
```

`src/ait-bridge.js`:

```js
// 앱인토스 SDK 브릿지 — 공유 기능을 미니앱 SDK에 연결해요.
import { share, getTossShareLink } from '@apps-in-toss/web-framework';

window.AppsInToss = {
  share,
  // 공유 링크: 토스 앱에서 이 미니앱을 바로 여는 딥링크
  getTossShareLink: () => getTossShareLink('intoss://subscription-diet'),
};
```

`.gitignore`:

```
node_modules/
dist/
index.html
public/
*.ait
.granite/
```

- [ ] **Step 2: 설치하고 빌드한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh/miniapp-subdiet && npm install && npm run build
```
Expected: `앱인토스 빌드가 완료되었습니다. (subscription-diet.ait)` 와 `deploymentId: ...` 출력.
`npm install` 중 esbuild·protobufjs 관련 `allow-scripts` 경고가 나오는 것은 정상이다.

- [ ] **Step 3: 번들을 확인한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh/miniapp-subdiet
grep -c "SUBSCRIPTION_SERVICES" dist/subscription-data.js
grep -c "SubMath" dist/subscription-math.js
grep -oE '(src|href)="[^"]*"' dist/index.html | grep -vE 'data:|#'
```
Expected: 앞의 두 명령이 1 이상을 출력하고, 세 번째 결과에 `http`로 시작하는 주소가 없다.

- [ ] **Step 4: 미니앱 개발 서버로 확인한다**

`C:\Users\atlia\Desktop\huhsame-script\.claude\launch.json` 에 추가한다.

```json
{
  "name": "miniapp-subdiet",
  "runtimeExecutable": "npm",
  "runtimeArgs": ["--prefix", "C:/Users/atlia/Documents/metaluca-fresh/miniapp-subdiet", "run", "web:dev"],
  "port": 5188
}
```

확인 항목:
1. 콘솔 에러가 0건이다
2. `window.SUBSCRIPTION_SERVICES` 가 배열이고 `window.SubMath` 가 객체다
3. `window.AppsInToss.share` 가 함수다
4. 구독을 고르고 총액 화면까지 실제로 넘어간다

- [ ] **Step 5: 커밋한다**

```bash
cd /c/Users/atlia/Documents/metaluca-fresh
git add miniapp-subdiet/package.json miniapp-subdiet/apps-in-toss.config.ts \
        miniapp-subdiet/sync.mjs miniapp-subdiet/src/ait-bridge.js miniapp-subdiet/.gitignore
git commit -m "구독료 다이어트: 앱인토스 미니앱 패키지 추가

기존 미니앱과 같은 구조예요. sync.mjs가 교체마다 적용 여부를 따로 확인하고
외부 링크는 href와 src를 모두 검사해요. 외부 호출이 없어 CORS 설정은 필요 없어요.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 콘솔 등록과 출시

여기부터는 루카가 직접 해야 한다. API 키와 콘솔 계정이 필요하다.

- [ ] **Step 1: 콘솔에서 앱을 만든다**

- appName: `subscription-diet` (**수정 불가**. `apps-in-toss.config.ts`와 일치해야 한다)
- 한글 이름: 구독료 다이어트 / 영어 이름: Subscription Diet (**하이픈 금지** — 특수문자는 `:` `·` `?` 만 된다)
- 부제(20자, 쉼표 금지): `1년에 얼마 새는지 아세요?`
- **카테고리: 생활 > 재테크 > 재테크**
- 앱 유형: 비게임

- [ ] **Step 2: 배포한다**

```powershell
npm --prefix C:/Users/atlia/Documents/metaluca-fresh/miniapp-subdiet run deploy
```

- [ ] **Step 3: QR로 실기기에서 확인한다**

특히 **금액 입력 패널이 제대로 뜨는지** 본다. 네이티브 대화상자를 안 쓰는 이유가 그것이다.

- [ ] **Step 4: 검토를 요청한다**

출시 노트:

```
1. 신규 앱입니다. 쓰고 있는 구독 서비스를 고르면 한 달과 1년에 얼마를
   쓰는지 계산해서 보여주는 앱입니다.
2. 분류별 목록에서 구독을 고르면 금액이 합산되고, 목록에 없는 서비스는
   직접 추가할 수 있습니다. 금액은 사용자가 직접 고칠 수 있습니다.
3. 3개월 넘게 안 쓴 구독을 고르면 정리했을 때 1년에 얼마를 아끼는지
   알려줍니다. 해지 기능은 없으며 안내만 합니다.
4. 모든 기록은 사용자 기기에만 저장되며 서버로 전송하지 않습니다.
```

---

## 나중에 (이번 범위 아님)

- 인앱 광고 배너 — 광고 ID 발급 후 총액 화면 아래에 하나. 고르는 화면에는 넣지 않는다(완주율)
- 가격 목록 채우기 — 40개 중 11개에 값이 있고 29개는 비어 있다. 확인되는 대로 `subscription-data.js` 한 곳만 고치면 된다
- 공유 카드 이미지 — 지금은 텍스트 공유만 한다

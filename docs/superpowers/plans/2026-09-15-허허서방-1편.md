# 허허서방 1편 「머슴 돌쇠와 낡은 부채」 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 20분짜리 가로 역전 야담 영상 1편(final.mp4)과, 2편부터 대본·프롬프트만 바꿔 재사용할 롱폼 제작 도구를 만든다.

**Architecture:** 루릭 내레이션 스크립트를 복사해 `docs/전래동화/도구/`에 롱폼용으로 새로 짠다. 순수 함수(대본 파싱·타임라인 계산·자막 분할·프롬프트 조립·그림 맞춤)는 `script_io.py`, `timeline.py`, `imaging.py`에 모아 unittest로 검증하고, 외부 호출(워커 TTS, 힉스필드 CLI, ffmpeg)은 `tts.py`, `gen_images.py`, `build.py`가 맡는다. 에피소드 폴더 안에서 `python ../도구/xxx.py`로 실행한다.

**Tech Stack:** Python 3.14 표준 라이브러리(unittest, wave, urllib), Pillow 12, ffmpeg 8.1(zoompan·subtitles 필터), higgsfield CLI(z_image), Cloudflare 워커 `/voice/tts`·`/voice/stt`.

**설계 문서:** `docs/superpowers/specs/2026-09-15-허허서방-1편-design.md`

**루카 확인 관문(여기서 멈추고 승인받기):** Task 9 포즈 웹 생성, Task 10 대본 승인, Task 13 크레딧 쓰기 전 + 웹 장면 생성, Task 14 최종 시청.

---

## 파일 구조

```
docs/전래동화/
  도구/
    script_io.py      # 대본.md 파싱, 장면 태그 해석, 프롬프트 조립 (순수)
    timeline.py       # 줄별 길이 → 줄/장면 타임라인, 자막 큐 분할, SRT 문자열 (순수)
    imaging.py        # 16:9 커버 맞춤, 웹 그림 색감 보정, 엔딩 카드, 켄 번스 필터 문자열 (순수+Pillow)
    tts.py            # 워커 TTS(Umbriel) 줄별 wav + durations.json, STT 받아쓰기 대조
    gen_images.py     # plan / z / huh / import / status
    build.py          # audio / video / subs
    tests/
      test_script_io.py
      test_timeline.py
      test_imaging.py
      test_tts.py
  허허서방/            # 포즈 4장 (인사, 부채탁, 뜸들이기, 껄껄) + 웹프롬프트.md
  01_머슴돌쇠와낡은부채/
    대본.md, prompts.json, 웹프롬프트.md, 받아쓰기.md, 배포문구.md
    audio/NNN.wav, audio/durations.json, narration.wav, timeline.json
    images/SNN.png, clips/, draft.mp4, subs.srt, final.mp4, 썸네일.jpg
```

테스트 실행 명령(모든 Task 공통):

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구" && python -m unittest discover -s tests -v
```

---

### Task 1: 대본 파싱과 프롬프트 조립 (`script_io.py`)

**Files:**
- Create: `docs/전래동화/도구/script_io.py`
- Test: `docs/전래동화/도구/tests/test_script_io.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# docs/전래동화/도구/tests/test_script_io.py
import sys, tempfile, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from script_io import parse_script, scene_kind, build_prompt, Z_MAX

SAMPLE = """# 대본
| 번호 | 장면 | 내레이션 |
|---|---|---|
| 001 | S01 | 옛날 옛적, 돌쇠라는 머슴이 살았습니다. |
| 002 | S01 | 돌쇠는 글자 한 자 몰랐지요. |
| 003 | S02 | 그러던 어느 겨울날이었습니다. |
"""

PROMPTS = {
    "styles": {"base": "MINHWA.", "sil": "SUNSET.", "web": "WEBSTYLE."},
    "characters": {"DOLSOE": "a ragged servant", "CHOI": "a fat rich man"},
    "scenes": {
        "S01": "DOLSOE sweeps the yard.",
        "S02": "[SIL] CHOI sneaks to the storehouse.",
        "S03": "[WEB] DOLSOE holds up a fan before CHOI.",
        "S04": "[HUH:부채탁]",
    },
}


class ParseScriptTest(unittest.TestCase):
    def write(self, text):
        f = Path(tempfile.mkdtemp()) / "대본.md"
        f.write_text(text, encoding="utf-8")
        return f

    def test_reads_rows_in_order(self):
        rows = parse_script(self.write(SAMPLE))
        self.assertEqual([r["num"] for r in rows], ["001", "002", "003"])
        self.assertEqual(rows[2], {"num": "003", "scene": "S02", "text": "그러던 어느 겨울날이었습니다."})

    def test_rejects_line_over_200_chars(self):
        long = "가" * 201
        with self.assertRaises(ValueError):
            parse_script(self.write(f"| 001 | S01 | {long} |\n"))


class SceneKindTest(unittest.TestCase):
    def test_tags(self):
        self.assertEqual(scene_kind("plain scene"), ("base", "plain scene"))
        self.assertEqual(scene_kind("[SIL] dusk"), ("sil", "dusk"))
        self.assertEqual(scene_kind("[WEB] action"), ("web", "action"))
        self.assertEqual(scene_kind("[HUH:껄껄]"), ("huh", "껄껄"))


class BuildPromptTest(unittest.TestCase):
    def test_base_replaces_characters(self):
        kind, prompt = build_prompt(PROMPTS, "S01")
        self.assertEqual(kind, "base")
        self.assertEqual(prompt, "MINHWA. Scene: a ragged servant sweeps the yard.")

    def test_sil_uses_sunset_style(self):
        self.assertEqual(build_prompt(PROMPTS, "S02"), ("sil", "SUNSET. Scene: a fat rich man sneaks to the storehouse."))

    def test_web_uses_web_style(self):
        kind, prompt = build_prompt(PROMPTS, "S03")
        self.assertEqual(kind, "web")
        self.assertTrue(prompt.startswith("WEBSTYLE. Scene: a ragged servant holds up"))

    def test_huh_returns_pose_name(self):
        self.assertEqual(build_prompt(PROMPTS, "S04"), ("huh", "부채탁"))

    def test_z_prompt_length_limit(self):
        p = {**PROMPTS, "scenes": {"S09": "x" * Z_MAX}}
        with self.assertRaises(ValueError):
            build_prompt(p, "S09")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: 테스트 실행 명령
Expected: `ModuleNotFoundError: No module named 'script_io'`

- [ ] **Step 3: 구현**

```python
# docs/전래동화/도구/script_io.py
"""허허서방 롱폼 공용: 대본.md 파싱, 장면 태그 해석, 이미지 프롬프트 조립.

대본.md 형식:  | 001 | S01 | 내레이션 한두 문장 |
prompts.json 형식:
  {"styles": {"base": "...", "sil": "...", "web": "..."},
   "characters": {"DOLSOE": "영문 옷차림 묘사", ...},
   "scenes": {"S01": "장면 묘사", "S02": "[SIL] ...", "S03": "[WEB] ...", "S04": "[HUH:부채탁]"}}
"""
import re
from pathlib import Path

ROW = re.compile(r"^\|\s*(\d{3})\s*\|\s*(S\d{2})\s*\|\s*(.+?)\s*\|\s*$")
MAX_LINE_CHARS = 200  # 워커 TTS 한 번 호출 단위
TAG = re.compile(r"^\[(SIL|WEB|HUH:[^\]]+)\]\s*")
Z_MAX = 800  # Z Image는 약 800자 넘으면 job failed (루릭 2편에서 확인)


def parse_script(path):
    rows = []
    for line in Path(path).read_text(encoding="utf-8").splitlines():
        m = ROW.match(line)
        if not m:
            continue
        num, scene, text = m.groups()
        if len(text) > MAX_LINE_CHARS:
            raise ValueError(f"{num}번 줄이 {len(text)}자예요 (최대 {MAX_LINE_CHARS}자)")
        rows.append({"num": num, "scene": scene, "text": text})
    return rows


def scene_kind(raw):
    m = TAG.match(raw)
    if not m:
        return "base", raw
    tag, rest = m.group(1), raw[m.end():]
    if tag.startswith("HUH:"):
        return "huh", tag[4:]
    return tag.lower(), rest


def build_prompt(p, sid):
    kind, body = scene_kind(p["scenes"][sid])
    if kind == "huh":
        return kind, body
    for key, desc in p["characters"].items():
        body = body.replace(key, desc)
    prompt = f"{p['styles'][kind]} Scene: {body}"
    if kind != "web" and len(prompt) >= Z_MAX:
        raise ValueError(f"{sid} 프롬프트가 {len(prompt)}자예요 — Z Image는 {Z_MAX}자 미만")
    return kind, prompt
```

- [ ] **Step 4: 통과 확인**

Run: 테스트 실행 명령
Expected: `Ran 8 tests ... OK`

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/script_io.py docs/전래동화/도구/tests/test_script_io.py && git commit -m "허허서방 도구: 대본 파싱·장면 태그·프롬프트 조립

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: 타임라인 계산과 자막 큐 (`timeline.py`)

**Files:**
- Create: `docs/전래동화/도구/timeline.py`
- Test: `docs/전래동화/도구/tests/test_timeline.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# docs/전래동화/도구/tests/test_timeline.py
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from timeline import build_timeline, split_cues, wrap, to_srt, GAP_LINE, GAP_SCENE

LINES = [
    {"num": "001", "scene": "S01", "text": "가", "sec": 2.0},
    {"num": "002", "scene": "S01", "text": "나", "sec": 3.0},
    {"num": "003", "scene": "S02", "text": "다", "sec": 1.0},
]


class TimelineTest(unittest.TestCase):
    def test_gaps_and_scene_spans(self):
        lines, scenes, total = build_timeline(LINES)
        self.assertEqual([l["start"] for l in lines], [0.0, 2.0 + GAP_LINE, 5.0 + GAP_LINE + GAP_SCENE])
        self.assertEqual(lines[1]["gap"], GAP_SCENE)  # 다음 줄이 다른 장면
        self.assertEqual(total, round(6.0 + GAP_LINE + GAP_SCENE + GAP_LINE, 3))
        self.assertEqual([s["scene"] for s in scenes], ["S01", "S02"])
        self.assertAlmostEqual(sum(s["dur"] for s in scenes), total, places=2)
        self.assertEqual(scenes[1]["start"], lines[2]["start"])

    def test_repeated_scene_later_is_new_segment(self):
        seq = LINES + [{"num": "004", "scene": "S01", "text": "라", "sec": 1.0}]
        _, scenes, _ = build_timeline(seq)
        self.assertEqual([s["scene"] for s in scenes], ["S01", "S02", "S01"])


class CueTest(unittest.TestCase):
    def test_wrap_24(self):
        text = "옛날 옛적 경상도 어느 고을에 돌쇠라는 머슴이 살았습니다"
        for line in wrap(text).split("\n"):
            self.assertLessEqual(len(line), 24)

    def test_short_line_is_one_cue(self):
        cues = split_cues("짧은 문장입니다.", start=10.0, sec=2.0)
        self.assertEqual(len(cues), 1)
        self.assertEqual((cues[0][0], cues[0][1]), (10.0, 12.0))

    def test_long_line_splits_by_length_and_covers_duration(self):
        text = "돌쇠는 글자 한 자 읽을 줄 몰랐지만 새벽닭이 울기도 전에 일어나 소죽을 끓이고 마당을 쓸었지요 주인집 최 부자는 그런 돌쇠를 사람 취급도 하지 않았습니다"
        cues = split_cues(text, start=0.0, sec=9.0)
        self.assertGreater(len(cues), 1)
        self.assertEqual(cues[0][0], 0.0)
        self.assertAlmostEqual(cues[-1][1], 9.0, places=2)
        for _, _, body in cues:
            self.assertLessEqual(len(body.split("\n")), 2)
        self.assertEqual(" ".join(b.replace("\n", " ") for _, _, b in cues), text)

    def test_srt_format(self):
        srt = to_srt([(0.0, 1.5, "첫 줄"), (61.25, 62.0, "둘째")])
        self.assertIn("1\n00:00:00,000 --> 00:00:01,500\n첫 줄\n", srt)
        self.assertIn("2\n00:01:01,250 --> 00:01:02,000\n둘째\n", srt)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: 테스트 실행 명령
Expected: `No module named 'timeline'`

- [ ] **Step 3: 구현**

```python
# docs/전래동화/도구/timeline.py
"""줄별 음성 길이 → 줄/장면 타임라인, 자막 큐 분할, SRT 문자열."""

GAP_LINE = 0.5   # 같은 장면 안 줄 사이 숨 고르기
GAP_SCENE = 0.9  # 장면이 바뀌기 직전 쉼
WIDTH = 24       # 가로 1920 자막 한 줄 글자 수
MAX_LINES = 2


def build_timeline(lines):
    """lines: [{num, scene, text, sec}] → (줄 목록+start/end/gap, 장면 구간 목록, 전체 길이)."""
    t, out, scenes = 0.0, [], []
    for i, ln in enumerate(lines):
        nxt = lines[i + 1]["scene"] if i + 1 < len(lines) else None
        gap = GAP_SCENE if nxt is not None and nxt != ln["scene"] else GAP_LINE
        if not scenes or scenes[-1]["scene"] != ln["scene"]:
            scenes.append({"scene": ln["scene"], "start": round(t, 3)})
        out.append({**ln, "start": round(t, 3), "end": round(t + ln["sec"], 3), "gap": gap})
        t += ln["sec"] + gap
    total = round(t, 3)
    for i, s in enumerate(scenes):
        end = scenes[i + 1]["start"] if i + 1 < len(scenes) else total
        s["dur"] = round(end - s["start"], 3)
    return out, scenes, total


def wrap(text, width=WIDTH):
    words, lines, cur = text.split(), [], ""
    for w in words:
        if cur and len(cur) + 1 + len(w) > width:
            lines.append(cur)
            cur = w
        else:
            cur = f"{cur} {w}".strip()
    if cur:
        lines.append(cur)
    return "\n".join(lines)


def split_cues(text, start, sec, width=WIDTH, max_lines=MAX_LINES):
    """한 줄 내레이션을 화면에 2줄 이하로 들어가는 큐들로 쪼개고 글자 수 비율로 시간 배분."""
    chunks, cur = [], ""
    for w in text.split():
        cand = f"{cur} {w}".strip()
        if cur and len(wrap(cand, width).split("\n")) > max_lines:
            chunks.append(cur)
            cur = w
        else:
            cur = cand
    if cur:
        chunks.append(cur)
    total_chars = sum(len(c) for c in chunks)
    cues, t = [], start
    for i, c in enumerate(chunks):
        end = start + sec if i == len(chunks) - 1 else t + sec * len(c) / total_chars
        cues.append((round(t, 3), round(end, 3), wrap(c, width)))
        t = end
    return cues


def _ts(t):
    ms = int(round(t * 1000))
    h, ms = divmod(ms, 3600000)
    m, ms = divmod(ms, 60000)
    s, ms = divmod(ms, 1000)
    return f"{h:02d}:{m:02d}:{s:02d},{ms:03d}"


def to_srt(cues):
    return "\n".join(f"{i}\n{_ts(a)} --> {_ts(b)}\n{body}\n" for i, (a, b, body) in enumerate(cues, 1))
```

- [ ] **Step 4: 통과 확인**

Run: 테스트 실행 명령
Expected: `OK` (Task 1 포함 14 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/timeline.py docs/전래동화/도구/tests/test_timeline.py && git commit -m "허허서방 도구: 줄·장면 타임라인과 자막 큐 분할

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: 그림 맞춤·색감 보정·엔딩 카드·켄 번스 (`imaging.py`)

**Files:**
- Create: `docs/전래동화/도구/imaging.py`
- Test: `docs/전래동화/도구/tests/test_imaging.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# docs/전래동화/도구/tests/test_imaging.py
import sys, tempfile, unittest
from pathlib import Path
from PIL import Image, ImageStat
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from imaging import fit_cover, web_tone, make_end_card, kenburns_filter, SIZE


class ImagingTest(unittest.TestCase):
    def test_fit_cover_any_ratio_to_1920x1080(self):
        for size in [(1024, 1536), (2048, 1152), (800, 800)]:
            self.assertEqual(fit_cover(Image.new("RGB", size, "red")).size, SIZE)

    def test_web_tone_raises_saturation(self):
        img = Image.new("RGB", SIZE, (150, 120, 100))
        before = ImageStat.Stat(img.convert("HSV")).mean[1]
        after = ImageStat.Stat(web_tone(img).convert("HSV")).mean[1]
        self.assertGreater(after, before)

    def test_end_card_size(self):
        d = Path(tempfile.mkdtemp())
        Image.new("RGB", (600, 800), "orange").save(d / "art.jpg")
        make_end_card(d / "art.jpg", d / "end.png")
        self.assertEqual(Image.open(d / "end.png").size, SIZE)

    def test_kenburns_filter(self):
        f_in = kenburns_filter(90, zoom_in=True)
        f_out = kenburns_filter(90, zoom_in=False)
        self.assertIn("d=90", f_in)
        self.assertIn("s=1920x1080", f_in)
        self.assertIn("1+0.08*on/90", f_in)
        self.assertIn("1.08-0.08*on/90", f_out)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: 테스트 실행 명령
Expected: `No module named 'imaging'`

- [ ] **Step 3: 구현**

```python
# docs/전래동화/도구/imaging.py
"""그림 16:9 맞춤, 웹 그림 색감 보정, 엔딩 카드, 켄 번스 ffmpeg 필터."""
from pathlib import Path
from PIL import Image, ImageDraw, ImageEnhance, ImageFont

SIZE = (1920, 1080)
FPS = 30
ZOOM = 0.08
PAPER = (245, 240, 228)
GUNGSUH = ("C:/Windows/Fonts/batang.ttc", 2)


def fit_cover(img, size=SIZE):
    """비율이 달라도 가운데 기준으로 잘라 size를 꽉 채운다."""
    img = img.convert("RGB")
    scale = max(size[0] / img.width, size[1] / img.height)
    img = img.resize((round(img.width * scale), round(img.height * scale)), Image.LANCZOS)
    left, top = (img.width - size[0]) // 2, (img.height - size[1]) // 2
    return img.crop((left, top, left + size[0], top + size[1]))


def web_tone(img):
    """웹 Nano Banana 그림은 Z Image보다 색이 옅어서 채도·대비를 조금 올린다."""
    img = ImageEnhance.Color(img).enhance(1.18)
    return ImageEnhance.Contrast(img).enhance(1.06)


def make_end_card(painting, out, credit="그림: Metaluca."):
    card = Image.new("RGB", SIZE, PAPER)
    art = Image.open(painting).convert("RGB")
    art.thumbnail((1100, 900), Image.LANCZOS)
    x, y = 180, (SIZE[1] - art.height) // 2
    card.paste(art, (x, y))
    d = ImageDraw.Draw(card)
    font = ImageFont.truetype(GUNGSUH[0], 80, index=GUNGSUH[1])
    d.text((x + art.width + 90, SIZE[1] // 2 - 50), credit, font=font, fill=(40, 34, 30))
    card.save(out)
    return out


def kenburns_filter(frames, zoom_in=True):
    z = f"1+{ZOOM}*on/{frames}" if zoom_in else f"{1 + ZOOM:.2f}-{ZOOM}*on/{frames}"
    return (f"scale=3840:2160:force_original_aspect_ratio=increase,crop=3840:2160,"
            f"zoompan=z='{z}':x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':d={frames}:s=1920x1080:fps={FPS},"
            f"format=yuv420p")
```

- [ ] **Step 4: 통과 확인**

Run: 테스트 실행 명령
Expected: `OK` (18 tests)

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/imaging.py docs/전래동화/도구/tests/test_imaging.py && git commit -m "허허서방 도구: 16:9 맞춤·웹 그림 보정·엔딩 카드·켄 번스 필터

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: 목소리 생성과 받아쓰기 대조 (`tts.py`)

**Files:**
- Create: `docs/전래동화/도구/tts.py`
- Test: `docs/전래동화/도구/tests/test_tts.py`

- [ ] **Step 1: 실패하는 테스트 작성**

```python
# docs/전래동화/도구/tests/test_tts.py
import sys, unittest
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent.parent))
from tts import payload, needs_check, similar


class TtsTest(unittest.TestCase):
    def test_payload_uses_umbriel(self):
        p = payload("안녕하시게")
        self.assertEqual(p["voice"], "Umbriel")
        self.assertEqual(p["preset"], "middleM")
        self.assertEqual(p["tone"], "calm")
        self.assertEqual(p["text"], "안녕하시게")
        self.assertLessEqual(len(p["style"]), 200)

    def test_needs_check_flags_digits_and_quotes(self):
        self.assertTrue(needs_check("사흘 뒤 3명이 왔다"))
        self.assertTrue(needs_check("“암행어사 출두요!”"))
        self.assertTrue(needs_check('"쉿"'))
        self.assertFalse(needs_check("돌쇠는 마당을 쓸었지요."))

    def test_similar_ignores_punctuation_and_spaces(self):
        self.assertTrue(similar("허허, 어서들 오시게.", "허허 어서들 오시게"))
        self.assertFalse(similar("사흘 뒤", "나흘 뒤"))


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 실패 확인**

Run: 테스트 실행 명령
Expected: `No module named 'tts'`

- [ ] **Step 3: 구현**

```python
# docs/전래동화/도구/tts.py
"""허허서방 목소리(Umbriel)로 대본 줄마다 wav를 뽑고, 위험한 줄은 받아쓰기로 대조한다.

사용법 (에피소드 폴더 안에서):
  python ../도구/tts.py make                 # 없는 줄만 생성 → audio/NNN.wav + audio/durations.json
  python ../도구/tts.py make --only 012,045  # 해당 줄 다시 생성(덮어쓰기)
  python ../도구/tts.py check                # 숫자·따옴표 줄 + --also 줄 받아쓰기 → 받아쓰기.md
  python ../도구/tts.py check --also 030,031
"""
import argparse, base64, json, re, sys, time, urllib.request, wave
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from script_io import parse_script

WORKER = "https://small-recipe-9345.atlia0318.workers.dev/voice"
HEADERS = {
    "Content-Type": "application/json",
    "Origin": "https://vaulted-bus-346411.web.app",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) KillingVoiceBatch/1.0",
}
VOICE = {
    "preset": "middleM",
    "tone": "calm",
    "voice": "Umbriel",
    "style": "구수하고 능청스러운 50대 남자 장터 이야기꾼 목소리로. 낮고 굵은 중저음, 느긋하게 뜸을 들이며 옛날이야기를 풀어내듯",
}


def payload(text):
    return {"text": text, **VOICE}


def needs_check(text):
    return bool(re.search(r"[0-9\"“”‘’'「」]", text))


def _norm(s):
    return re.sub(r"[\s\W_]+", "", s)


def similar(a, b):
    return _norm(a) == _norm(b)


def post(path, body, timeout=180):
    req = urllib.request.Request(f"{WORKER}/{path}", data=json.dumps(body).encode("utf-8"), headers=HEADERS)
    with urllib.request.urlopen(req, timeout=timeout) as r:
        return json.loads(r.read().decode("utf-8"))


def tts(text, retries=3):
    last = None
    for i in range(retries):
        try:
            data = post("tts", payload(text))
            if "audio" in data:
                return base64.b64decode(data["audio"]), int(data.get("rate", 24000))
            last = data.get("error")
        except Exception as e:  # noqa: BLE001 — 네트워크 오류는 재시도
            last = str(e)
        time.sleep(3 * (i + 1))
    raise RuntimeError(f"TTS 실패: {last}")


def save_wav(pcm, rate, path):
    Path(path).parent.mkdir(parents=True, exist_ok=True)
    with wave.open(str(path), "wb") as w:
        w.setnchannels(1)
        w.setsampwidth(2)
        w.setframerate(rate)
        w.writeframes(pcm)
    return len(pcm) / 2 / rate


def wav_sec(path):
    with wave.open(str(path)) as w:
        return w.getnframes() / w.getframerate()


def make(rows, only):
    for r in rows:
        out = Path(f"audio/{r['num']}.wav")
        if out.exists() and not (only and r["num"] in only):
            continue
        if only and r["num"] not in only:
            continue
        pcm, rate = tts(r["text"])
        sec = save_wav(pcm, rate, out)
        print(f"{r['num']} {sec:5.2f}s  {r['text']}", file=sys.stderr)
    durations = [{**r, "sec": round(wav_sec(f"audio/{r['num']}.wav"), 3)}
                 for r in rows if Path(f"audio/{r['num']}.wav").exists()]
    Path("audio/durations.json").write_text(json.dumps(durations, ensure_ascii=False, indent=1), encoding="utf-8")
    total = sum(d["sec"] for d in durations)
    print(json.dumps({"lines": len(durations), "of": len(rows), "voice_min": round(total / 60, 1)}, ensure_ascii=False))


def check(rows, also):
    targets = [r for r in rows if needs_check(r["text"]) or r["num"] in also]
    out = ["| 번호 | 일치 | 원문 | 받아쓰기 |", "|---|---|---|---|"]
    bad = 0
    for r in targets:
        b64 = base64.b64encode(Path(f"audio/{r['num']}.wav").read_bytes()).decode()
        heard = post("stt", {"audio": b64, "mime": "audio/wav"}).get("text", "")
        ok = similar(r["text"], heard)
        bad += not ok
        out.append(f"| {r['num']} | {'✅' if ok else '❌'} | {r['text']} | {heard} |")
    Path("받아쓰기.md").write_text("\n".join(out) + "\n", encoding="utf-8")
    print(json.dumps({"checked": len(targets), "mismatch": bad}, ensure_ascii=False))


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("cmd", choices=["make", "check"])
    ap.add_argument("--only", default="")
    ap.add_argument("--also", default="")
    a = ap.parse_args()
    rows = parse_script("대본.md")
    if a.cmd == "make":
        make(rows, set(filter(None, a.only.split(","))))
    else:
        check(rows, set(filter(None, a.also.split(","))))


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 통과 확인**

Run: 테스트 실행 명령
Expected: `OK` (21 tests)

- [ ] **Step 5: 실제 워커 한 줄 연기 테스트**

```bash
mkdir -p "C:/Users/atlia/AppData/Local/Temp/claude/huh_smoke" && cd "C:/Users/atlia/AppData/Local/Temp/claude/huh_smoke" && printf '| 001 | S01 | 허허, 어서들 오시게. |\n| 002 | S01 | 사흘 뒤 "암행어사 출두요!" |\n' > 대본.md && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/tts.py" make && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/tts.py" check && cat 받아쓰기.md
```

Expected: `{"lines": 2, "of": 2, ...}` 그리고 받아쓰기.md에 002번 한 줄. (❌여도 도구 동작 확인이 목적 — 원문/받아쓰기 칸이 채워지면 통과)

- [ ] **Step 6: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/tts.py docs/전래동화/도구/tests/test_tts.py && git commit -m "허허서방 도구: Umbriel 줄별 TTS와 받아쓰기 대조

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: 그림 도구 (`gen_images.py`)

**Files:**
- Create: `docs/전래동화/도구/gen_images.py`

외부 호출(힉스필드) 위주라 단위 테스트 대신 `plan`(크레딧 0)과 `import`로 검증한다. 순수 로직은 Task 1·3에서 이미 테스트됨.

- [ ] **Step 1: 구현**

```python
# docs/전래동화/도구/gen_images.py
"""장면 그림 준비.

사용법 (에피소드 폴더 안에서):
  python ../도구/gen_images.py plan                  # 장면 종류·길이·예상 크레딧 표 + 웹프롬프트.md 작성 (크레딧 0)
  python ../도구/gen_images.py z [--only S01,S07]    # base·sil 장면을 Z Image로 생성 (없는 것만, --only는 덮어쓰기)
  python ../도구/gen_images.py huh                   # [HUH:포즈] 장면에 허허서방 포즈 복사
  python ../도구/gen_images.py import images/S12.png <다운로드한 파일>   # 16:9 맞춤 + 웹 색감 보정 후 저장
  python ../도구/gen_images.py status                # 빠진 그림 목록
"""
import argparse, json, shutil, subprocess, sys, time, urllib.request
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from script_io import build_prompt, parse_script
from imaging import fit_cover, web_tone
from PIL import Image

HF = shutil.which("higgsfield") or shutil.which("higgsfield.cmd")
HUH_DIR = Path(__file__).resolve().parent.parent / "허허서방"
Z_COST = 0.15
BATCH = 4


def load():
    p = json.loads(Path("prompts.json").read_text(encoding="utf-8"))
    used = sorted({r["scene"] for r in parse_script("대본.md")})
    missing = [s for s in used if s not in p["scenes"]]
    if missing:
        sys.exit(f"prompts.json에 없는 장면: {missing}")
    return p, used


def hf(args):
    r = subprocess.run([HF, *args, "--json"], capture_output=True, text=True, encoding="utf-8")
    if r.returncode != 0:
        raise RuntimeError(r.stderr.strip() or r.stdout.strip())
    return json.loads(r.stdout)


def cmd_plan(p, used):
    counts, web = {"base": 0, "sil": 0, "web": 0, "huh": 0}, []
    for sid in used:
        kind, prompt = build_prompt(p, sid)
        counts[kind] += 1
        size = "-" if kind == "huh" else f"{len(prompt)}자"
        have = "✅" if Path(f"images/{sid}.png").exists() else "  "
        print(f"{have} {sid} {kind:4} {size}")
        if kind == "web":
            web.append(f"## {sid}\n\n참고 그림: `docs/전래동화/그림체샘플/A_민화수묵/02.png`\n\n```\n{prompt}\n```\n")
    Path("웹프롬프트.md").write_text(
        "# 웹에서 뽑을 장면\n\n힉스필드 웹 · **Nano Banana(2 아님) · Unlimited 켜기 · 16:9 · 참고 그림 첨부** · 버튼에 숫자 없는지 확인\n\n"
        + "\n".join(web), encoding="utf-8")
    z = counts["base"] + counts["sil"]
    print(json.dumps({**counts, "z_credits": round(z * Z_COST, 2)}, ensure_ascii=False))


def cmd_z(p, used, only):
    Path("images").mkdir(exist_ok=True)
    todo = []
    for sid in used:
        kind, prompt = build_prompt(p, sid)
        if kind not in ("base", "sil"):
            continue
        if only and sid not in only:
            continue
        if not only and Path(f"images/{sid}.png").exists():
            continue
        todo.append((sid, prompt))
    print(f"Z Image {len(todo)}장, 약 {len(todo) * Z_COST:.2f}크레딧", file=sys.stderr)
    logp = Path("images/jobs.json")
    log = json.loads(logp.read_text(encoding="utf-8")) if logp.exists() else {}
    for i in range(0, len(todo), BATCH):
        jobs = {}
        for sid, prompt in todo[i:i + BATCH]:
            jobs[sid] = hf(["generate", "create", "z_image", "--prompt", prompt, "--aspect_ratio", "16:9"])[0]
            time.sleep(1)
        for sid, jid in jobs.items():
            try:
                res = hf(["generate", "wait", jid])
                url = res.get("result_url")
                if res.get("status") == "completed" and url:
                    urllib.request.urlretrieve(url, f"images/{sid}.png")
                    log[sid] = {"job": jid, "status": "completed"}
                    print(f"{sid} 저장", file=sys.stderr)
                else:
                    log[sid] = {"job": jid, "status": res.get("status")}
                    print(f"{sid} 실패 {res.get('status')}", file=sys.stderr)
            except RuntimeError as e:
                log[sid] = {"job": jid, "status": "failed"}
                print(f"{sid} 실패 {e}", file=sys.stderr)
            logp.write_text(json.dumps(log, ensure_ascii=False, indent=1), encoding="utf-8")


def cmd_huh(p, used):
    Path("images").mkdir(exist_ok=True)
    for sid in used:
        kind, pose = build_prompt(p, sid)
        if kind == "huh":
            src = HUH_DIR / f"{pose}.png"
            if not src.exists():
                sys.exit(f"허허서방 포즈 없음: {src}")
            shutil.copy(src, f"images/{sid}.png")
            print(f"{sid} ← {pose}")


def cmd_import(dest, src):
    Path(dest).parent.mkdir(parents=True, exist_ok=True)
    web_tone(fit_cover(Image.open(src))).save(dest)
    print(f"{dest} 저장 (1920x1080, 웹 색감 보정)")


def cmd_status(p, used):
    missing = [s for s in used if not Path(f"images/{s}.png").exists()]
    print(json.dumps({"scenes": len(used), "missing": missing}, ensure_ascii=False))


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("cmd", choices=["plan", "z", "huh", "import", "status"])
    ap.add_argument("args", nargs="*")
    ap.add_argument("--only", default="")
    a = ap.parse_args()
    if a.cmd == "import":
        return cmd_import(*a.args)
    p, used = load()
    if a.cmd == "plan":
        cmd_plan(p, used)
    elif a.cmd == "z":
        cmd_z(p, used, set(filter(None, a.only.split(","))))
    elif a.cmd == "huh":
        cmd_huh(p, used)
    else:
        cmd_status(p, used)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 크레딧 없이 동작 확인**

```bash
cd "C:/Users/atlia/AppData/Local/Temp/claude/huh_smoke" && printf '{"styles":{"base":"MINHWA.","sil":"SUNSET.","web":"WEB."},"characters":{"DOLSOE":"a ragged servant"},"scenes":{"S01":"[WEB] DOLSOE bows."}}' > prompts.json && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/gen_images.py" plan && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/gen_images.py" import images/S01.png "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/그림체샘플/A_민화수묵/03_웹나노바나나.png" && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/gen_images.py" status
```

Expected: `S01 web` 줄, `{"base": 0, "sil": 0, "web": 1, "huh": 0, "z_credits": 0.0}`, 웹프롬프트.md 생성, `images/S01.png 저장`, `{"scenes": 1, "missing": []}`

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/gen_images.py && git commit -m "허허서방 도구: 장면 그림 plan·Z Image·포즈 복사·웹 그림 가져오기

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: 조립 도구 (`build.py`)

**Files:**
- Create: `docs/전래동화/도구/build.py`

- [ ] **Step 1: 구현**

```python
# docs/전래동화/도구/build.py
"""audio/durations.json + images → narration.wav, timeline.json, draft.mp4, final.mp4

사용법 (에피소드 폴더 안에서):
  python ../도구/build.py audio
  python ../도구/build.py video [--endcard 경로]
  python ../도구/build.py subs
"""
import argparse, json, subprocess, sys, wave
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
from timeline import build_timeline, split_cues, to_srt
from imaging import FPS, kenburns_filter, make_end_card

END_SEC = 5
DEFAULT_ENDCARD = Path.home() / "Desktop" / "피지컬아트" / "집가는길.jpg"


def ff(*args):
    subprocess.run(["ffmpeg", "-y", "-loglevel", "error", *map(str, args)], check=True)


def build_audio():
    lines = json.loads(Path("audio/durations.json").read_text(encoding="utf-8"))
    tl_lines, scenes, total = build_timeline(lines)
    rate, pcm = None, bytearray()
    for ln in tl_lines:
        with wave.open(f"audio/{ln['num']}.wav") as w:
            rate = rate or w.getframerate()
            pcm += w.readframes(w.getnframes())
        pcm += b"\x00\x00" * round(ln["gap"] * rate)
    with wave.open("narration.wav", "wb") as w:
        w.setnchannels(1)
        w.setsampwidth(2)
        w.setframerate(rate)
        w.writeframes(bytes(pcm))
    Path("timeline.json").write_text(json.dumps({"lines": tl_lines, "scenes": scenes, "total": total},
                                                ensure_ascii=False, indent=1), encoding="utf-8")
    print(f"줄 {len(tl_lines)}개, 장면 구간 {len(scenes)}개, 총 {int(total // 60)}분 {int(total % 60)}초")


def build_video(endcard):
    tl = json.loads(Path("timeline.json").read_text(encoding="utf-8"))
    missing = sorted({s["scene"] for s in tl["scenes"] if not Path(f"images/{s['scene']}.png").exists()})
    if missing:
        sys.exit(f"그림 없음: {missing}")
    clips = Path("clips")
    clips.mkdir(exist_ok=True)
    make_end_card(endcard, clips / "end.png")
    items = [(f"images/{s['scene']}.png", s["dur"]) for s in tl["scenes"]] + [(clips / "end.png", END_SEC)]
    listing = []
    for i, (img, dur) in enumerate(items):
        frames = max(1, round(dur * FPS))
        out = clips / f"{i:03d}.mp4"
        ff("-i", img, "-vf", kenburns_filter(frames, zoom_in=(i % 2 == 0)), "-frames:v", frames,
           "-c:v", "libx264", "-preset", "veryfast", "-crf", 20, "-r", FPS, out)
        listing.append(f"file '{out.name}'\n")
        print(f"\r클립 {i + 1}/{len(items)}", end="", file=sys.stderr)
    print(file=sys.stderr)
    (clips / "list.txt").write_text("".join(listing), encoding="utf-8")
    ff("-f", "concat", "-safe", 0, "-i", clips / "list.txt", "-i", "narration.wav",
       "-af", "apad", "-t", round(tl["total"] + END_SEC, 3),
       "-c:v", "copy", "-c:a", "aac", "-b:a", "160k", "draft.mp4")
    print("draft.mp4 생성 완료")


def build_subs():
    tl = json.loads(Path("timeline.json").read_text(encoding="utf-8"))
    cues = [c for ln in tl["lines"] for c in split_cues(ln["text"], ln["start"], ln["sec"])]
    Path("subs.srt").write_text(to_srt(cues), encoding="utf-8")
    style = ("FontName=Malgun Gothic,FontSize=15,Bold=1,PrimaryColour=&H00FFFFFF,"
             "BorderStyle=4,BackColour=&H99000000,Outline=2,OutlineColour=&H00000000,Shadow=0,"
             "Alignment=2,MarginV=16,MarginL=40,MarginR=40")
    ff("-i", "draft.mp4", "-vf", f"subtitles=subs.srt:force_style='{style}'",
       "-c:v", "libx264", "-preset", "medium", "-crf", 20, "-c:a", "copy", "final.mp4")
    print(f"final.mp4 생성 완료 (자막 {len(cues)}개)")


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("cmd", choices=["audio", "video", "subs"])
    ap.add_argument("--endcard", default=str(DEFAULT_ENDCARD))
    a = ap.parse_args()
    {"audio": build_audio, "video": lambda: build_video(a.endcard), "subs": build_subs}[a.cmd]()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 연기 테스트 (Task 4·5 연기 폴더 재사용, 그림 1장·음성 2줄)**

```bash
cd "C:/Users/atlia/AppData/Local/Temp/claude/huh_smoke" && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/build.py" audio && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/build.py" video && PYTHONIOENCODING=utf-8 python "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/도구/build.py" subs && ffprobe -v error -show_entries format=duration -of csv=p=0 final.mp4 && ffmpeg -y -loglevel error -ss 1 -i final.mp4 -frames:v 1 frame.jpg
```

Expected: `줄 2개, 장면 구간 1개`, `draft.mp4 생성 완료`, `final.mp4 생성 완료`, 길이 ≈ 음성 합계+1.0초(간격)+5초. `frame.jpg`를 Read로 열어 그림 위에 자막 띠가 아래쪽에 두 줄 이하로 보이는지 눈으로 확인. 자막이 너무 작거나 크면 `FontSize`만 조정 후 subs 재실행.

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/도구/build.py && git commit -m "허허서방 도구: 합본·켄 번스 영상·엔딩 카드·자막 굽기

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: 에피소드 폴더와 고정 인사·마무리 대사

**Files:**
- Create: `docs/전래동화/01_머슴돌쇠와낡은부채/대본.md` (틀만)
- Create: `docs/전래동화/고정대사.md`

- [ ] **Step 1: 고정 대사 파일 작성** (매 편 ②⑦에 그대로 복사)

```markdown
# 허허서방 고정 대사

## ② 인사 (장면: [HUH:인사] → [HUH:부채탁])
허허, 어서들 오시게. 장터 이야기꾼 허허서방이올시다.
오늘도 먼 길 오셨으니, 편히 앉아 들어들 보시게.
자, 그럼 부채 한 번 치고 시작해 봅시다. 탁!

## ⑦ 마무리 (장면: [HUH:껄껄] → 엔딩 카드)
허허, 오늘 이야기는 여기까지올시다.
다음 이야기가 궁금하시면, 엽전 대신 구독 한 번 눌러 주시게.
그럼 편히들 쉬시고, 다음 장날에 또 뵙겠소.
```

- [ ] **Step 2: 대본.md 틀 작성**

```markdown
# 1편 「머슴 돌쇠와 낡은 부채」 대본

| 번호 | 장면 | 내레이션 |
|---|---|---|
```

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/고정대사.md docs/전래동화/01_머슴돌쇠와낡은부채/대본.md && git commit -m "허허서방: 고정 인사·마무리 대사와 1편 폴더

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 8: 도구 전체 테스트 한 번 더 + 연기 폴더 정리

- [ ] **Step 1:** 테스트 실행 명령 → Expected `Ran 21 tests ... OK`
- [ ] **Step 2:** 연기 폴더 `C:/Users/atlia/AppData/Local/Temp/claude/huh_smoke`는 스크래치라 그대로 둔다(프로젝트 밖). 커밋할 것 없음.

---

### Task 9: 허허서방 포즈 4장 (🧑‍🎨 루카 웹 생성)

**Files:**
- Create: `docs/전래동화/허허서방/웹프롬프트.md`
- Create: `docs/전래동화/허허서방/{인사,부채탁,뜸들이기,껄껄}.png`

- [ ] **Step 1: 웹프롬프트.md 작성**

````markdown
# 허허서방 포즈 4장

힉스필드 웹 · **Nano Banana(2 아님) · Unlimited 켜기 · 16:9** · 참고 그림: `docs/전래동화/그림체샘플/A+루카/03_허허서방부채탁.png`

공통 앞부분(매번 붙이기):
```
Korean minhwa ink-wash painting on cream hanji paper mixed with bold acrylic touches, matching the reference image exactly: the same jolly middle-aged Joseon storyteller, black gat hat, short beard, indigo robe, rosy pink round cheeks. Plain cream hanji background, character on the left third, upper body. No text, no letters anywhere.
```

## 인사
```
He bows his head politely with a warm smile, folded fan held in both hands at his chest, welcoming the audience.
```

## 부채탁
```
He snaps his folding fan shut with a flourish and a big grin; bright bursts of polka dots and bubbles in yellow, turquoise, red and white pop around the fan.
```

## 뜸들이기
```
He leans in slyly, resting his chin on the closed fan, eyes narrowed with a knowing half smile, as if about to reveal a secret.
```

## 껄껄
```
He throws his head back laughing heartily, fan open and raised, eyes crinkled shut with joy.
```
````

- [ ] **Step 2: 🧑‍🎨 루카에게 요청** — 위 4개를 웹에서 뽑아 다운로드. 한 장씩 "받았어" 알려주면 다음 단계.

- [ ] **Step 3: 가져오기** (다운로드 폴더 최신 hf_ 파일, 루카가 알려준 순서대로)

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화" && f=$(ls -t /c/Users/atlia/Downloads/hf_*.png | head -1) && PYTHONIOENCODING=utf-8 python 도구/gen_images.py import 허허서방/인사.png "$f"
```

(부채탁·뜸들이기·껄껄도 같은 명령에서 파일명만 바꿔 실행)

- [ ] **Step 4: 4장 Read로 확인** — 같은 얼굴·분홍 볼·갓·남색 옷이 유지되는지, 글자 없는지. 틀리면 루카에게 해당 포즈만 재생성 요청.

- [ ] **Step 5: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/허허서방 && git commit -m "허허서방: 포즈 4장(인사·부채탁·뜸들이기·껄껄)과 웹 프롬프트

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 10: 1편 대본 작성 (🧑‍🎨 루카 승인 관문)

**Files:**
- Modify: `docs/전래동화/01_머슴돌쇠와낡은부채/대본.md`

- [ ] **Step 1: 대본 작성** — 설계 문서 4장 줄거리를 아래 분량표대로 쓴다. 한 줄 1~2문장·200자 이내, 장면당 3~5줄. 허허서방 말투는 "~올시다, ~시게, ~지요". 목소리 테스트 대본(돌쇠·보리밥·나그네·낡은 부채 3문단)을 1막에 그대로 살린다. TTS 함정 피하기: "여기서 끝." 같은 지시처럼 들리는 문장 금지, 숫자는 한글로("사흘", "세 명").

| 구간 | 줄 수 | 장면 번호 | 장면 종류 가이드 |
|---|---|---|---|
| ① 훅 | 5~6 | S01~S02 | S01 `[SIL]` 노을 관아 마당, S02 `[WEB]` 부자가 머슴 앞에 무릎 |
| ② 인사 | 고정대사 3줄 | S03~S04 | `[HUH:인사]`, `[HUH:부채탁]` |
| ③ 1막 | 45~55 | S05~S18 | 기본 민화, 눈 오는 밤 1~2장 `[SIL]` |
| ④ 2막 | 110~130 | S19~S48 | 곳간 빼돌리기 `[SIL]` 4~6장, 옥에 갇힘·몰림 `[WEB]` 2~3장, 끼어들기 `[HUH:뜸들이기]` 2회 |
| ⑤ 3막 | 30~40 | S49~S58 | 부채 꺼냄·어사 출두·무릎 꿇음 `[WEB]` 3~4장 |
| ⑥ 한마디 | 5~7 | S59~S60 | `[HUH:뜸들이기]` → `[HUH:부채탁]` |
| ⑦ 마무리 | 고정대사 3줄 | S61 | `[HUH:껄껄]` |

목표 합계 약 5,000자(공백 포함), 줄 약 210~250개.

- [ ] **Step 2: 기계 점검**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python -c "
import sys; sys.path.insert(0,'../도구')
from script_io import parse_script
r=parse_script('대본.md'); n=sum(len(x['text']) for x in r)
print('줄',len(r),'글자',n,'예상',round(n/250,1),'분','장면',len({x['scene'] for x in r}))"
```

Expected: 글자 4,300~5,700, 예상 17~23분, 장면 55~65. 벗어나면 2막 분량으로 조정.

- [ ] **Step 3: 🧑‍🎨 루카에게 대본.md 전달 → 끝까지 읽고 승인받기.** 수정 요청은 반영 후 Step 2 다시. 승인 전에는 Task 11 이후로 가지 않는다.

- [ ] **Step 4: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/대본.md && git commit -m "허허서방 1편: 대본 완성(루카 승인)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 11: 장면 프롬프트 (`prompts.json`)

**Files:**
- Create: `docs/전래동화/01_머슴돌쇠와낡은부채/prompts.json`

- [ ] **Step 1: 스타일·인물 고정 부분 작성** (scenes는 Step 2에서 채움)

```json
{
 "styles": {
  "base": "Korean traditional minhwa folk painting in ink-wash damchae style: confident black ink brush outlines, soft muted watercolor washes of indigo, ochre, persimmon red and pine green on aged hanji paper texture. Bright, clear, high contrast, faces large and readable. Joseon dynasty. No text, no letters anywhere.",
  "sil": "Korean minhwa ink-wash painting on hanji paper mixed with bold acrylic painting: confident black ink outlines, textured acrylic brushstrokes. Huge blazing orange, red and gold sunset or deep blue moonlit sky with tiny white dots like stars, figures as solid black silhouettes, blue-violet shadows. Joseon dynasty. No text, no letters anywhere.",
  "web": "Korean traditional minhwa folk painting in ink-wash damchae style, matching the style of the reference image: confident black ink brush outlines, soft muted watercolor washes of indigo, ochre, persimmon red and pine green on aged cream hanji paper. Bright and clear, faces large and readable. No text, no letters anywhere."
 },
 "characters": {
  "DOLSOE": "a young Joseon servant in ragged patched gray hemp clothes with a worn straw hat",
  "CHOI": "a fat greedy rich man in a purple silk robe and black gat hat",
  "GUEST": "a thin old traveling scholar in a faded white robe and torn gat hat",
  "SATTO": "an arrogant magistrate in a blue official robe and black official hat"
 },
 "scenes": {}
}
```

- [ ] **Step 2: scenes 채우기** — 대본.md의 장면 번호마다 영어 한두 문장. 태그 규칙: 사람 둘 이상이 서로 동작하면 `[WEB]`(웹 역할별 번호 방식: "1. LEFT: … 2. RIGHT: …"), 밤·음모·긴장은 `[SIL]`, 허허서방은 `[HUH:포즈]`. 기본 장면은 한 사람 또는 사람 없는 풍경 위주.

- [ ] **Step 3: plan으로 점검 (크레딧 0)**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python ../도구/gen_images.py plan
```

Expected: 오류 없음(모든 base·sil 800자 미만), 마지막 줄 `web` 6~10, `z_credits` 약 7~8. 800자 넘는 장면은 scene 문장 줄이기.

- [ ] **Step 4: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/prompts.json docs/전래동화/01_머슴돌쇠와낡은부채/웹프롬프트.md && git commit -m "허허서방 1편: 장면 프롬프트와 웹 생성 목록

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 12: 목소리 생성과 받아쓰기 대조

- [ ] **Step 1: 생성**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python ../도구/tts.py make
```

Expected: `{"lines": N, "of": N, "voice_min": 15~21}`. 중간에 끊기면 같은 명령 재실행(있는 줄은 건너뜀).

- [ ] **Step 2: 받아쓰기 대조** — 숫자·따옴표 줄은 자동, 한자어가 든 줄은 키워드로 골라 `--also`에 넣는다.

```bash
also=$(PYTHONIOENCODING=utf-8 python -c "
import re,sys; sys.path.insert(0,'../도구')
from script_io import parse_script
kw='어사|출두|환곡|사또|관아|곳간|포졸|마패|형틀|옥사|선비|나그네'
print(','.join(r['num'] for r in parse_script('대본.md') if re.search(kw,r['text'])))") && PYTHONIOENCODING=utf-8 python ../도구/tts.py check --also "$also"
```

Expected: `받아쓰기.md` 생성. ❌ 줄은 원문과 받아쓰기를 비교해 **실제 낭독 오류**면 대본 표현을 바꾸거나 `tts.py make --only 번호`로 재생성, **받아쓰기만 틀린 것**(허허→하하 등)이면 해당 wav를 직접 듣고 넘어간다.

- [ ] **Step 3: 커밋** (wav는 용량 크지만 루릭 에피소드와 같은 관행으로 커밋)

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/audio docs/전래동화/01_머슴돌쇠와낡은부채/받아쓰기.md && git commit -m "허허서방 1편: Umbriel 음성과 받아쓰기 대조

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 13: 그림 생성 (🧑‍🎨 크레딧 승인 + 루카 웹 생성)

- [ ] **Step 1: 크레딧 확인** — `higgsfield account status` 와 `gen_images.py plan`의 `z_credits`를 루카에게 보여주고 승인받는다.

- [ ] **Step 2: Z Image 생성 + 허허서방 포즈 복사**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python ../도구/gen_images.py z && PYTHONIOENCODING=utf-8 python ../도구/gen_images.py huh
```

- [ ] **Step 3: 🧑‍🎨 루카에게 `웹프롬프트.md` 전달** → 장면별 생성·다운로드 → 받을 때마다 가져오기:

```bash
f=$(ls -t /c/Users/atlia/Downloads/hf_*.png | head -1) && PYTHONIOENCODING=utf-8 python ../도구/gen_images.py import images/S02.png "$f"
```

- [ ] **Step 4: 전수 검수** — 장면을 6장씩 이어 붙인 확인 시트를 만들어 Read로 본다.

```bash
PYTHONIOENCODING=utf-8 python -c "
from PIL import Image; from pathlib import Path
fs=sorted(Path('images').glob('S*.png'))
for k in range(0,len(fs),6):
    sheet=Image.new('RGB',(1280,1080),'white')
    for i,f in enumerate(fs[k:k+6]):
        sheet.paste(Image.open(f).convert('RGB').resize((640,360)),((i%2)*640,(i//2)*360))
    sheet.save(f'_검수_{k//6+1:02d}.jpg',quality=80)
print('시트',(len(fs)+5)//6)"
```

체크: 돌쇠·최 부자 옷차림 일관 / 글자 새어 나옴 / 팔다리 개수 / 너무 어두운 밤. 문제 장면은 `gen_images.py z --only S번호`로 재생성하거나 prompts.json에서 `[SIL]`로 바꿔 재생성. `gen_images.py status` → `"missing": []`.

- [ ] **Step 5: 커밋** (검수 시트는 제외)

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/images docs/전래동화/01_머슴돌쇠와낡은부채/prompts.json && git commit -m "허허서방 1편: 장면 그림 완성(Z Image·웹·허허서방 포즈)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 14: 조립과 최종 시청 (🧑‍🎨 루카 시청 관문)

- [ ] **Step 1: 조립**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python ../도구/build.py audio && PYTHONIOENCODING=utf-8 python ../도구/build.py video && PYTHONIOENCODING=utf-8 python ../도구/build.py subs
```

Expected: `총 17~23분`, `draft.mp4 생성 완료`, `final.mp4 생성 완료 (자막 N개)`

- [ ] **Step 2: 기계 점검**

```bash
ffprobe -v error -show_entries format=duration -of csv=p=0 final.mp4 && ffmpeg -loglevel error -i final.mp4 -vf "select='not(mod(n\,9000))'" -fps_mode vfr _점검_%02d.jpg
```

Expected: 길이 = timeline total + 5초(±0.5). 5분 간격 캡처를 Read로 열어 그림·자막 정상 확인.

- [ ] **Step 3: 🧑‍🎨 루카에게 final.mp4 전달** — "틀어놓고 딴일 하면서 끝까지" 들어보고 승인. 수정 사항은 해당 Task로 돌아가 부분 재생성(음성 `--only`, 그림 `--only`) 후 Step 1 재실행.

- [ ] **Step 4: 배포용 복사본과 커밋** — 업로드용 복사본 이름을 붙이고, 영상 파일(final.mp4·draft.mp4·복사본·clips/)은 용량 때문에 커밋하지 않는다. 타임라인과 자막만 커밋.

```bash
cp final.mp4 허허서방_1편_머슴돌쇠와낡은부채.mp4 && cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/timeline.json docs/전래동화/01_머슴돌쇠와낡은부채/subs.srt && git commit -m "허허서방 1편: 조립 완료(타임라인·자막)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 15: 썸네일과 업로드 문구

**Files:**
- Create: `docs/전래동화/01_머슴돌쇠와낡은부채/썸네일.jpg`
- Create: `docs/전래동화/01_머슴돌쇠와낡은부채/배포문구.md`

- [ ] **Step 1: 썸네일 후보 3장** — 노을 실루엣 장면 1, 반전 `[WEB]` 장면 1, 허허서방 부채탁 1을 1280×720으로 맞추고 궁서 굵은 글자 3줄(흰 테두리, 마지막 줄 주황 강조) 얹기.

```bash
cd "C:/Users/atlia/Desktop/huhsame-script/docs/전래동화/01_머슴돌쇠와낡은부채" && PYTHONIOENCODING=utf-8 python -c "
from PIL import Image, ImageDraw, ImageFont
import sys
cands={'후보1':'images/S01.png','후보2':'images/S02.png','후보3':'images/S04.png'}
lines=['글 모르는 머슴이','사흘 만에','부자를 꿇렸다']
f=ImageFont.truetype('C:/Windows/Fonts/batang.ttc',110,index=2)
for name,src in cands.items():
    im=Image.open(src).convert('RGB').resize((1280,720)); d=ImageDraw.Draw(im)
    for i,t in enumerate(lines):
        d.text((60,90+i*170),t,font=f,fill=(255,140,40) if i==2 else (255,255,255),stroke_width=8,stroke_fill=(20,16,14))
    im.save(f'썸네일_{name}.jpg',quality=92)
print('ok')"
```

Read로 3장 보고 루카에게 고르게 한다 → 고른 것을 `썸네일.jpg`로 복사.

- [ ] **Step 2: 배포문구.md 작성**

```markdown
# 허허서방 1편 업로드 문구

## 제목
글 모르는 머슴이 사흘 만에 고을 부자를 무릎 꿇린 사연 | 야담 | 옛날이야기 | 잠잘때 듣는 이야기

## 설명
허허, 어서들 오시게. 장터 이야기꾼 허허서방이올시다.
식은 보리밥 한 덩이에 헛간 잠을 자던 머슴 돌쇠. 눈밭에 쓰러진 나그네를 살려주고 받은 건 낡은 부채 하나뿐이었는데…
그 부채가 사흘 뒤 고을 전체를 뒤집어 놓을 줄은 아무도 몰랐지요.

※ 그림과 목소리는 AI의 도움을 받아 만들고, 이야기는 사람이 직접 다듬습니다.
엔딩 그림: Metaluca.

#야담 #옛날이야기 #조선야담 #민담 #잠잘때듣는이야기 #오디오북 #암행어사

## 업로드 체크
- 시청자층: "아니요, 아동용이 아닙니다"
- 변경되거나 합성된 콘텐츠: 사실적인 인물 아님 → "아니요"
- 재생목록: 허허서방 옛날이야기
- 공개: 예약 공개(주 1편 페이스)
```

- [ ] **Step 3: 커밋**

```bash
cd "C:/Users/atlia/Desktop/huhsame-script" && git add docs/전래동화/01_머슴돌쇠와낡은부채/썸네일.jpg docs/전래동화/01_머슴돌쇠와낡은부채/배포문구.md && git commit -m "허허서방 1편: 썸네일과 업로드 문구

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 16: 기록 정리

- [ ] **Step 1:** 메모리 `folktale-channel-plan.md`에 1편 완성 상태·실제 크레딧·실제 길이·도구 사용법 한 줄·다음 편(2편 후보 「쫓겨난 며느리의 된장독」) 기록, MEMORY.md 한 줄 갱신.
- [ ] **Step 2:** 도구에서 겪은 함정(자막 크기, zoompan 속도, TTS 낭독 오류 패턴)을 메모리에 노하우로 추가.
- [ ] **Step 3:** 메모리 폴더 git 커밋(전역 지침: 메모리 크게 정리한 날 커밋).
